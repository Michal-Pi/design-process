---
phase: 02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - assets/scripts/tokens-project.mjs
  - assets/scripts/cli/budget-check.mjs
  - skills/workflows/style.md
  - skills/atoms/tokens/emit.md
  - skills/atoms/hifi/variants-preview.md
  - evals/fixtures/golden/tokens-project/input.json
  - evals/fixtures/golden/tokens-project/expected-shadcn.json
  - evals/fixtures/golden/tokens-project/expected-tailwind-v4.json
  - evals/fixtures/golden/tokens-project/expected-plain-css.json
  - tests/tokens/tokens-project.test.ts
autonomous: true
requirements:
  - WF-06
  - ATOM-13
  - ATOM-14
  - ADAPT-01
  - ADAPT-03
  - GATE-08
  - FID-05
  - COST-05
  - MVPA-04

must_haves:
  truths:
    - "tokens-project.mjs emits valid DTCG v2025.10 JSON with three tiers (primitive, semantic, component) and frontmatter evidence:INFERRED stage:5a-lite"
    - "tokens-project.mjs shadcn adapter emits CSS variables in components/ wrappers (never modifying components/ui/)"
    - "tokens-project.mjs tailwind-v4 adapter emits a valid @theme block merging with (not replacing) existing app/globals.css content"
    - "tokens-project.mjs plain-css adapter emits :root { } CSS custom properties"
    - "tokens-project.mjs emits deterministic output (golden test passes 5x byte-identical)"
    - "gate-stage-5a-complete is NOT modified — still hard-coded to return not_runnable reason:stage-4-artifacts-absent (D-43)"
    - "style workflow SKILL.md includes explicit step asserting gate-stage-5a returns not_runnable (BLOCKER test documented)"
    - "budget-check.mjs CLI script enforces p50 ≤55k soft warn and 110k hard-stop for style stage (D-49)"
    - "skills/workflows/style.md and two atom SKILL.md files exist"
  artifacts:
    - path: "assets/scripts/tokens-project.mjs"
      provides: "DTCG v2025.10 emit + three adapter outputs (shadcn, tailwind-v4, plain-css) — D-41"
      contains: "evidence: INFERRED"
    - path: "assets/scripts/cli/budget-check.mjs"
      provides: "Per-stage token budget soft/hard enforcement CLI (D-49)"
      contains: "budgetP50"
    - path: "skills/workflows/style.md"
      provides: "W6-lite style workflow SKILL.md (Stage 5a-lite)"
      contains: "stage: 5a"
    - path: "skills/atoms/tokens/emit.md"
      provides: "ATOM-14 tokens/emit atom"
      contains: "DTCG"
    - path: "skills/atoms/hifi/variants-preview.md"
      provides: "ATOM-13 hifi/variants-preview atom"
      contains: "Playwright"
    - path: "evals/fixtures/golden/tokens-project/expected-shadcn.json"
      provides: "Golden fixture for tokens-project.mjs shadcn adapter determinism"
      contains: "evidence"
    - path: "tests/tokens/tokens-project.test.ts"
      provides: "Unit + golden tests for tokens-project.mjs"
      contains: "INFERRED"
  key_links:
    - from: "skills/workflows/style.md"
      to: "assets/scripts/tokens-project.mjs"
      via: "Bash step: node assets/scripts/tokens-project.mjs --design-dir design/ --adapter <detected>"
      pattern: "tokens-project.mjs"
    - from: "assets/scripts/tokens-project.mjs"
      to: "assets/scripts/routing/registry.mjs"
      via: "detectStack(projectRoot) → adapterSignal"
      pattern: "detectStack"
    - from: "skills/workflows/style.md"
      to: "assets/scripts/gates/stage-5a.mjs"
      via: "Bash step: node assets/scripts/cli/gate.mjs --stage 5a"
      pattern: "not_runnable"
---

## Plan 02-03 — Style-Lite Workflow + DTCG Tokens Emit + Three Adapter Paths + Stage 5a Gate Assertion

**Goal:** Fulfill ROADMAP Phase 2 Success Criterion 1 (tokens + DESIGN.md layer) and Criterion 3 (style stage under 55k tokens): ship `tokens-project.mjs` with three adapter paths; ship the `style` workflow SKILL.md (W6-lite) and two atoms (ATOM-14 tokens/emit, ATOM-13 hifi/variants-preview); enforce D-43 (gate-stage-5a never returns pass) in CI; ship `budget-check.mjs` for per-stage token enforcement.

**Locked decisions consumed:** D-41 (DTCG primitive→semantic→component tiers; three adapters), D-42 (all style-lite output labeled stage:5a-lite, evidence:INFERRED), D-43 (gate/stage-5a-complete hard-coded not_runnable — DO NOT TOUCH stage-5a.mjs), D-48 (adapter detection from registry.mjs), D-49 (soft warn at p50, hard-stop at 2× p50), D-51 (INFERRED is only schema-valid value for v2.0a Stage 5a/5b), D-52 (diff-by-default, --apply required), D-53 (host fallback)

**Phase 1 dependencies:** `assets/scripts/gates/stage-5a.mjs` (ALREADY hard-coded not_runnable — DO NOT MODIFY), `assets/scripts/routing/registry.mjs` (detectStack() for adapter signal), `assets/scripts/preview/variant-distance.mjs` (6-axis visual diversity check on 3 variants), `assets/scripts/port-manager.mjs` + `assets/scripts/playwright-runner.mjs` + `assets/scripts/security-sandbox.mjs` (preview spawn), `assets/scripts/preview/vite-adapter.mjs` + `next-adapter.mjs` + `astro-adapter.mjs`, `assets/scripts/verify-golden.mjs` (add tokens-project golden fixture), `references/dtcg-v2025-10.md`, `references/shadcn-tailwind-v4.md`, `references/wcag-2-2.md`

**Open research flags resolved in this plan:**
- Pitfall NEW-2 (`@theme` import path ambiguity): tokens-project.mjs tailwind-v4 adapter writes to app/globals.css. If the file exists and already has an `@theme { }` block, the adapter merges (adds new CSS custom properties under the existing block header; does not replace). If no globals.css exists, create it. If globals.css exists but has no @theme block, append a new `@theme { }` block. The merge strategy is additive-only — never removes existing tokens.
- Claude's Discretion (tokens-project.mjs adapter dispatch): switch on adapter name, matching registry.mjs discriminated-union pattern. Implements exhaustiveness check via `assertNever` per ESLint switch-exhaustiveness-check rule.
- Claude's Discretion (slop-tell pattern loading): loaded from `references/slop-tells/heuristics.md` at runtime for maintainability (this plan ships the heuristics.md file; Plan 02-05 ships slop-tells.mjs that loads it).

---

### CRITICAL: D-43 enforcement — do not touch stage-5a.mjs

`assets/scripts/gates/stage-5a.mjs` already hard-codes `not_runnable` for empty interactions/. This plan must NOT modify that file. The style workflow SKILL.md procedure body must include an explicit step: "Bash: node assets/scripts/cli/gate.mjs --stage 5a --design-dir design/". The expected result is not_runnable — this is correct behavior for v2.0a, not an error. The workflow surfaces this to the user as: "Stage 5a gate returns not_runnable (expected in v2.0a — Stage 4 interaction artifacts are required for full gate pass, which ships in v2.0b). Your tokens and DESIGN.md are labeled evidence:INFERRED." A CI test in this plan asserts that running the gate on a fixture with no interactions/ still returns not_runnable (regression guard per D-43).

### Anti-patterns to avoid

- **Pitfall 13 (style-lite BLOCKER):** Do NOT write workflow text that implies the style gate can return PASS. The user-facing messaging must use "provisional", "evidence:INFERRED", and "requires Stage 4 artifacts (v2.0b)".
- **CLAUDE.md shadcn rule:** shadcn adapter writes wrappers to `components/` — NEVER touches `components/ui/`. Use semantic color tokens (`bg-primary`, `text-destructive`) in wrappers. Never raw Tailwind colors (`bg-blue-500`).
- **Plain CSS adapter:** Tailwind v4 @theme block and shadcn CSS vars are separate outputs. The plain-CSS adapter emits modern CSS (`:root { --color-primary: oklch(60% 0.2 270); }` — CSS Color 4, not hex).
- **DTCG compliance:** All emitted tokens must use `$type` field per DTCG v2025.10 spec. The `$value` must be in the canonical format for each type. Do NOT use DTCG v1 or Style Dictionary proprietary format.

---

<objective>
Ship `assets/scripts/tokens-project.mjs` — the DTCG v2025.10 emit script with three adapter projections; ship `assets/scripts/cli/budget-check.mjs` for per-stage cost gate enforcement; ship the `style` workflow SKILL.md (W6-lite), `tokens/emit` atom (ATOM-14), and `hifi/variants-preview` atom (ATOM-13); write golden tests for tokens-project.mjs determinism; assert the stage-5a gate regression (D-43).

Purpose: tokens-project.mjs is the core script deliverable of the style-lite stage — it translates LLM-picked color/scale decisions into machine-validated DTCG JSON and three adapter projections. It must be deterministic (golden tests), pass lint-determinism (no LLM imports), and emit artifacts labeled evidence:INFERRED so the honest provenance contract holds.

Output: tokens-project.mjs, budget-check.mjs, 3 SKILL.md files, golden fixtures, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-CONTEXT.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-02-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-01-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 registry.mjs -->
From assets/scripts/routing/registry.mjs:
  export function detectStack(projectRoot) → { nextjs: boolean, nextMajor?: number, tailwindV4: boolean, shadcn: boolean, vite: boolean, astro: boolean }
  Adapter selection (D-48):
    nextjs && tailwindV4 && shadcn → adapter: 'shadcn'
    tailwindV4 (without shadcn) → adapter: 'tailwind-v4'
    otherwise → adapter: 'plain-css'

From assets/scripts/gates/stage-5a.mjs (Phase 1 — DO NOT MODIFY):
  Returns {kind:'not_runnable', reason:'stage-4-artifacts-absent'} when design/interactions/ is empty or absent.
  This is the Phase 1 GATE-07/GATE-08 hard-code. Phase 2 must NOT override it.

From references/dtcg-v2025-10.md:
  DTCG v2025.10 token shape: { $type: 'color'|'dimension'|'fontFamily'|..., $value: ..., $description?: string }
  Three tiers: primitive (raw OKLCH values), semantic (role-mapped aliases), component (component-specific tokens)
  File extension: .tokens.json; media type: application/design-tokens+json

From references/shadcn-tailwind-v4.md:
  shadcn/ui CSS variable naming: --background, --foreground, --primary, --primary-foreground, etc.
  Tailwind v4 @theme block: @theme { --color-background: oklch(...)... }

From assets/scripts/preview (Phase 1):
  port-manager.mjs: export async function reservePort(label) → { port, release }
  playwright-runner.mjs: export async function spawnAndProbe(serverCmd, port, opts) → { screenshots, ready }
  security-sandbox.mjs: export function isPathAllowed(p, projectRoot) + scrubEnvForPreview(env)
  variant-distance.mjs: 6-axis visual distance; returns 0..1 diversity score

DTCG tokens-project.mjs output contract (new script — defined in this plan):
  Input: {
    adapter: 'shadcn' | 'tailwind-v4' | 'plain-css',
    colorPrimary: string (OKLCH e.g. 'oklch(60% 0.2 270)'),
    colorBackground: string,
    colorForeground: string,
    borderRadius: string (e.g. '0.5rem'),
    fontFamilyBase: string (e.g. 'Inter, system-ui'),
    spacingBase: number (px, e.g. 4),
    designDir: string (path to design/),
    projectRoot: string,
    generatedAt?: string (ISO datetime for golden tests)
  }
  Output: {
    tokensPath: string (design/tokens.json),
    projectionPath: string (adapter-specific),
    adapterUsed: string
  }
  Side effects: writes design/tokens.json (DTCG) + adapter projection file to staging area
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-02-03-A: tokens-project.mjs — DTCG emit + three adapters + golden tests + budget-check.mjs</name>
  <files>assets/scripts/tokens-project.mjs, assets/scripts/cli/budget-check.mjs, evals/fixtures/golden/tokens-project/input.json, evals/fixtures/golden/tokens-project/expected-shadcn.json, evals/fixtures/golden/tokens-project/expected-tailwind-v4.json, evals/fixtures/golden/tokens-project/expected-plain-css.json, tests/tokens/tokens-project.test.ts</files>
  <behavior>
    - tokens-project.mjs shadcn adapter: emits design/tokens.json with stage:'5a-lite', evidence:'INFERRED'; emits .complete-design/preview/run-<id>/components/ CSS wrapper (never modifies components/ui/)
    - tokens-project.mjs tailwind-v4 adapter: emits @theme block; if app/globals.css exists with @theme block, merges (additive, no removal); if no @theme block, appends
    - tokens-project.mjs plain-css adapter: emits :root { } CSS custom properties with OKLCH values
    - tokens-project.mjs: running twice with same input produces byte-identical design/tokens.json (deterministic via fixed generatedAt parameter; canonicalize key-sort)
    - tokens-project.mjs: emitted design/tokens.json passes ajv validation against DTCG v2025.10 schema (all tokens have $type and $value)
    - tokens-project.mjs: all tokens carry evidence:'INFERRED' in the tokens.json frontmatter (not per-token — at the file root)
    - budget-check.mjs --stage style --check post: if token count in run-log.jsonl exceeds 55k → logs WARNING; if exceeds 110k → exits 1 (requires --continue-anyway flag to proceed)
    - budget-check.mjs --stage discover --check post: p50 threshold 30k, hard-stop 60k
    - budget-check.mjs --stage structure --check post: p50 threshold 25k, hard-stop 50k
  </behavior>
  <action>Create `assets/scripts/tokens-project.mjs` (ESM pure, no LLM imports — must pass lint-determinism). The script exports `emitTokens(options)` and also runs as a CLI via `node assets/scripts/tokens-project.mjs --design-dir design/ --adapter shadcn [--generated-at ISO]`. Internal structure: (1) Define DTCG primitive tier: colorPrimary, colorBackground, colorForeground, colorBorder (auto-derived: mix(background, foreground, 15%)), plus dimension tokens (borderRadius, spacingBase × 8-step scale: 0.5×, 1×, 1.5×, 2×, 3×, 4×, 6×, 8×). (2) Define semantic tier: color.background (maps to primitive.colorBackground), color.foreground, color.primary, color.primary-foreground (auto: lightness-flip of primary), color.border, color.muted, color.muted-foreground. (3) Define component tier: button.background (= color.primary), button.foreground (= color.primary-foreground), input.border (= color.border), card.background (= color.background). (4) Assemble DTCG JSON with $schema reference, three $groups (primitive, semantic, component), and tokens in W3C DTCG v2025.10 format. Add frontmatter YAML block at file top: artifact:tokens, stage:5a-lite, evidence:INFERRED, schemaVersion:1, generated:<generatedAt or new Date().toISOString()>. Run output through canonicalize() for determinism. (5) Adapter dispatch using switch on adapterSignal with assertNever for exhaustiveness (ESLint switch-exhaustiveness-check): shadcn case → emitShadcnAdapter(tokens, stagingDir, projectRoot): reads existing components/ CSS files via globby; writes .complete-design/preview/run-<id>/complete-design-tokens.css with CSS custom properties using shadcn naming (--background, --foreground, --primary, etc.); writes a sample wrapper component at .complete-design/preview/run-<id>/components/complete-design-theme-provider.tsx that imports the CSS file (uses semantic color tokens via var(), not raw values; never touches components/ui/). tailwind-v4 case → emitTailwindV4Adapter(tokens, stagingDir, projectRoot): reads projectRoot/app/globals.css if it exists; if @theme block found, append new complete-design properties inside it; if no @theme block, append "@theme { <new props> }" to end of file; write merged result to .complete-design/preview/run-<id>/app/globals.css. plain-css case → emitPlainCssAdapter(tokens, stagingDir): write :root { --color-primary: oklch(...); ... } to .complete-design/preview/run-<id>/complete-design-tokens.css. (6) Return {tokensPath, projectionPath, adapterUsed}. Write golden fixtures: create evals/fixtures/golden/tokens-project/input.json (fixed seed input with generatedAt:'2026-05-25T00:00:00.000Z'), expected-shadcn.json, expected-tailwind-v4.json, expected-plain-css.json (generated by running the script on the input). Write tests/tokens/tokens-project.test.ts: (a) unit test each adapter; (b) golden test: run emitTokens on input.json 5× with fixed generatedAt, assert byte-identical output matching expected fixture; (c) DTCG schema validation test: emitted JSON passes ajv against DTCG spec. First check if `dtcg-lint.mjs` exists (expected Phase 1 artifact from RESEARCH.md §3) — if found, use it. If absent (it is NOT in the 01-01-SUMMARY key_files list, confirming it was not shipped by Phase 1), use a structural check: (a) validate every token in the DTCG body has a `$type` field and a `$value` field using ajv with an inline JSON Schema; (b) validate known `$type` values against the DTCG v2025.10 allowed types enum (color, dimension, fontFamily, fontStyle, fontWeight, number, duration, cubicBezier, strokeStyle, border, transition, shadow, gradient, typography). Document which validation path was used in the test file header with a comment: "DTCG validation: using structural ajv check (dtcg-lint.mjs not in Phase 1 deliverables per 01-01-SUMMARY — F-11)." (F-11 fix). Create `assets/scripts/cli/budget-check.mjs` (CLI subcommand per auto-discovery pattern from Phase 1). Command name: 'budget-check'. Options: --stage (discover|structure|style|systematize|design-bug|brand-refresh|PR-audit), --check (pre|post), --run-log (default: .complete-design/private/run-log.jsonl). Budget table (per D-49): discover: p50=30k, hard=60k; structure: p50=25k, hard=50k; style: p50=55k, hard=110k; systematize: p50=40k, hard=80k; design-bug: p50=20k, hard=40k; brand-refresh: p50=55k, hard=110k; PR-audit: p50=15k, hard=30k. Post-check reads last entry from run-log.jsonl for the given stage; if tokensUsed > hard: exit 1 (unless --continue-anyway flag); if tokensUsed > p50: logs warning and exits 0. Pre-check just logs the expected budget as informational. Add tokens-project golden fixtures to verify-golden.mjs fixture list (extend the FIXTURES array in the script per Phase 1 pattern). Implements D-41, D-42, D-48, D-49, ADAPT-01, ADAPT-03, MVPA-04, COST-05.</action>
  <verify>
    <automated>npx tsc --noEmit && node assets/scripts/lint-determinism.mjs --scope assets/scripts/ && npx vitest run tests/tokens/tokens-project.test.ts --reporter=verbose</automated>
  </verify>
  <done>tokens-project.mjs emits valid DTCG JSON (unit test passes) with evidence:INFERRED in frontmatter; all three adapters emit correctly; golden tests confirm 5x byte-identical output; lint-determinism passes; budget-check.mjs CLI registered and handles all 7 route budgets.</done>
</task>

<task type="auto">
  <name>T-02-03-B: Style workflow SKILL.md + tokens/emit atom + hifi/variants-preview atom + D-43 CI regression</name>
  <files>skills/workflows/style.md, skills/atoms/tokens/emit.md, skills/atoms/hifi/variants-preview.md, tests/gates/stage-5a-not-runnable-regression.test.ts</files>
  <action>Create `skills/workflows/style.md` (W6-lite style workflow, per D-32). Frontmatter: name="complete-design/style", description≤200 chars ("Style-lite: emit provisional DTCG tokens (3 variants), run hi-fi preview with Playwright; gate returns not_runnable (v2.0b needed for full pass)"), stage:"5a", gate:"gate/stage-5a-complete", artifacts.reads:["design/.handoff/stage-2-bundle.md"], artifacts.writes:["design/tokens.json","design/.handoff/stage-5a-bundle.md"], composition.atoms:["tokens/emit","hifi/variants-preview"], mvp:true, compatibility:[claude-code,codex-cli,cursor], allows-tools:[Read,Write,Bash]. Procedure body: 1. Read design/.handoff/stage-2-bundle.md. If absent, halt: "Run structure workflow first." 2. Pre-check token budget: "Bash: node assets/scripts/cli/budget-check.mjs --stage style --check pre". 3. Load references: references/dtcg-v2025-10.md, references/shadcn-tailwind-v4.md, references/wcag-2-2.md. 3a. --depth dispatch (F-07): if `--depth lightweight` → skip TRUST-05 intake, use defaults (brand: professional/calm, primary: oklch(60% 0.15 260), font: system-ui), generate 1 token variant only. If `--depth full` → expand to 5 brand questions + 3 preview variants + extended contrast report. Default is `standard` (3 questions, 3 variants). 4. TRUST-05 intake (3 questions, standard/full only): (a) "What is the brand personality? (e.g., professional/calm, playful/energetic, technical/minimal)", (b) "Primary brand color? (hex, oklch, or 'choose for me')", (c) "Font family preference? (or 'default to system-ui stack')". Do NOT generate tokens before user responds. 5. Based on user responses, select a color palette (3 OKLCH values: primary, background, foreground) and border-radius (0.25rem tight → 1rem rounded). Note: REPORT measured WCAG 2.2 AA contrast ratios — never CLAIM WCAG conformance (TRUST posture per CLAUDE.md). 6. Inline ATOM-14 (tokens/emit): "Bash: node assets/scripts/tokens-project.mjs --design-dir design/ --adapter <detected from registry.mjs> --color-primary '<oklch>' --color-background '<oklch>' --color-foreground '<oklch>' --border-radius '<rem>'". This emits design/tokens.json (DTCG) to staging area. 7. Inline ATOM-13 (hifi/variants-preview): Spawn 3 preview variants with slightly different palette variations (±10% lightness on primary). Each variant is a separate preview run via the preview harness. Screenshots are captured. Diversity check: ensure 6-axis variant-distance ≥0.15 between variants. 8. Post-check token budget: "Bash: node assets/scripts/cli/budget-check.mjs --stage style --check post". 9. IMPORTANT — Gate invocation (D-43): "Bash: node assets/scripts/cli/gate.mjs --stage 5a --design-dir design/". This will return not_runnable. This is EXPECTED and CORRECT in v2.0a. Message to user: 'Stage 5a gate returns not_runnable — this is the expected v2.0a behavior. Stage 4 interaction artifacts (Mermaid stateDiagram-v2 + XState machines) are required for the full gate pass, which ships in v2.0b. Your tokens.json and preview screenshots are labeled evidence:INFERRED.' Do NOT treat not_runnable as a workflow failure. 10. Handoff bundle: "Bash: node assets/scripts/handoff-bundle-build.mjs --stage 5a --design-dir design/". 11. Present staged artifacts. Await --apply to write to design/. Include ## Host fallback section (D-53). Create `skills/atoms/tokens/emit.md` (ATOM-14, per D-33). Frontmatter: name="complete-design/tokens/emit", description≤200 chars ("Emit DTCG v2025.10 design tokens JSON + adapter projection (Tailwind v4 @theme / shadcn CSS vars / plain :root); all evidence:INFERRED"), stage:"5a", mvp:true. Body: ## Standalone bootstrap (if no stage-2 bundle: ask "What adapter does your project use? (shadcn/tailwind-v4/plain-css)") + ## Workflow procedure. Procedure: 1. Detect adapter via registry.mjs (or use --adapter flag if provided). 2. Invoke tokens-project.mjs with LLM-provided color/scale values. 3. Validate emitted design/tokens.json against DTCG schema. 4. Return {tokensPath, projectionPath, adapterUsed}. Create `skills/atoms/hifi/variants-preview.md` (ATOM-13, per D-33). Frontmatter: name="complete-design/hifi/variants-preview", description≤200 chars ("Spawn 3 hi-fi preview variants via Playwright + stage adapter; capture screenshots; check 6-axis visual diversity ≥0.15"), stage:"5a", mvp:true. Body: ## Standalone bootstrap + ## Workflow procedure. Procedure: 1. Read design/tokens.json from staging area. 2. For each of 3 palette variants: use port-manager to reserve port; spawn dev server via appropriate adapter (next-adapter.mjs, vite-adapter.mjs, astro-adapter.mjs); probe readiness via playwright-runner.mjs spawnAndProbe; capture screenshot; release port. 3. Compute variant-distance between screenshots. If any pair < 0.15 diversity → log WARNING (don't halt; variants are exploratory). Write `tests/gates/stage-5a-not-runnable-regression.test.ts`: import runGate from base.mjs; call runGate('5a', fixtureDir, {}) where fixtureDir has no interactions/ directory; assert result.kind === 'not_runnable' AND result.reason === 'stage-4-artifacts-absent'. This is the D-43 regression guard. Also call runGate('5a', fixtureWithInteractionsDir, {}) where the dir HAS interactions/ — this should still return not_runnable in v2.0a because gate-stage-5a.mjs is hard-coded (interactions/ exists but Stage 4 specs are not analyzed in v2.0a). Implements D-32, D-33, D-41, D-42, D-43, D-48, D-52, D-53, WF-06, ATOM-13, ATOM-14, FID-05, GATE-08, MVPA-04, TRUST-05.</action>
  <verify>
    <automated>test -f skills/workflows/style.md && test -f skills/atoms/tokens/emit.md && test -f skills/atoms/hifi/variants-preview.md && npx vitest run tests/gates/stage-5a-not-runnable-regression.test.ts --reporter=verbose && node -e "import('gray-matter').then(({default:m}) => { ['skills/workflows/style.md','skills/atoms/tokens/emit.md','skills/atoms/hifi/variants-preview.md'].forEach(f => { const r = m.read(f); if (!r.data.name) throw new Error(f+' missing name'); if (r.data.description && r.data.description.length > 200) throw new Error(f+' description >200 chars'); console.log(f, 'OK:', r.data.name); }); })"</automated>
  </verify>
  <done>Three SKILL.md files exist with valid frontmatter; style.md includes TRUST-05 intake (3 questions), explicit D-43 gate step with not_runnable messaging, ## Host fallback; tokens/emit.md and hifi/variants-preview.md have ## Standalone bootstrap + ## Workflow procedure; D-43 regression test asserts gate-stage-5a always returns not_runnable; no stage-5a.mjs modification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM color picks → tokens-project.mjs | LLM provides OKLCH color strings as CLI args; must be validated before emitting |
| tokens-project.mjs → user's app/globals.css | Script writes into user's project; additive-only merge prevents data loss |
| preview spawn → filesystem | Preview harness spawns dev servers; security-sandbox.mjs scrubs env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | design/tokens.json evidence field | mitigate | frontmatter-validate.mjs extended in Plan 02-01 rejects evidence:validated on 5a-lite artifacts (D-51). Golden test asserts evidence:INFERRED field. |
| T-02-03-02 | Tampering | app/globals.css @theme merge | mitigate | Tailwind v4 adapter is additive-only (never removes tokens). Write to staging area first; --apply required to write to project root (D-52). |
| T-02-03-03 | Information Disclosure | preview spawn environment | mitigate | security-sandbox.mjs scrubEnvForPreview() removes secrets before server spawn (Phase 1 Plan 05). |
| T-02-03-04 | Elevation of Privilege | WCAG conformance claim | mitigate | TRUST posture: tokens-project.mjs reports measured contrast ratios, never claims WCAG compliance. Workflow procedure body includes TRUST note. |
| T-02-03-05 | Denial of Service | Playwright preview timeout | accept | playwright-runner.mjs uses AbortSignal.timeout (Phase 1). Preview failures log warning and skip screenshot rather than halting the full workflow. |
</threat_model>

<verification>
After all tasks complete:

```bash
# Tokens emit + golden tests
npx vitest run tests/tokens/tokens-project.test.ts --reporter=verbose

# D-43 regression guard
npx vitest run tests/gates/stage-5a-not-runnable-regression.test.ts --reporter=verbose

# Lint determinism (tokens-project.mjs must have no LLM imports)
node assets/scripts/lint-determinism.mjs --scope assets/scripts/

# SKILL.md files parseable + descriptions ≤200 chars
node -e "import('gray-matter').then(({default:m}) => { ['skills/workflows/style.md','skills/atoms/tokens/emit.md','skills/atoms/hifi/variants-preview.md'].forEach(f => { const r = m.read(f); console.log(f, r.data.name, 'desc:', r.data.description?.length); }); })"

# TypeScript clean
npx tsc --noEmit
```
</verification>

<success_criteria>
- tokens-project.mjs emits DTCG v2025.10 JSON with evidence:INFERRED frontmatter; all 3 adapter paths tested
- Golden tests confirm 5x byte-identical output from tokens-project.mjs
- lint-determinism passes on assets/scripts/ (no LLM imports in tokens-project.mjs)
- D-43 regression test passes (gate-stage-5a always returns not_runnable)
- budget-check.mjs CLI registered for 7 stage budgets
- 3 SKILL.md files exist with valid ≤200-char descriptions
- style.md includes explicit D-43 not_runnable step with correct user messaging
- tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-03-SUMMARY.md`.
</output>
