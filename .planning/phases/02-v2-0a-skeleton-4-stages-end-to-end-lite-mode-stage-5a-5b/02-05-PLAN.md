---
phase: 02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b
plan: 05
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - assets/scripts/audit/slop-tells.mjs
  - assets/scripts/audit/stage-5a-pr.mjs
  - assets/scripts/audit/stage-5b-pr.mjs
  - assets/scripts/cli/apply.mjs
  - assets/scripts/routing/dispatch.mjs
  - skills/workflows/ingest.md
  - skills/atoms/prd/parse-or-interview.md
  - skills/workflows/audit.md
  - skills/design/SKILL.md
  - evals/fixtures/e2e/next15-tailwind4-shadcn/PRD.md
  - evals/fixtures/e2e/next15-tailwind4-shadcn/next.config.mjs
  - evals/fixtures/e2e/next15-tailwind4-shadcn/package.json
  - evals/fixtures/e2e/next15-tailwind4-shadcn/app/globals.css
  - evals/fixtures/e2e/next15-tailwind4-shadcn/components/ui/.gitkeep
  - tests/e2e/new-feature-route.test.ts
  - tests/audit/slop-tells.test.ts
  - tests/audit/stage-5a-pr.test.ts
  - tests/audit/stage-5b-pr.test.ts
  - tests/routing/dispatch-real-stages.test.ts
  - tests/cli/apply.test.ts
  - references/slop-tells/heuristics.md
autonomous: true
requirements:
  - WF-01
  - WF-08
  - WF-09
  - ATOM-01
  - ROUTE-02
  - ROUTE-04
  - ROUTE-05
  - ROUTE-07
  - ROUTE-09
  - AUDIT-01
  - AUDIT-03
  - AUDIT-05
  - AUDIT-08
  - DIST-04
  - COST-08
  - COST-09
  - MVPA-01
  - MVPA-02
  - MVPA-03
  - MVPA-05
  - MVPA-06
  - MVPA-07
  - MVPA-08

must_haves:
  truths:
    - "design --route new-feature on the Next 15 + Tailwind v4 + shadcn e2e fixture produces design/research/personas/*.persona.json, design/ia/sitemap.json, design/tokens.json, design/DESIGN.md — all labeled evidence:INFERRED — in the correct design/ directory"
    - "audit --slop-tells detects rainbow gradient, Inter-default, glass-stack, three-column-grid patterns from fixture seeded with slop CSS"
    - "audit --pr emits severity-ranked AUDIT-REPORT.md validated against audit-report.v1.json with findingId, evidence pointer, fix recipe"
    - "apply.mjs copies staging artifacts from .design-os/preview/run-<id>/ to design/ on --apply; warns on file conflict but proceeds by default; --no-overwrite aborts on conflict"
    - "dispatch.mjs wires real runSubagent calls for 4 implemented routes (new-feature, design-bug, brand-refresh, PR-audit); 3 unimplemented routes still return ROUTE_NOT_YET_IMPLEMENTED"
    - "ingest workflow SKILL.md and parse-or-interview atom exist with auto-detect PRD logic (D-35)"
    - "audit workflow SKILL.md exists with --slop-tells and --pr modes documented"
    - "Phase 2 CI: cross-host smoke test extends evals/hosts workspaces to run at least 2 of the 5 new workflows on Codex/Cursor sequential paths"
  artifacts:
    - path: "assets/scripts/audit/slop-tells.mjs"
      provides: "Deterministic slop-tell pattern matcher (D-46) loading patterns from references/slop-tells/heuristics.md"
      contains: "rainbow"
    - path: "assets/scripts/audit/stage-5a-pr.mjs"
      provides: "Stage 5a PR diff detector (D-45)"
      contains: "design/tokens"
    - path: "assets/scripts/audit/stage-5b-pr.mjs"
      provides: "Stage 5b PR diff detector (D-45)"
      contains: "evidence"
    - path: "assets/scripts/cli/apply.mjs"
      provides: "apply.mjs: copy from .design-os/preview/run-<id>/ to design/ with --apply flag (D-52)"
      contains: "design-os/preview"
    - path: "assets/scripts/routing/dispatch.mjs"
      provides: "Extended dispatch.mjs with real runSubagent calls for 4 routes (OF-01 resolved)"
      contains: "runSubagent"
    - path: "skills/workflows/ingest.md"
      provides: "W0 ingest workflow SKILL.md (D-35 PRD auto-detect)"
      contains: "stage: 0"
    - path: "skills/atoms/prd/parse-or-interview.md"
      provides: "ATOM-01 parse-or-interview atom"
      contains: "Lenny"
    - path: "skills/workflows/audit.md"
      provides: "W8-basic audit workflow SKILL.md (--slop-tells + --pr)"
      contains: "slop-tells"
    - path: "evals/fixtures/e2e/next15-tailwind4-shadcn/PRD.md"
      provides: "Minimal PRD for end-to-end fixture"
      contains: "artifact: prd"
    - path: "tests/e2e/new-feature-route.test.ts"
      provides: "End-to-end test: design --route new-feature on fixture (script-level, no LLM)"
      contains: "new-feature"
    - path: "references/slop-tells/heuristics.md"
      provides: "Slop-tell pattern definitions loaded at runtime by slop-tells.mjs"
      contains: "rainbow-gradient"
  key_links:
    - from: "assets/scripts/routing/dispatch.mjs"
      to: "assets/scripts/run-subagent.mjs"
      via: "runSubagent({ prompt: stageWorkflowPath, context: {...} })"
      pattern: "runSubagent"
    - from: "assets/scripts/audit/slop-tells.mjs"
      to: "references/slop-tells/heuristics.md"
      via: "fs.readFile at runtime"
      pattern: "heuristics.md"
    - from: "assets/scripts/cli/apply.mjs"
      to: "design/"
      via: "globby + fs.copyFile from .design-os/preview/run-<id>/"
      pattern: "design-os/preview"
---

## Plan 02-05 — Audit Scripts + apply.mjs + Ingest Workflow + Dispatch Wiring + End-to-End Fixture

**Goal:** Fulfill ROADMAP Phase 2 Success Criteria 1 (end-to-end), 3 (cost budgets), 4 (audit working), and 5 (cross-host scaffold): ship `audit --slop-tells` + `audit --pr`, `apply.mjs`, wire real stage dispatch into `dispatch.mjs` for all 4 implemented routes, ship the `ingest` workflow SKILL.md (W0) + `parse-or-interview` atom (ATOM-01), ship the `audit` workflow SKILL.md (W8-basic), create the Next 15 + Tailwind v4 + shadcn end-to-end fixture, and extend the cross-host smoke test.

**Locked decisions consumed:** D-35 (PRD auto-detect via registry.mjs; fall back to Lenny 1-pager interview if absent or < 50 tokens), D-45 (audit --pr diff walker: git diff --name-only HEAD~1 or $GITHUB_BASE_REF), D-46 (slop-tells: deterministic regex patterns loaded from heuristics.md), D-47 (AUDIT-REPORT.md validated against audit-report.v1.json; findingId schema `<stage>-<type>-<seq>`), D-53 (host fallback in each SKILL.md)

**Phase 1 dependencies used:** `assets/scripts/routing/dispatch.mjs` (extend with real runSubagent calls), `assets/scripts/run-subagent.mjs` (host-detection + Claude Code + sequential fallback), `assets/scripts/routing/registry.mjs` (detectStack for PRD detection and adapter detection), `schemas/dist/audit-report.v1.json` (validate AUDIT-REPORT.md output), `evals/hosts/claude-code/`, `evals/hosts/codex-cli/`, `evals/hosts/cursor/` (extend with Phase 2 workflow tests)

**Open research flags resolved in this plan:**
- OF-01 (`dispatch.mjs` Phase 2 extension): Real `runSubagent()` call (Claude Code native) with sequential-fallback shim for Codex/Cursor. The dispatch loop calls `await runSubagent({ prompt: stageWorkflowPath, context: { designDir, bundlePath } })` for each required stage per the route definition. The stub objects from Phase 1 (`{kind:'route_stub_dispatched'}`) are replaced with actual runSubagent invocations. The sequential-fallback for Codex/Cursor is already in `run-subagent.mjs` (Phase 1 Plan 05) — dispatch.mjs just calls the same API.
- OF-03 (bundle sufficiency for Stage 1→2 and Stage 2→5a): The e2e fixture ships a representative Stage 1 bundle (`stage-1-to-2/bundle.md` updated in Plan 02-01) and Stage 2 bundle. The sufficiency eval is verified as part of the e2e fixture setup.
- OF-04 (`apply.mjs` conflict default): Default is overwrite with WARNING logged to run-log.jsonl. `--no-overwrite` flag aborts on conflict (no .bak files — diffing is the user's responsibility; the TRUST-02 `--apply` gate is the protection).
- Claude's Discretion (slop-tells.mjs): Patterns loaded from `references/slop-tells/heuristics.md` at runtime for maintainability. The heuristics.md file is committed to git; updates to patterns are visible in git diff.
- Claude's Discretion (apply.mjs implementation): globby v14 batch copy (matches Phase 1 globby precedent) rather than per-file fs.copyFile.

---

### Anti-patterns to avoid

- **Anti-Pattern 1 (LLM for slop-tell detection):** slop-tells.mjs is pure regex matching. No LLM calls. Must pass lint-determinism.
- **Anti-Pattern 2 (PR walker reading raw design/):** `audit --pr` reads ONLY the handoff bundle for context (stage-5b-bundle.md or latest available). It does NOT walk the full design/ directory for every PR.
- **audit --reverse-engineer-stages (Phase 3 item):** Do NOT implement or mention this in the audit workflow SKILL.md. The audit SKILL.md supports only `--slop-tells` and `--pr` modes in v2.0a.
- **Full new-product route (Phase 3 item):** dispatch.mjs wires exactly 4 routes: `new-feature`, `design-bug`, `brand-refresh`, `PR-audit`. The 3 unimplemented routes (`new-product`, `mature-app-refactor`, `DS-extraction`) continue to return `ROUTE_NOT_YET_IMPLEMENTED`.

---

<objective>
Ship the final set of Phase 2 deliverables: `slop-tells.mjs`, `stage-5a-pr.mjs`, `stage-5b-pr.mjs` (audit scripts), `apply.mjs` (D-52 staging area copy), the `ingest` workflow SKILL.md + `parse-or-interview` atom, the `audit` workflow SKILL.md, extended `dispatch.mjs` with real runSubagent wiring for 4 routes, the Next 15 + Tailwind v4 + shadcn end-to-end eval fixture, and cross-host smoke test extension.

Purpose: This plan closes the loop on Phase 2's "shippable standalone" criterion. After this plan, a user can run `design --route new-feature` on the fixture and receive all five artifact types end-to-end; `audit --pr` returns a severity-ranked report; `audit --slop-tells` flags the four table-stakes patterns; and the cross-host scaffold is exercised on Codex/Cursor.

Output: 7 scripts, 2 CLI registrations, 4 SKILL.md files (2 workflows + 2 atoms: ingest, parse-or-interview, audit, updated design SKILL.md body), e2e fixture, extended host workspaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-CONTEXT.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-03-SUMMARY.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-04-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-04-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Phase 1 dispatch.mjs — Phase 2 extends with real runSubagent calls -->
From assets/scripts/routing/dispatch.mjs (Phase 1):
  export function dispatchRoute(route, designDir, projectRoot) → DispatchResult
  DispatchResult kinds: 'route_stub_dispatched' | 'route_not_yet_implemented' | 'unknown_route'
  Phase 2 replaces 'route_stub_dispatched' with real runSubagent calls for 4 routes.
  The 4 Phase 2 routes and their required stages:
    new-feature: stages [discover(skip-with-warning-if-design-exists), structure, style-5a, systematize-5b]
    design-bug: stages [style-5a] (Stage 5a touch-up only; skip-with-warning if no tokens.json)
    brand-refresh: stages [style-5a, systematize-5b]
    PR-audit: stages [audit --pr]

From assets/scripts/run-subagent.mjs (Phase 1):
  export async function runSubagent({ prompt, context }) → SubagentResult
  host detection: CLAUDE_CODE_BIN env → Claude Code; else → sequential fallback
  SubagentResult: { success: boolean, output?: string, error?: string }

From assets/scripts/routing/registry.mjs (Phase 1):
  export function detectStack(projectRoot) → StackSignals
  Also use for PRD detection: scan projectRoot + projectRoot/design/ for PRD*.md files

Phase 2 route dispatch shape (OF-01 resolution):
  For each route, build stageWorkflowMap:
    'discover' → 'skills/workflows/discover.md'
    'structure' → 'skills/workflows/structure.md'
    'style-5a' → 'skills/workflows/style.md'
    'systematize-5b' → 'skills/workflows/systematize.md'
  Call runSubagent per required stage in sequence (one-per-stage per D-34)

From assets/scripts/cli/gate.mjs (Phase 1):
  'design-os gate --stage N --design-dir <path>'
  Calls runGate(stage, designDir, config) and prints result JSON

Slop-tell heuristics format (references/slop-tells/heuristics.md):
  Markdown table with columns: Pattern, Regex, FindingId, Severity, Description
  Loaded at runtime by slop-tells.mjs via fs.readFile + markdown-table parse (or simple regex extraction)

apply.mjs contract (D-52):
  CLI: 'design-os apply --run-id <id> --design-dir <path> [--no-overwrite]'
  Reads .design-os/preview/run-<id>/ (staging area from D-52)
  Uses globby to find all files in staging dir
  Copies each to design/<relative-path> with fs.copyFile
  Default: overwrite existing files; log WARNING to run-log.jsonl for each conflict
  --no-overwrite flag: if conflict detected, exit 1 with message listing conflicting files
  Returns {applied: string[], warnings: string[]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-02-05-A: Audit scripts (slop-tells.mjs, stage-5a-pr.mjs, stage-5b-pr.mjs) + apply.mjs + heuristics.md</name>
  <files>assets/scripts/audit/slop-tells.mjs, assets/scripts/audit/stage-5a-pr.mjs, assets/scripts/audit/stage-5b-pr.mjs, assets/scripts/cli/apply.mjs, references/slop-tells/heuristics.md, tests/audit/slop-tells.test.ts, tests/audit/stage-5a-pr.test.ts, tests/audit/stage-5b-pr.test.ts, tests/cli/apply.test.ts</files>
  <behavior>
    - slop-tells.mjs: CSS string with "linear-gradient(red, orange, yellow, green, blue, purple)" → finding 5a-slop-001 ERROR
    - slop-tells.mjs: CSS with "font-family: 'Inter'" (no custom var) → finding 5a-slop-002 WARNING
    - slop-tells.mjs: CSS with "backdrop-filter: blur(10px)" → finding 5a-slop-003 WARNING
    - slop-tells.mjs: CSS with "grid-template-columns: repeat(3, 1fr)" → finding 5a-slop-004 INFO
    - slop-tells.mjs: CSS with "linear-gradient(to bottom, #fff, #eee, #ddd)" → finding 5a-slop-005 WARNING
    - slop-tells.mjs: clean CSS with semantic tokens only → empty findings array
    - stage-5a-pr.mjs: changed CSS file containing raw hex "#1a2b3c" instead of CSS custom property → finding 5a-token-001 WARNING
    - stage-5b-pr.mjs: diff showing design/tokens.json evidence field changed from INFERRED to validated → finding 5b-evidence-001 BLOCKER
    - apply.mjs: staging dir with 2 artifacts → both copied to design/; returns {applied:['tokens.json','DESIGN.md'], warnings:[]}
    - apply.mjs --no-overwrite: staging dir with artifact that already exists in design/ → exits 1 listing conflict
    - apply.mjs: no --no-overwrite, artifact already exists → overwritten; WARNING in warnings array
  </behavior>
  <action>Create `references/slop-tells/heuristics.md` — Markdown table of slop-tell patterns (5 rows per RESEARCH.md §7): rainbow-gradient (regex, 5a-slop-001, ERROR), Inter-default (regex, 5a-slop-002, WARNING), glass-stack (regex, 5a-slop-003, WARNING), three-column-grid (regex, 5a-slop-004, INFO), linear-gradient-3plus-stops (regex, 5a-slop-005, WARNING). Table format: `| Pattern | Regex | FindingId | Severity | Description |`. Each regex is a JavaScript regex string (no slashes, just the pattern string). Create `assets/scripts/audit/slop-tells.mjs` (ESM, no LLM imports). Export `detectSlopTells(cssOrTsxContent, filePath)` → `Finding[]`. Implementation: (1) Read heuristics.md via `readFileSync(path.join(import.meta.dirname, '../../references/slop-tells/heuristics.md'), 'utf8')`. (2) Parse table rows: extract Pattern, Regex (as string), FindingId, Severity using a simple line-split + pipe-split parser (no markdown library needed — table is regular). (3) For each pattern, construct `new RegExp(regexStr, 'i')` and test against content. If match found, create a Finding with {id:findingId, severity, message:`${pattern} detected in ${filePath}`, fixRecipe:'Replace with design token variable', suppressWith:`${findingId}`}. (4) Return findings array. The pattern file is loaded once per process call (no caching needed for CLI use). Create `assets/scripts/audit/stage-5a-pr.mjs`. Export `detectStage5aPrIssues(changedFilePath, content)` → `Finding[]`. Checks: (a) raw hex values: `/#[0-9a-fA-F]{3,6}\b/.test(content)` → finding {id:'5a-token-001', severity:'WARNING', message:'Raw hex value in '+changedFilePath+' — use DTCG token variable instead'}; (b) hardcoded Tailwind color classes: `/\b(bg|text|border)-(red|blue|green|yellow|purple|pink|gray|slate|zinc|neutral|stone|orange|amber|lime|emerald|teal|cyan|sky|violet|fuchsia|rose)-\d{2,3}\b/.test(content)` → finding {id:'5a-token-002', severity:'WARNING', message:'Hardcoded Tailwind color class — use semantic token class'}. Create `assets/scripts/audit/stage-5b-pr.mjs`. Export `detectStage5bPrIssues(changedFilePath, content)` → `Finding[]`. Checks for tokens.json diff: (a) evidence field changed: if content contains `"evidence": "validated"` or `"evidence": "proto"` → finding {id:'5b-evidence-001', severity:'BLOCKER', message:'tokens.json evidence field changed from INFERRED — violates D-51'}; (b) $schema mismatch: if content contains `$schema` but not `design-tokens.org` URL → finding {id:'5b-schema-001', severity:'WARNING'}. Create `assets/scripts/cli/apply.mjs`. Command name: 'apply'. Options: --run-id (string, required), --design-dir (default: 'design/'), --no-overwrite (boolean flag). Handler: (1) Resolve staging path: `.design-os/preview/run-${runId}/`. (2) Use globby v14 to list all files in staging path recursively. (3) For each file, compute relative path from staging root, compute target path as `${designDir}/${relativePath}`. (4) If target exists and --no-overwrite: collect all conflicts, exit 1 with list. (5) If target exists (no --no-overwrite): copy, log WARNING to run-log.jsonl entry: `{event:'apply-conflict-overwrite', file:relativePath, timestamp}`. (6) If target does not exist: copy. (7) Return {applied, warnings}. Uses globby v14 (Phase 1 precedent). Write unit tests for all scripts. Must pass lint-determinism. Implements D-45, D-46, D-47, D-52, AUDIT-01, AUDIT-03, AUDIT-05.</action>
  <verify>
    <automated>npx tsc --noEmit && node assets/scripts/lint-determinism.mjs --scope assets/scripts/audit/ && node assets/scripts/lint-determinism.mjs --scope assets/scripts/cli/ && npx vitest run tests/audit/ tests/cli/apply.test.ts --reporter=verbose</automated>
  </verify>
  <done>slop-tells.mjs detects all 5 pattern types; stage-5a-pr.mjs detects hex values and hardcoded Tailwind colors; stage-5b-pr.mjs detects evidence field changes; apply.mjs copies staging area to design/ with correct --no-overwrite behavior; all lint-determinism passes; all unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>T-02-05-B: dispatch.mjs real wiring + ingest/audit SKILL.md files + design SKILL.md update + e2e fixture</name>
  <files>assets/scripts/routing/dispatch.mjs, skills/workflows/ingest.md, skills/atoms/prd/parse-or-interview.md, skills/workflows/audit.md, skills/design/SKILL.md, evals/fixtures/e2e/next15-tailwind4-shadcn/PRD.md, evals/fixtures/e2e/next15-tailwind4-shadcn/next.config.mjs, evals/fixtures/e2e/next15-tailwind4-shadcn/package.json, evals/fixtures/e2e/next15-tailwind4-shadcn/app/globals.css, evals/fixtures/e2e/next15-tailwind4-shadcn/components/ui/.gitkeep, tests/e2e/new-feature-route.test.ts, tests/routing/dispatch-real-stages.test.ts, evals/hosts/codex-cli/host-profile.test.ts, evals/hosts/cursor/host-profile.test.ts</files>
  <behavior>
    - dispatch.mjs dispatchRoute('new-feature', designDir, projectRoot): calls runSubagent for each of [discover-or-skip, structure, style-5a, systematize-5b] stages in sequence; returns {kind:'route_dispatched', stages:['discover','structure','style-5a','systematize-5b'], results:[...]}
    - dispatch.mjs dispatchRoute('design-bug', designDir, projectRoot): calls runSubagent for [style-5a] only; returns {kind:'route_dispatched', stages:['style-5a'], results:[...]}
    - dispatch.mjs dispatchRoute('brand-refresh', designDir, projectRoot): calls runSubagent for [style-5a, systematize-5b]; returns {kind:'route_dispatched', ...}
    - dispatch.mjs dispatchRoute('PR-audit', designDir, projectRoot): calls runSubagent for [audit-pr]; returns {kind:'route_dispatched', stages:['audit-pr'], results:[...]}
    - dispatch.mjs dispatchRoute('new-product', ...): still returns {kind:'route_not_yet_implemented'}
    - e2e fixture: evals/fixtures/e2e/next15-tailwind4-shadcn/ has valid PRD.md, next.config.mjs (detectable by registry.mjs as Next.js), app/globals.css with @import "tailwindcss" (detectable as Tailwind v4), components/ui/ directory (detectable as shadcn)
    - e2e test (script-level, no LLM): calls dispatchRoute('new-feature', fixtureDesignDir, fixtureRoot); asserts kind:'route_dispatched'; asserts each required stage workflow SKILL.md exists and is readable
    - Cross-host: evals/hosts/codex-cli/host-profile.test.ts and cursor test updated to include Phase 2 workflow SKILL.md path existence checks
  </behavior>
  <action>Extend `assets/scripts/routing/dispatch.mjs` (Phase 1 deliverable). Add: (1) Import `runSubagent` from `../run-subagent.mjs`. (2) Define stageWorkflowMap: {discover:'skills/workflows/discover.md', structure:'skills/workflows/structure.md', 'style-5a':'skills/workflows/style.md', 'systematize-5b':'skills/workflows/systematize.md', 'audit-pr':'skills/workflows/audit.md'}. (3) Define routeStages: {new-feature: {requiredStages:['discover','structure','style-5a','systematize-5b'], skipIfExists:{discover:'design/research/personas/'}}, 'design-bug':{requiredStages:['style-5a']}, 'brand-refresh':{requiredStages:['style-5a','systematize-5b']}, 'PR-audit':{requiredStages:['audit-pr']}}. (4) In dispatchRoute, for the 4 implemented routes: replace the stub return with a loop: `for (const stage of routeStages[route].requiredStages) { ... check skipIfExists ... const result = await runSubagent({ prompt: stageWorkflowMap[stage], context: { designDir, bundlePath: ..., route } }); results.push(result); }`. (5) Return `{kind:'route_dispatched', route, stages, results}`. Unimplemented routes (new-product, mature-app-refactor, DS-extraction) keep `ROUTE_NOT_YET_IMPLEMENTED`. The D-49 cost-gate token check is handled within each stage workflow — dispatch does not call budget-check itself. Write `tests/routing/dispatch-real-stages.test.ts`: mock runSubagent to return {success:true, output:''}; assert each route calls the correct stages; assert new-product still returns not_yet_implemented. Create `skills/workflows/ingest.md` (W0 ingest workflow, per D-32). Frontmatter: name="design-os/ingest", description≤200 chars ("Ingest a PRD or launch Lenny 1-pager interview; emit design/PRD.md with frontmatter validation and stage-0 handoff bundle"), stage:0, gate:null (no gate at Stage 0 in v2.0a), artifacts.reads:[], artifacts.writes:["design/PRD.md","design/.handoff/stage-0-bundle.md"], composition.atoms:["prd/parse-or-interview"], mvp:true, compatibility:[claude-code,codex-cli,cursor], allows-tools:[Read,Write,Bash]. Procedure body: 1. Auto-detect PRD: "Bash: node assets/scripts/cli/detect-prd.mjs --project-root ." (or inline: scan for PRD*.md via registry.mjs signals; if found display "Found PRD.md — parsed N requirements"). If PRD exists: proceed to parse. If PRD < 50 tokens of structured content or absent: launch interview mode. 2. Inline ATOM-01 (prd/parse-or-interview): either parse existing PRD or run 5-7 question Lenny 1-pager interview. Write result to .design-os/preview/run-<id>/PRD.md with YAML frontmatter (artifact:prd, stage:0, provenance:generated OR validated, evidence:proto). 3. Validate frontmatter: "Bash: node bin/design-os.mjs validate --artifact prd --file .design-os/preview/run-<id>/PRD.md" (the prd artifact uses a simplified schema — frontmatter-validate.mjs for now). 4. Handoff bundle: "Bash: node assets/scripts/handoff-bundle-build.mjs --stage 0 --design-dir design/". 5. Present staged PRD for review. Await --apply. Init check: "Bash: node bin/design-os.mjs init --design-dir design/" (creates design/ + .design-os/ structure if absent). Include ## Host fallback section. Create `skills/atoms/prd/parse-or-interview.md` (ATOM-01, per D-33). Frontmatter: name="design-os/prd/parse-or-interview", description≤200 chars ("Auto-detect existing PRD and parse it, or launch 5-7 question Lenny 1-pager interview if no PRD found or PRD < 50 tokens"), stage:0, mvp:true. Body: ## Standalone bootstrap (the whole atom is a standalone step) + ## Workflow procedure. Procedure (D-35): 1. Check for PRD*.md in projectRoot and projectRoot/design/. 2. If found: parse it. Output structured frontmatter: product_name, problem_statement, target_users, success_metrics, constraints, out_of_scope. Validate structured content ≥ 50 tokens of substantive text. 3. If not found or < 50 tokens: launch Lenny 1-pager interview — 5-7 questions: (a) "What product are you building?", (b) "Who is the primary user?", (c) "What problem does it solve?", (d) "What does success look like in 6 months?", (e) "What are the biggest constraints?", (f) "What is explicitly out of scope?", (g) "Any existing solutions the user already has?" (optional). 4. Write output to design/PRD.md. 5. Surface 3-line summary: "Found/Created PRD: <name>. Parsed N requirements. Run `design --route <suggested>` to proceed." Create `skills/workflows/audit.md` (W8-basic, per D-32). Frontmatter: name="design-os/audit", description≤200 chars ("Audit design artifacts: --slop-tells (regex linters) or --pr (Stage 5a/5b diff detectors); emits AUDIT-REPORT.md"), stage:"cross-stage", gate:null, artifacts.reads:["design/.handoff/stage-5b-bundle.md"], artifacts.writes:["design/AUDIT-REPORT.md"], mvp:true, compatibility:[claude-code,codex-cli,cursor], allows-tools:[Read,Write,Bash]. Procedure body (two modes): Mode --slop-tells: 1. Scan user's CSS and TSX files in components/ and src/ (or paths from .design-os/ci.yaml if present). 2. "Bash: node assets/scripts/audit/slop-tells.mjs --scan-dir . --output .design-os/private/audit-findings.jsonl". 3. Aggregate findings, validate against audit-report.v1.json, emit AUDIT-REPORT.md sorted by severity descending. Mode --pr: 1. "Bash: git diff --name-only HEAD~1" (or $GITHUB_BASE_REF). 2. For each changed file: route to stage-5a-pr.mjs or stage-5b-pr.mjs. 3. Load design/.handoff/stage-5b-bundle.md for context. 4. Run slop-tells on CSS/TSX changed files. 5. Aggregate findings, emit AUDIT-REPORT.md. Both modes: validate AUDIT-REPORT.md against schemas/dist/audit-report.v1.json. Apply suppressions from .design-os/audit-suppressions.json if present. Surface CI blocking behavior: only BLOCKER findings block by default (reads .design-os/ci.yaml blockOnSeverity if present). Include ## Host fallback. Update `skills/design/SKILL.md` body (Phase 1 skeleton — extend the Routes table and procedure body to list the 4 implemented Phase 2 routes with brief descriptions): add to the Routes table: new-feature (Stages 1+2+5a+5b lite), design-bug (Stage 5a touch-up), brand-refresh (Stages 5a+5b), PR-audit (audit --pr). Update the Default behavior section to mention all 4 implemented routes. Create e2e fixture: `evals/fixtures/e2e/next15-tailwind4-shadcn/PRD.md` — minimal PRD for a task management app with YAML frontmatter (artifact:prd, stage:0, provenance:validated, schemaVersion:1) and structured content (product_name, problem_statement, target_users, success_metrics). `next.config.mjs` — minimal Next.js 15 config file (just `export default {}` is sufficient for stack detection). `package.json` — minimal with next@^15, tailwindcss@^4 in dependencies. `app/globals.css` — with `@import "tailwindcss"` and `@theme { }` block (detectable by registry.mjs as Tailwind v4). `components/ui/.gitkeep` — empty file to create components/ui/ directory (detectable as shadcn). Write `tests/e2e/new-feature-route.test.ts`: (1) Test that dispatchRoute('new-feature', fixtureDesignDir, fixtureRoot) with mocked runSubagent calls the correct 4 stage workflows. (2) Test that detectStack(fixtureRoot) returns {nextjs:true, tailwindV4:true, shadcn:true} — this is a real detectStack call on the fixture (no LLM needed, just filesystem detection). (3) Test that all 5 workflow + atom SKILL.md files from Plans 02-01 through 02-04 exist and have parseable gray-matter frontmatter. Extend `evals/hosts/codex-cli/host-profile.test.ts` and `evals/hosts/cursor/host-profile.test.ts` (Phase 1 deliverables) to add Phase 2 checks: assert that all 5 new workflow SKILL.md files exist (discover, structure, style, systematize, audit) and are parseable with gray-matter. This is the Phase 2 cross-host scaffold (D-53): the host workspaces verify SKILL.md presence + parsability rather than full execution (full cross-host parity is Phase 4). Implements OF-01, D-35, D-45, D-46, D-47, D-52, D-53, WF-01, WF-08, ATOM-01, ROUTE-02, ROUTE-04, ROUTE-05, ROUTE-07, ROUTE-09, AUDIT-01, AUDIT-03, AUDIT-05, AUDIT-08, DIST-04, MVPA-01..08.</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/routing/dispatch-real-stages.test.ts tests/e2e/new-feature-route.test.ts --reporter=verbose && test -f skills/workflows/ingest.md && test -f skills/atoms/prd/parse-or-interview.md && test -f skills/workflows/audit.md && node -e "import('gray-matter').then(({default:m}) => { ['skills/workflows/ingest.md','skills/atoms/prd/parse-or-interview.md','skills/workflows/audit.md'].forEach(f => { const r=m.read(f); if(!r.data.name) throw new Error(f); if(r.data.description && r.data.description.length>200) throw new Error(f+' desc >200: '+r.data.description.length); console.log('OK:',r.data.name); }); })" && node assets/scripts/lint-determinism.mjs --scope assets/scripts/routing/</automated>
  </verify>
  <done>dispatch.mjs wires real runSubagent for 4 routes (new-feature, design-bug, brand-refresh, PR-audit); 3 unimplemented routes still return ROUTE_NOT_YET_IMPLEMENTED; ingest.md, parse-or-interview.md, audit.md all exist with valid ≤200-char descriptions; e2e fixture detectable by registry.mjs as Next15+TailwindV4+shadcn; e2e test asserts dispatch stage sequence and fixture detectability; cross-host workspaces updated with Phase 2 SKILL.md presence checks; lint-determinism passes on routing/.</done>
</task>

<task type="auto">
  <name>T-02-05-C: Audit CLI integration + AUDIT-REPORT.md emit + end-to-end vitest run</name>
  <files>assets/scripts/cli/audit.mjs, tests/audit/audit-report-emit.test.ts</files>
  <action>Create `assets/scripts/cli/audit.mjs` (CLI subcommand per Phase 1 auto-discovery pattern). Command name: 'audit'. Options: --slop-tells (boolean), --pr (boolean), --design-dir (default 'design/'), --output (default 'design/AUDIT-REPORT.md'), --block-on-severity (default 'BLOCKER'), --continue-anyway (boolean for budget hard-stop override). Handler: (1) Determine mode from flags. If neither --slop-tells nor --pr: print usage and exit 1 ("Specify --slop-tells or --pr"). (2) Slop-tells mode: glob CSS/TSX files in user project (src/, components/, app/ — exclude node_modules); call detectSlopTells on each; collect findings. (3) PR mode: spawn `git diff --name-only HEAD~1` (or process.env.GITHUB_BASE_REF for CI); parse changed file paths; route each to stage-5a-pr or stage-5b-pr; also run slop-tells on CSS/TSX changed files; collect findings. (4) Load suppressions from .design-os/audit-suppressions.json if exists; filter suppressed findings. (5) Sort findings by severity: BLOCKER > ERROR > WARNING > INFO. Build AUDIT-REPORT.md: YAML frontmatter (artifact:audit-report, stage:cross-stage, auditType: slop-tells|pr-review, generated:<ISO>), body: sorted findings as a GFM table with findingId, severity, message, fixRecipe, suppressWith columns. (6) Validate emitted AUDIT-REPORT.md against schemas/dist/audit-report.v1.json via frontmatter-validate. (7) Exit code: 0 if no BLOCKER findings (or --block-on-severity is overridden); 1 if BLOCKER present and --continue-anyway not set. Write `tests/audit/audit-report-emit.test.ts`: (a) test with known slop fixture CSS → assert AUDIT-REPORT.md contains all 5 findings in severity-descending order; (b) test that AUDIT-REPORT.md frontmatter validates against audit-report.v1.json; (c) test suppression: if suppression file present for a findingId → that finding excluded from output. Run the complete vitest suite to confirm no regressions from Phase 1 (467 tests) plus all new Phase 2 tests pass. Record the new total test count. This is the Phase 2 completion verification step. Implements AUDIT-01, AUDIT-03, AUDIT-05, AUDIT-08, D-47, WF-08.</action>
  <verify>
    <automated>npx tsc --noEmit && node assets/scripts/lint-determinism.mjs --scope assets/scripts/ && npx vitest run tests/audit/audit-report-emit.test.ts --reporter=verbose && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>audit CLI subcommand registered; AUDIT-REPORT.md emitted and validated against audit-report.v1.json; suppression file respected; exit code 1 on BLOCKER findings (without --continue-anyway); full vitest suite passes (all Phase 1 tests + all Phase 2 tests); final test count recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git diff output → stage-5a/5b detectors | git diff output from user's repo crosses into audit scripts; must handle binary diffs, large diffs, and unusual file paths |
| apply.mjs → user's design/ directory | apply.mjs writes to the user's project; conflict resolution must not silently destroy user edits |
| audit --pr → $GITHUB_BASE_REF | CI environment variable could be poisoned in adversarial CI; only used for git diff base ref, not executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | apply.mjs overwriting user edits | mitigate | Default: overwrite with WARNING log. --no-overwrite flag allows users to abort before any overwrite. TRUST-02 --apply gate ensures writes only happen on explicit user confirmation. |
| T-02-05-02 | Spoofing | audit --pr $GITHUB_BASE_REF injection | accept | $GITHUB_BASE_REF is used only as git diff base ref (passed to `git diff` CLI). No code execution from env var. Low severity. |
| T-02-05-03 | Denial of Service | slop-tells.mjs on very large CSS files | mitigate | globby traversal excludes node_modules. Per-file regex matching is fast (no backtracking-prone patterns in heuristics.md). File size limit not enforced but typical CSS is well under 1MB. |
| T-02-05-04 | Information Disclosure | AUDIT-REPORT.md in committed design/ | accept | AUDIT-REPORT.md contains findings about the user's own design artifacts — no secrets. The suppression file (.design-os/audit-suppressions.json) is in the project; users control what is committed. |
| T-02-05-05 | Repudiation | dispatch.mjs real runSubagent calls replace stubs | mitigate | The DispatchResult kind changes from 'route_stub_dispatched' to 'route_dispatched'; test asserts this for all 4 routes. manifest.lock hash chain records each gate run, creating an audit trail. |
</threat_model>

<verification>
After all tasks complete:

```bash
# All audit scripts pass lint-determinism
node assets/scripts/lint-determinism.mjs --scope assets/scripts/audit/
node assets/scripts/lint-determinism.mjs --scope assets/scripts/routing/
node assets/scripts/lint-determinism.mjs --scope assets/scripts/cli/

# All unit tests
npx vitest run tests/audit/ tests/cli/ tests/routing/dispatch-real-stages.test.ts tests/e2e/ --reporter=verbose

# SKILL.md files parseable
node -e "import('gray-matter').then(({default:m}) => { ['skills/workflows/ingest.md','skills/atoms/prd/parse-or-interview.md','skills/workflows/audit.md','skills/workflows/discover.md','skills/workflows/structure.md','skills/workflows/style.md','skills/workflows/systematize.md'].forEach(f => { const r=m.read(f); console.log(r.data.name, 'OK'); }); })"

# E2e fixture detectable
node -e "import('./assets/scripts/routing/registry.mjs').then(m => m.detectStack('evals/fixtures/e2e/next15-tailwind4-shadcn')).then(s => { console.log(s); if (!s.nextjs || !s.tailwindV4 || !s.shadcn) process.exit(1); })"

# Full test suite (Phase 1 + Phase 2 — expect 467+ tests)
npx vitest run 2>&1 | tail -3

# TypeScript clean
npx tsc --noEmit
```
</verification>

<success_criteria>
- slop-tells.mjs detects all 5 pattern types; loaded from heuristics.md at runtime; lint-determinism passes
- stage-5a-pr.mjs and stage-5b-pr.mjs detect their respective diff violations
- apply.mjs copies staging artifacts with correct conflict behavior (overwrite + WARNING by default; --no-overwrite aborts)
- audit CLI subcommand: emits AUDIT-REPORT.md validated against audit-report.v1.json; exits 1 on BLOCKER without --continue-anyway
- dispatch.mjs wires real runSubagent for 4 routes; 3 unimplemented routes unchanged
- ingest.md, parse-or-interview.md, audit.md exist with valid ≤200-char descriptions and host fallback sections
- e2e fixture detectable as Next15+TailwindV4+shadcn by registry.mjs
- Cross-host workspaces verify Phase 2 SKILL.md presence
- All Phase 1 tests still pass; new total test count ≥ 600 (estimate: 467 Phase 1 + ~150 Phase 2)
- tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-05-SUMMARY.md` recording total Phase 2 test count, all files created, and a self-check confirming all Phase 2 Success Criteria are addressed.
</output>
