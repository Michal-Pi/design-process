---
phase: 01-v1-5-infrastructure-determinism-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-04"
files_modified:
  - assets/scripts/port-manager.mjs
  - assets/scripts/playwright-runner.mjs
  - assets/scripts/security-sandbox.mjs
  - assets/scripts/preview/vite-adapter.mjs
  - assets/scripts/preview/next-adapter.mjs
  - assets/scripts/preview/astro-adapter.mjs
  - assets/scripts/preview/variant-distance.mjs
  - assets/scripts/run-subagent.mjs
  - assets/scripts/routing/registry.mjs
  - assets/scripts/routing/dispatch.mjs
  - bin/design-os.mjs
  - skills/design/SKILL.md
  - references/garrett-elements.md
  - references/cooper-goodwin.md
  - references/torres-ost.md
  - references/klement-jtbd.md
  - references/indi-young-thinking-styles.md
  - references/rosenfeld-ia.md
  - references/dtcg-v2025-10.md
  - references/design-md.md
  - references/wcag-2-2.md
  - references/radix-step-roles.md
  - references/shadcn-tailwind-v4.md
  - references/prd/lenny-one-pager.md
  - references/gates/stage-3.md
  - references/gates/stage-4.md
  - evals/hosts/claude-code/package.json
  - evals/hosts/claude-code/vitest.config.ts
  - evals/hosts/claude-code/host-profile.test.ts
  - evals/hosts/codex-cli/package.json
  - evals/hosts/codex-cli/vitest.config.ts
  - evals/hosts/codex-cli/host-profile.test.ts
  - evals/hosts/cursor/package.json
  - evals/hosts/cursor/vitest.config.ts
  - evals/hosts/cursor/host-profile.test.ts
  - .github/workflows/anthropic-watcher.yml
  - .github/workflows/anthropic-watcher-heartbeat.yml
  - docs/MAINTAINERS.md
  - docs/RAPID-RESPONSE.md
  - package.json
  - tests/preview/port-manager.test.ts
  - tests/preview/playwright-runner.test.ts
  - tests/preview/security-sandbox.test.ts
  - tests/preview/variant-distance.test.ts
  - tests/routing/registry.test.ts
  - tests/routing/dispatch.test.ts
  - tests/routing/route-08-default.test.ts
  - tests/references/corpus-completeness.test.ts
  - tests/hosts/profiles.test.ts
  - tests/watcher/keyword-filter.test.ts
autonomous: true
requirements:
  - DIST-02
  - DIST-03
  - PREV-01
  - PREV-02
  - PREV-05
  - REF-01
  - REF-02
  - REF-04
  - ROUTE-08
  - GTM-06

must_haves:
  truths:
    - "`assets/scripts/port-manager.mjs` exports `allocatePort(runId)` using `get-port@^7` with `reserve: true` + writes a `.design-os/preview/run-<id>/port.lock` so parallel spawns do not race (PREV-01)."
    - "`assets/scripts/playwright-runner.mjs` exports `spawnAndProbe({ command, args, port, readyUrl, timeoutMs })` that spawns a dev server and probes for readiness via fetch — accepts 2xx/3xx/400-403 per Playwright webServer convention; rejects after timeout (PREV-01)."
    - "`assets/scripts/security-sandbox.mjs` exports `isPathAllowed(path, mode, projectRoot)` + `scrubEnvForPreview(env)` — a permission boundary (path allow-list for reads/writes; env scrub strips `*_KEY|*_SECRET|*_TOKEN|*_PASSWORD`); EXPLICITLY NOT a vm-based sandbox (vm2 CVE-2026-22709)."
    - 3 preview adapter scaffolds exist for Vite 6 / Next 15 (App Router) / Astro 5 — each exports `prepare(repoRoot) → { command, args, env, port, readyUrl }` to wire to playwright-runner (PREV-02).
    - "`variant-distance.mjs` implements the v1.0.1 6-axis Stage 5a visual-style distance metric (preserved per PREV-05); the Stage 3 structural-diversity metric is explicitly deferred to Phase 3 (per PROJECT.md research flag); Phase 1 ships axis names + stub computation + metric registry."
    - "`run-subagent.mjs` detects host via env (`CLAUDE_CODE_SESSION`, `CODEX_SESSION`, `CURSOR_SESSION`); Claude Code path uses native Task dispatch (best-effort against current Claude Code IPC); Codex/Cursor get a minimum-viable sequential-fallback shim (D-23)."
    - "Routing registry recognizes all 7 routes (`new-product`, `new-feature`, `mature-app-refactor`, `design-bug`, `brand-refresh`, `DS-extraction`, `PR-audit`); Phase 1 ships dispatcher + stubs — 3 stubs (mature-app-refactor, DS-extraction, full new-product) exit cleanly with `ROUTE_NOT_YET_IMPLEMENTED — ships in v2.0b`; ROUTE-08 default-is-not-all-5-stages enforced (orchestrator suggests route based on repo signals or asks for confirmation)."
    - 12 mandatory MVPA-06 references exist under `references/` per D-25 (Garrett, Cooper-Goodwin, Torres-OST, Klement-JTBD, Indi Young, Rosenfeld IA, DTCG v2025.10, Google DESIGN.md, WCAG 2.2, Radix step roles, shadcn-Tailwind-v4, Lenny one-pager) — each ≤2k tokens, citations not full quotes (D-24); also 2 additional stage-gate checklists (`references/gates/stage-3.md`, `stage-4.md`) complete the 6-gate set (REF-04).
    - 3 host-profile vitest workspaces exist (`evals/hosts/claude-code`, `codex-cli`, `cursor`) each with `host-profile.test.ts` running the same fixture suite (D-22 scaffolding); package.json `workspaces` field includes them.
    - Anthropic-Labs watcher GitHub Actions workflows exist: a daily cron (`anthropic-watcher.yml`) polls `anthropics/skills` releases + Anthropic blog RSS + Claude Design release notes for keyword hits; a separate heartbeat workflow (`anthropic-watcher-heartbeat.yml`) detects missed cron runs (Pitfall E mitigation); `docs/MAINTAINERS.md` names the human owner; `docs/RAPID-RESPONSE.md` template ships per D-31.
    - "Final `design` SKILL.md description trigger-budget audit: total trigger metadata across the 3 v1.5 skills (design, audit, handoff) ≤ 5k chars / 3 = ~1.6k chars per skill (DIST-02 — Phase 1 has 3 skills only; the 22-skill total budget is enforced when Phase 2 + 3 ship the remaining 19 atoms)."
  artifacts:
    - path: assets/scripts/port-manager.mjs
      provides: get-port@^7 with reserve + port.lock file (PREV-01)
      contains: "reserve: true"
    - path: assets/scripts/playwright-runner.mjs
      provides: spawnAndProbe with Playwright webServer-style readiness probe
      contains: "AbortSignal.timeout"
    - path: assets/scripts/security-sandbox.mjs
      provides: Permission boundary (path allowlist + env scrub) — NOT vm-based
      contains: "isPathAllowed"
    - path: assets/scripts/preview/vite-adapter.mjs
      provides: Vite 6 adapter (spawn vite dev + ready URL)
      contains: "vite"
    - path: assets/scripts/preview/next-adapter.mjs
      provides: Next 15 App Router adapter (spawn next dev)
      contains: "next"
    - path: assets/scripts/preview/astro-adapter.mjs
      provides: Astro 5 adapter (spawn astro dev)
      contains: "astro"
    - path: assets/scripts/preview/variant-distance.mjs
      provides: 6-axis visual-style distance metric (Stage 5a preserved per PREV-05)
      contains: "6-axis"
    - path: assets/scripts/run-subagent.mjs
      provides: Host-detect + Claude Code native / Codex+Cursor sequential fallback (D-23)
      contains: "CLAUDE_CODE_SESSION"
    - path: assets/scripts/routing/registry.mjs
      provides: 7-route registry (4 implemented + 3 stubs)
      contains: "ROUTE_NOT_YET_IMPLEMENTED"
    - path: assets/scripts/routing/dispatch.mjs
      provides: ROUTE-08 default-not-all-5-stages dispatcher
      contains: "suggestRoute"
    - path: .github/workflows/anthropic-watcher.yml
      provides: Daily cron polling Anthropic feeds (GTM-06)
      contains: "schedule:"
    - path: .github/workflows/anthropic-watcher-heartbeat.yml
      provides: Pitfall E mitigation — heartbeat checks the watcher actually ran
      contains: "heartbeat"
    - path: docs/MAINTAINERS.md
      provides: Names the watcher owner (D-30)
      contains: "Anthropic-Labs watcher"
    - path: docs/RAPID-RESPONSE.md
      provides: Positioning pivot template (D-31)
      contains: "interoperability with Claude Design"
  key_links:
    - from: assets/scripts/preview/vite-adapter.mjs
      to: assets/scripts/playwright-runner.mjs
      via: ES import for spawnAndProbe
      pattern: "spawnAndProbe"
    - from: assets/scripts/preview/vite-adapter.mjs
      to: assets/scripts/port-manager.mjs
      via: ES import for allocatePort
      pattern: "allocatePort"
    - from: assets/scripts/preview/vite-adapter.mjs
      to: assets/scripts/security-sandbox.mjs
      via: ES import for scrubEnvForPreview
      pattern: "scrubEnvForPreview"
    - from: assets/scripts/routing/dispatch.mjs
      to: assets/scripts/routing/registry.mjs
      via: ES import + route lookup
      pattern: "registry"
    - from: skills/design/SKILL.md
      to: assets/scripts/routing/registry.mjs
      via: design orchestrator body invokes route dispatcher (documented in body)
      pattern: "route"
    - from: .github/workflows/anthropic-watcher.yml
      to: docs/MAINTAINERS.md
      via: workflow CC's the named owner on competitive-watch issues
      pattern: "MAINTAINERS"
---

<objective>
Land the preview-harness infrastructure preserved from v1.0.1 (port manager, security sandbox as permission boundary, Playwright readiness probe, Vite 6 / Next 15 / Astro 5 adapter scaffolds, 6-axis variant-distance metric for Stage 5a), the routing-matrix scaffolding for all 7 routes (4 wired stubs + 3 ROUTE_NOT_YET_IMPLEMENTED stubs with ROUTE-08 default-not-all-5-stages dispatcher), the 12 mandatory MVPA-06 references + 2 additional stage-gate checklists (Stages 3 + 4 — completing the 6-gate set started in Plan 02), the 3 host-profile vitest workspaces completing D-22, and the Anthropic-Labs watcher (daily cron + heartbeat for Pitfall E + MAINTAINERS.md owner + RAPID-RESPONSE.md template per D-30 + D-31).

Purpose: This plan closes the remaining Phase-1 infrastructure: preview harness so Phase 2's `style-lite` workflow has dev-server spawning available; routing scaffolds so the orchestrator skill can dispatch all 7 routes from day one; references corpus so Phase 2 gates can cite canon; host profiles so the D-22 matrix has all 3 profile dirs; watcher so the GTM kill-risk monitor (Pitfall 9) is live from week 1. Critical security note: the security sandbox is implemented as a permission boundary (path allowlist + env scrub) per RESEARCH.md "Security Sandbox — Permission Boundary (NOT vm-based)" — vm2 is explicitly NOT used (CVE-2026-22709).

Output: 8 preview scripts (port-manager, playwright-runner, security-sandbox, 3 adapters, variant-distance, run-subagent) + routing registry + dispatcher + 12 references + 2 stage-gate checklists (stages 3 + 4) + 3 host-profile workspaces + 2 GitHub Actions watcher workflows + MAINTAINERS.md + RAPID-RESPONSE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-CONTEXT.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-RESEARCH.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-01-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-02-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-04-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@design-os-mrd-v2.md
@skills/design/SKILL.md
@skills/audit/SKILL.md
@skills/handoff/SKILL.md
@bin/design-os.mjs

<interfaces>
<!-- From Plans 01 + 02 + 04 — consumed here: -->

GateResult discriminated union (Plan 01) — the routing dispatcher returns GateResult-shaped errors on bad route names

SKILL.md frontmatter (Plan 04 ships 3 stubs) — DIST-02 trigger budget aggregates description char counts across skills

Plan 02's runGate(stage, designDir, config) — Plan 05's routing dispatcher orchestrates gate sequencing per route

Plan 04's STAGE_ORDER constant (schemas/src/spine.ts) — routing required-stages arrays subset this
</interfaces>

<interfaces_introduced_here>
<!-- New types this plan introduces (consumed in Phase 2): -->

RouteDefinition (assets/scripts/routing/registry.mjs):
- name: 'new-product' | 'new-feature' | 'mature-app-refactor' | 'design-bug' | 'brand-refresh' | 'DS-extraction' | 'PR-audit'
- requiredStages: Stage[]
- skipWithWarning: Stage[]
- optionalStages: Stage[]
- budgetTokensP50: number
- status: 'implemented-stub' | 'not-yet-implemented'  ← 4 implemented-stub for v2.0a routes; 3 not-yet-implemented for v2.0b routes

PreviewAdapter (assets/scripts/preview/<framework>.mjs):
- prepare(repoRoot) → { command, args, env, port, readyUrl }

variant-distance metric registry (assets/scripts/preview/variant-distance.mjs):
- AXES = ['hue', 'saturation', 'lightness', 'typographyContrast', 'spacingRhythm', 'cornerRoundness'] as const
- distance(a, b) → number (Stage 5a visual-style; Stage 3 structural distance flagged as Phase 3 deferred)
</interfaces_introduced_here>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preview harness (port-manager + playwright-runner + security-sandbox + 3 adapters + variant-distance + run-subagent)</name>
  <files>assets/scripts/port-manager.mjs, assets/scripts/playwright-runner.mjs, assets/scripts/security-sandbox.mjs, assets/scripts/preview/vite-adapter.mjs, assets/scripts/preview/next-adapter.mjs, assets/scripts/preview/astro-adapter.mjs, assets/scripts/preview/variant-distance.mjs, assets/scripts/run-subagent.mjs, bin/design-os.mjs, tests/preview/port-manager.test.ts, tests/preview/playwright-runner.test.ts, tests/preview/security-sandbox.test.ts, tests/preview/variant-distance.test.ts</files>
  <read_first>
    - .planning/phases/01-v1-5-infrastructure-determinism-foundation/01-CONTEXT.md PREV-01..05 list, D-23 (subagent dispatch shim), Claude's Discretion on logging (use pino per RESEARCH)
    - .planning/phases/01-v1-5-infrastructure-determinism-foundation/01-RESEARCH.md "Port Manager" code example, "Playwright Readiness Probe" code example, "Security Sandbox — Permission Boundary (NOT vm-based)" code example, "Pattern 6: Subagent Dispatch Shim" code example, alternatives-considered (vm2 CVE-2026-22709 → DO NOT USE)
    - .planning/research/STACK.md (get-port@^7, Playwright 1.60.x, no vm2)
    - .planning/research/PITFALLS.md (security sandbox: permission boundary only)
    - design-os-mrd-v2.md §3.11 (preview harness preserved from v1.0.1), §3.18 (security)
  </read_first>
  <behavior>
    - `allocatePort(runId)` uses `get-port@^7` with `port: [5173, 3000, 4321]` (Vite, Next, Astro defaults) and `reserve: true`; writes `.design-os/preview/run-<id>/port.lock` JSON `{port, pid, allocated}`; returns `{port, release()}` where release deletes the lock file.
    - "`spawnAndProbe({ command, args, cwd, env, port, readyUrl, timeoutMs })` spawns a child process; in a polling loop fetches readyUrl with `AbortSignal.timeout(1000)`; ALLOWED statuses are 2xx, 3xx, 400, 401, 402, 403 (Playwright webServer convention); on ready returns `{ ready: true, pid, kill }`; on timeout kills the child + throws."
    - `isPathAllowed(absPath, mode, projectRoot)` resolves the path; if outside projectRoot returns false; if mode='write' uses `WRITE_ALLOWLIST = ['design/', '.design-os/']`; if mode='read' uses `READ_ALLOWLIST = ['design/', 'design/.handoff/', '.design-os/', 'PRD.md']`; returns true only when the relative path is or starts-with an allow-list entry.
    - `scrubEnvForPreview(env)` returns a copy of env with any key matching `/_(KEY|SECRET|TOKEN|PASSWORD)$/i` removed.
    - Each adapter (`vite-adapter`, `next-adapter`, `astro-adapter`) exports `prepare(repoRoot, { runId })` → builds `command = 'npx'`, args specific to framework (`['vite', 'dev', '--port', '<port>']` for vite; `['next', 'dev', '-p', '<port>']` for next; `['astro', 'dev', '--port', '<port>']` for astro), env=scrubEnvForPreview(process.env), allocates port + builds readyUrl `http://127.0.0.1:<port>/`.
    - `variant-distance.mjs` exports `AXES = ['hue', 'saturation', 'lightness', 'typographyContrast', 'spacingRhythm', 'cornerRoundness']` and `distance(a, b)` computing Euclidean distance across the 6 axes (a, b are objects with numeric values per axis 0..1); exports `STAGE_3_STRUCTURAL_DEFERRED = true` constant + a stub throwing "Stage 3 structural-diversity metric ships in Phase 3".
    - `run-subagent.mjs` `detectHost()` checks env vars in order; `dispatchSubagent({ prompt, tools, context })` invokes the appropriate path; Phase 1 Claude Code path is a documented best-effort against the current Claude Code IPC (clearly commented as "interface may change — adapter pattern keeps the shim swappable"); Codex/Cursor sequential fallback spawns the workflow inline via shell command construction (minimum-viable per CONTEXT.md D-23).
  </behavior>
  <action>Create `assets/scripts/port-manager.mjs` per RESEARCH.md "Port Manager with cross-process safety" code example: import `getPort` from `get-port`; `PREFERRED = [5173, 3000, 4321]`; `allocatePort(runId, designOsDir = '.design-os')` makes run dir, calls `getPort({ port: PREFERRED, reserve: true })`, writes port.lock, returns `{ port, release }`. Add a `concurrent allocation` smoke check: in tests/preview/port-manager.test.ts, call allocatePort twice in parallel with different runIds and assert distinct ports returned. Create `assets/scripts/playwright-runner.mjs` per RESEARCH.md "Playwright Readiness Probe" code example: import `spawn` from `node:child_process`, `setTimeout as sleep` from `node:timers/promises`; export `spawnAndProbe({ command, args, cwd, env, port, readyUrl, timeoutMs = 30000 })`; loop fetching readyUrl with `AbortSignal.timeout(1000)`; ALLOWED check `(s>=200 && s<400) || [400,401,402,403].includes(s)`; on ready return `{ ready, pid, kill }`; on timeout kill child + throw with diagnostic. Create `assets/scripts/security-sandbox.mjs` per RESEARCH.md "Security Sandbox — Permission Boundary (NOT vm-based)" code example: `READ_ALLOWLIST` and `WRITE_ALLOWLIST` arrays; `isPathAllowed(absPath, mode, projectRoot)` using `node:path.resolve` + `node:path.relative`; reject `..` prefixed paths; check allowlist prefix matching. `scrubEnvForPreview(env)` strips keys matching `/_(KEY|SECRET|TOKEN|PASSWORD)$/i`. Include a top-of-file comment: "Threat model: Phase 1 does NOT execute arbitrary user code in a sandbox. The 'sandbox' enforces (1) path allow-listing for reads/writes, (2) env scrub for spawned dev servers. vm2 explicitly NOT used (CVE-2026-22709 + 11 advisories early 2026)." Create the 3 preview adapters under `assets/scripts/preview/`. Each imports `allocatePort` from port-manager, `scrubEnvForPreview` from security-sandbox, and exports `async function prepare(repoRoot, { runId } = { runId: Date.now().toString() })`. Vite adapter: `command = 'npx', args = ['vite', 'dev', '--port', String(port), '--host', '127.0.0.1'], readyUrl = \`http://127.0.0.1:${port}/\``. Next adapter (App Router): `command = 'npx', args = ['next', 'dev', '-p', String(port)], readyUrl = \`http://127.0.0.1:${port}/\``. Astro adapter: `command = 'npx', args = ['astro', 'dev', '--port', String(port), '--host', '127.0.0.1'], readyUrl = \`http://127.0.0.1:${port}/\``. Each returns `{ command, args, env, port, readyUrl, runId, releasePort }` where releasePort closes the get-port reservation. Create `assets/scripts/preview/variant-distance.mjs`: `export const AXES = ['hue', 'saturation', 'lightness', 'typographyContrast', 'spacingRhythm', 'cornerRoundness'] as const`. `export function distance(a, b) { return Math.sqrt(AXES.reduce((sum, axis) => sum + Math.pow((a[axis] ?? 0) - (b[axis] ?? 0), 2), 0)); }`. Include constants `STAGE_3_STRUCTURAL_DEFERRED = true` + stub function `stage3StructuralDistance() { throw new Error('Stage 3 structural-diversity metric ships in Phase 3 per project research flag'); }`. Create `assets/scripts/run-subagent.mjs` per RESEARCH.md "Pattern 6": `detectHost()` returns 'claude-code' | 'codex-cli' | 'cursor' | 'unknown' based on env vars; `dispatchSubagent({ prompt, tools, context })` switches on host; Claude Code path: comment + best-effort invocation (e.g., write a temp stdin payload and shell out to `claude --task` if `CLAUDE_CODE_BIN` env is set — document that the exact API is best-effort and the adapter pattern keeps it swappable); Codex/Cursor sequential fallback: print a structured message + invoke the prompt as a child process spawning the host CLI with `--prompt <text>` (best-effort); on 'unknown' return `{ error: 'host-not-detected' }`. Extend `bin/design-os.mjs` with subcommands: `preview spawn --framework <vite|next|astro> --repo-root <path>`, `preview release-port --run-id <id>`. Tests: `tests/preview/port-manager.test.ts` (allocate two distinct runIds in parallel → distinct ports; lock file written; release deletes it); `tests/preview/playwright-runner.test.ts` (spawn a `node -e "require('http').createServer((req,res)=>{res.end('ok')}).listen(<port>)"` toy server; probe succeeds within 5s; spawn a non-listening server; probe times out + throws); `tests/preview/security-sandbox.test.ts` (isPathAllowed: design/foo → true read+write; ../outside → false; .design-os/private → true read+write; some/random/path → false; scrubEnvForPreview strips API_KEY, MY_SECRET, GH_TOKEN, DB_PASSWORD; preserves NODE_ENV, PATH); `tests/preview/variant-distance.test.ts` (distance({hue: 0...}, {hue: 1, others: 0}) ≈ 1; distance(identical) = 0; stage3StructuralDistance throws). Implements PREV-01, PREV-02, PREV-05, D-22 dependency for Plan 05 Task 3, D-23.</action>
  <verify>
    <automated>npx vitest run tests/preview/ --reporter=verbose && tsx -e "import { isPathAllowed } from './assets/scripts/security-sandbox.mjs'; if (!isPathAllowed('design/foo.json', 'write', process.cwd())) process.exit(1); if (isPathAllowed('/etc/passwd', 'read', process.cwd())) process.exit(1)" && tsx -e "import { scrubEnvForPreview } from './assets/scripts/security-sandbox.mjs'; const e = scrubEnvForPreview({API_KEY: 'x', PATH: 'p'}); if (e.API_KEY) process.exit(1); if (!e.PATH) process.exit(1)"</automated>
  </verify>
  <done>Port manager allocates non-colliding ports via get-port reserve; Playwright runner probes with allowed-status check; security sandbox is a permission boundary (no vm2); 3 preview adapters scaffold-only; variant-distance ships the 6-axis Stage 5a metric; Stage 3 structural metric explicitly deferred + stub throws; run-subagent shim ships with all 3 host paths (Claude Code best-effort + Codex/Cursor minimum-viable sequential fallback); ≥10 vitest assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Routing registry + ROUTE-08 dispatcher + 12 references + 2 stage-gate checklists + design SKILL.md body update</name>
  <files>assets/scripts/routing/registry.mjs, assets/scripts/routing/dispatch.mjs, references/garrett-elements.md, references/cooper-goodwin.md, references/torres-ost.md, references/klement-jtbd.md, references/indi-young-thinking-styles.md, references/rosenfeld-ia.md, references/dtcg-v2025-10.md, references/design-md.md, references/wcag-2-2.md, references/radix-step-roles.md, references/shadcn-tailwind-v4.md, references/prd/lenny-one-pager.md, references/gates/stage-3.md, references/gates/stage-4.md, skills/design/SKILL.md, bin/design-os.mjs, tests/routing/registry.test.ts, tests/routing/dispatch.test.ts, tests/routing/route-08-default.test.ts, tests/references/corpus-completeness.test.ts</files>
  <read_first>
    - skills/design/SKILL.md (Plan 04 — to extend body with route docs)
    - .planning/phases/01-v1-5-infrastructure-determinism-foundation/01-CONTEXT.md D-21 (all 7 routes wired, 4 v2.0a impl + 3 v2.0b stubs), D-24 (≤2k tokens per reference; citations not full quotes), D-25 (12 mandatory MVPA-06 references + 4 v1.5 stage-gate checklists — Plan 02 shipped stage-1, 2, 5a, 5b; Plan 05 ships stage-3 + stage-4 to complete the 6-gate set; D-26 (gate checklist Markdown table format)
    - .planning/research/ARCHITECTURE.md (the references/ subdir layout in the diagram)
    - design-os-mrd-v2.md §3.10 (references corpus — Markdown only, no vector DB)
    - REQUIREMENTS.md ROUTE-01..09 (route requirements), REF-01, REF-02, REF-04
  </read_first>
  <behavior>
    - "Routing registry exports `ROUTES` (a Record keyed by route name) with all 7 routes per ROUTE-01..07: `new-product` (status: not-yet-implemented; requiredStages [0,1,2,5a,5b]; optional [3,4]; budget 150000), `new-feature` (implemented-stub; required [2,4,5a]; skipWithWarning [1]; budget 60000), `mature-app-refactor` (not-yet-implemented; required [2,4,5b]; skip [1,3,5a]; budget 45000), `design-bug` (implemented-stub; required [4,5a]; skip [1,2,3,5b]; budget 20000), `brand-refresh` (implemented-stub; required [5a,5b]; skip [1,2,3,4]; budget 55000), `DS-extraction` (not-yet-implemented; required [audit-reverse,1,2,4,5b]; skip [0]; budget 120000), `PR-audit` (implemented-stub; required [audit-pr]; budget 15000)."
    - "Dispatcher: `dispatchRoute({ routeName, designDir, opts })`. If route's `status === 'not-yet-implemented'`, return `{ kind: 'route_not_yet_implemented', name, ships: 'v2.0b' }`. Otherwise return `{ kind: 'route_stub_dispatched', name, requiredStages, budgetTokensP50, status: 'implemented-stub' }` (Phase 1 ships only the dispatcher shape; Phase 2 plugs in the real stage workflows)."
    - "ROUTE-08 default-not-all-5-stages: `suggestRoute(repoSignals) → { route, confidence, reasoning }`. Repo signals are a small bag of heuristics: existing `package.json` indicating Next/Vite/Astro framework → suggest `new-feature`; existing `design/sketch/` (rejected wireframes) without `design/research/` → suggest `mature-app-refactor`; `audit --pr` invocation → `PR-audit`; pure framework prototype (Lovable-style hint via `package.json` referencing v0/Lovable in author or having pnpm-lock.yaml without design dir) → `DS-extraction`. When confidence < 0.6, the dispatcher prints `Which route? Suggestions: <top-3>. Run `design --route <name>` to choose. (Default is NOT all 5 stages.)` and exits 0 — never silently runs all 5 stages."
    - 12 reference files exist under `references/` each starting with frontmatter `# >>> design-os reference` (just a comment), having a short summary section + a "Canon citations" section listing pointers (`Garrett §4.1 p.62`), keeping each file under 2k tokens (use tiktoken estimate or manual line budget — a 6k character ceiling is a safe proxy).
    - 2 more stage-gate checklists exist: `references/gates/stage-3.md` (Stage 3 Sketch gate — Crazy 8s structural diversity; Excalidraw validator rejects color/typography) + `references/gates/stage-4.md` (Stage 4 Interact gate — state catalogs complete; Mermaid stateDiagram-v2 designer-readable canonical; XState only when async + ≥3 states + conditional). Each uses the D-26 4-column Markdown table format. The full 6-gate set now exists (Plan 02 shipped stage-1, 2, 5a, 5b; Plan 05 adds stage-3 + stage-4).
    - "`skills/design/SKILL.md` body is updated to enumerate all 7 routes by name + link `references/` table; the orchestrator skill body documents that the default behavior is to ask before running all 5 stages."
  </behavior>
  <action>Create `assets/scripts/routing/registry.mjs` exporting `ROUTES = { 'new-product': { name: 'new-product', status: 'not-yet-implemented', shipsIn: 'v2.0b', requiredStages: ['0','1','2','5a','5b'], optionalStages: ['3','4'], skipWithWarning: [], budgetTokensP50: 150000 }, ... }` covering all 7 routes per the behavior block. Also export `IMPLEMENTED_ROUTES = ['new-feature', 'design-bug', 'brand-refresh', 'PR-audit']` and `NOT_YET_IMPLEMENTED_ROUTES = ['new-product', 'mature-app-refactor', 'DS-extraction']` (note: `new-product` full path is v2.0b; v2.0a only ships `new-feature` partial which is `implemented-stub` here per CONTEXT.md D-21 — the full new-product path is not_yet_implemented). Create `assets/scripts/routing/dispatch.mjs` exporting `dispatchRoute({ routeName, designDir, opts })`: lookup ROUTES; if absent return `{ kind: 'unknown_route', name: routeName, available: Object.keys(ROUTES) }`; if `status === 'not-yet-implemented'` return `{ kind: 'route_not_yet_implemented', name, shipsIn }`; else return `{ kind: 'route_stub_dispatched', name, requiredStages, budgetTokensP50, status: 'implemented-stub' }`. Export `suggestRoute(repoSignals) → { suggestion: routeName, confidence: 0-1, reasoning: string[], alternatives: routeName[] }`. Heuristics: parse `repoSignals` (a record of feature flags computed by the caller); when no signals or low confidence, return `{ suggestion: 'new-feature', confidence: 0.3, reasoning: ['default-fallback'], alternatives: ['design-bug','brand-refresh','PR-audit'] }`. Wire `bin/design-os.mjs design --route <name> [--design-dir <path>]` calling dispatchRoute; when no `--route`, call suggestRoute and print the prompt + exit 0 (ROUTE-08 — never silently run all 5 stages). Create the 12 reference files. Each follows this skeleton: `# <Canon Title>\n\n## Purpose\n<1-2 sentences explaining where in the workflow this canon applies>\n\n## Citations\n- <Author §X.Y, p.NN>: <one-line summary of cited claim>\n- ... (≥5 citations)\n\n## How design-os uses it\n<2-3 bullet points naming the specific REQ-IDs or workflows that depend on this canon>\n\n## Drift watch\n<spec stability note; e.g., DESIGN.md spec drift watch is medium-confidence — cite STACK.md MEDIUM stability note for DESIGN.md, HIGH for the rest>`. Specific files: `garrett-elements.md` (the 5 planes — Strategy / Scope / Structure / Skeleton / Surface — cite Garrett "The Elements of User Experience" 2nd ed. §1.x, §4.x; how design-os uses: SPINE-01 maps 1:1); `cooper-goodwin.md` (Goodwin "Designing for the Digital Age"); `torres-ost.md` (Teresa Torres Opportunity Solution Tree); `klement-jtbd.md` (Alan Klement Jobs To Be Done); `indi-young-thinking-styles.md` (Indi Young thinking-style format → persona fields); `rosenfeld-ia.md` (Information Architecture for the World Wide Web, Rosenfeld & Morville); `dtcg-v2025-10.md` (W3C DTCG v2025.10 — pin version, cite spec sections, note primitive→semantic→component tiers); `design-md.md` (Google DESIGN.md April 2026 release, Apache-2.0, `$extensions.design-os` namespace, drift watch note); `wcag-2-2.md` (WCAG 2.2 AA contrast measurement — never claim conformance per TRUST-01); `radix-step-roles.md` (Radix UI step roles for component scaffolding patterns); `shadcn-tailwind-v4.md` (shadcn + Tailwind v4 `@theme` projection); `prd/lenny-one-pager.md` (Lenny Rachitsky 1-pager PRD format → Stage 0 interview fallback). Each file under 6000 characters (safe proxy for 2k tokens). Create `references/gates/stage-3.md` with header `# Stage 3 — Sketch gate operational checklist` and the 4-column Markdown table per D-26: rows including "Each screen has ≥3 Crazy 8s variants" (cite Sprint methodology), "Wireframes are Excalidraw JSON pinned to v0.18.0 schema" (cite STACK.md), "No color or typography drift in `.excalidraw` (FID-03)", "Sprint Decider rationale recorded" (cite WF-04). Create `references/gates/stage-4.md` with header `# Stage 4 — Interact gate operational checklist`: rows "State catalogs complete per component" (cite ATOM-12), "Mermaid stateDiagram-v2 designer-readable canonical (FID-04)", "XState v5 emitted only when async + ≥3 states + conditional transitions (MVPB-08)" (cite XState v5 docs + STACK.md), "HAX-18 audit for AI-product components" (cite Microsoft HAX). Update `skills/design/SKILL.md` body (only the body, not the frontmatter description which is already ≤200 chars): add a `## Routes` section listing all 7 routes with their budget + status; add a `## Default behavior` section documenting ROUTE-08 (`When invoked without --route, design-os asks before running; default is NOT all 5 stages`); add a `## References` section listing the 12 canon refs + the 6 gate checklists. Verify the description in frontmatter is still ≤200 chars (untouched). Tests: `tests/routing/registry.test.ts` (all 7 routes exist; 4 implemented-stub + 3 not-yet-implemented + new-product = 4 total not-yet-implemented; budgets present; required stages present); `tests/routing/dispatch.test.ts` (dispatchRoute on each of 7 routes returns the expected kind; unknown route → unknown_route); `tests/routing/route-08-default.test.ts` (suggestRoute with empty signals → default-fallback suggestion with confidence < 0.6; CLI `design` without --route prints ROUTE-08 prompt and exits 0); `tests/references/corpus-completeness.test.ts` (12 reference files exist; each contains "## Citations" section; each file < 6000 characters; 6 stage-gate checklists exist now including stage-3 and stage-4; each contains the 4-column Markdown table header). Implements ROUTE-08, REF-01, REF-02, REF-04, D-21, D-24, D-25, D-26 (extended to all 6 gates).</action>
  <verify>
    <automated>npx vitest run tests/routing/ tests/references/ --reporter=verbose && tsx bin/design-os.mjs design --route design-bug --design-dir /tmp | grep -q "route_stub_dispatched" && tsx bin/design-os.mjs design --route new-product --design-dir /tmp | grep -q "route_not_yet_implemented" && tsx bin/design-os.mjs design --route invalid-route --design-dir /tmp | grep -q "unknown_route" && ls references/*.md | wc -l | awk '$1 >= 12 {exit 0} {exit 1}' && ls references/gates/*.md | wc -l | awk '$1 == 6 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Registry contains all 7 routes; 4 implemented-stub + 3 not-yet-implemented; suggestRoute heuristics return ROUTE-08 default-fallback when no signals; 12 mandatory references exist + 6 gate checklists complete; design SKILL.md body updated to document all routes + default-not-all-5-stages behavior; ≥15 vitest assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Host-profile workspaces (D-22) + Anthropic-Labs watcher (daily cron + heartbeat) + MAINTAINERS.md + RAPID-RESPONSE.md</name>
  <files>evals/hosts/claude-code/package.json, evals/hosts/claude-code/vitest.config.ts, evals/hosts/claude-code/host-profile.test.ts, evals/hosts/codex-cli/package.json, evals/hosts/codex-cli/vitest.config.ts, evals/hosts/codex-cli/host-profile.test.ts, evals/hosts/cursor/package.json, evals/hosts/cursor/vitest.config.ts, evals/hosts/cursor/host-profile.test.ts, .github/workflows/anthropic-watcher.yml, .github/workflows/anthropic-watcher-heartbeat.yml, docs/MAINTAINERS.md, docs/RAPID-RESPONSE.md, package.json, tests/hosts/profiles.test.ts, tests/watcher/keyword-filter.test.ts</files>
  <read_first>
    - package.json (Plan 01 — to extend `workspaces` field)
    - assets/scripts/run-subagent.mjs (Task 1)
    - .planning/phases/01-v1-5-infrastructure-determinism-foundation/01-CONTEXT.md D-22 (3 host profiles as vitest workspaces; targets within 0.10 of host-first pass rate; v2.0 GA enforces, v1.5 scaffolds), D-30 (hybrid watcher: weekly manual + daily cron; named maintainer; keyword filter), D-31 (rapid-response template)
    - .planning/phases/01-v1-5-infrastructure-determinism-foundation/01-RESEARCH.md "GitHub Actions Cron for Anthropic-Labs Watcher (D-30)" code example, Pitfall E (heartbeat — missed-cron detection), Open Q4 (keyword tuning — Phase 1 ships 6 keywords + heuristic; tune in week 2 based on first week's hits)
    - .planning/research/STACK.md (npm workspaces recommendation for 3 host profiles)
    - design-os-mrd-v2.md §12 (existential pitfalls — GTM kill-risk monitor)
  </read_first>
  <behavior>
    - Root `package.json` declares `"workspaces": ["evals/hosts/claude-code", "evals/hosts/codex-cli", "evals/hosts/cursor"]`.
    - Each of the 3 host workspaces contains `package.json` (private, with vitest dep), `vitest.config.ts` setting environment variable `HOST_PROFILE` to the workspace name, and `host-profile.test.ts` that imports `dispatchSubagent` from `../../../assets/scripts/run-subagent.mjs` and asserts dispatching against the configured host runs without crashing (Phase 1 fixture: a trivial echo prompt; passes when fallback path activates on unknown host).
    - "Top-level npm script `test:hosts` runs vitest across all 3 host workspaces via the host-matrix GitHub Action (Plan 03) — Plan 05 ships the workspace dirs themselves; Plan 03 shipped the workflow."
    - "`.github/workflows/anthropic-watcher.yml` runs daily at 07:37 UTC (offset prime per RESEARCH.md): polls `anthropics/skills` releases via GH REST API, fetches Anthropic blog RSS, fetches Claude Design release notes (best available endpoint — start with anthropics/skills issue tracker if no release-notes feed); for each title check if any of 6 keywords (`5-stage`, `design process`, ` IA `, `wireframe`, `state machine`, `audit`) appears; per Open Q4 heuristic, additionally require either ≥2 keywords or 1 keyword + word stem `design` in body; if hits, check if a `[competitive-watch]` issue with the title prefix already exists; if not, open one and cc the maintainer named in `docs/MAINTAINERS.md`."
    - "`.github/workflows/anthropic-watcher-heartbeat.yml` runs daily at 08:37 UTC (1h after the watcher); checks that the watcher workflow's most-recent run is within 26h; if not, opens an issue `Anthropic watcher heartbeat missed`. Pitfall E mitigation."
    - `docs/MAINTAINERS.md` names a maintainer for the Anthropic-Labs watcher review (placeholder: TBD-by-project-owner; per D-30 + STATE.md todos line 81); also names a backup; the placeholder is acceptable in Phase 1 — the project owner fills it in before GA.
    - `docs/RAPID-RESPONSE.md` provides the template per D-31: positioning pivot to `interoperability with Claude Design`, marketplace copy variants (8 marketplaces), outreach list (Brad Frost, Marty Cagan).
    - Keyword-filter unit test: feeds a corpus of representative-real-world Anthropic blog post titles to the filter; asserts the heuristic correctly identifies known-positive titles (containing `5-stage design process` or `wireframe AI`) AND rejects false-positives (titles containing only `audit` without `design` context).
  </behavior>
  <action>Extend root `package.json`: add `"workspaces": ["evals/hosts/claude-code", "evals/hosts/codex-cli", "evals/hosts/cursor"]`; add npm script `"test:hosts": "vitest --workspace evals/hosts"`. Create `evals/hosts/<host>/package.json` for each of the 3 hosts: `{ "name": "@design-os/evals-hosts-<host>", "private": true, "type": "module", "scripts": { "test": "vitest" }, "devDependencies": { "vitest": "^2" } }`. Create `evals/hosts/<host>/vitest.config.ts`: `import { defineConfig } from 'vitest/config'; export default defineConfig({ test: { env: { HOST_PROFILE: '<host>' } } });` — substitute the host name. Create `evals/hosts/<host>/host-profile.test.ts`: import `dispatchSubagent` and `detectHost` from the absolute path `../../../assets/scripts/run-subagent.mjs`; assert that `process.env.HOST_PROFILE === '<host>'` (sanity check); for claude-code: mock the env so `CLAUDE_CODE_SESSION = '1'`, call dispatchSubagent with a trivial prompt, assert it does not throw (Phase 1's best-effort Claude Code path may return `{ error: 'no CLAUDE_CODE_BIN' }` which is acceptable as long as it does not throw); for codex-cli and cursor: mock CODEX_SESSION / CURSOR_SESSION, assert dispatch returns a sequential-fallback shape `{ kind: 'sequential-fallback', host, prompt }`. Create `.github/workflows/anthropic-watcher.yml`: `name: Anthropic-Labs Watcher`; trigger `schedule: [{ cron: '37 7 * * *' }]`, `workflow_dispatch: {}`; job: ubuntu-latest; steps: checkout, setup-node@v4 (node 22), `npm ci`, run a Node script (inline via `actions/github-script@v7` or external script) that: (1) calls `https://api.github.com/repos/anthropics/skills/releases?per_page=20` and extracts titles; (2) fetches Anthropic blog RSS via `node-fetch` or `fast-xml-parser` (use `fast-xml-parser` from STACK supporting libs list); (3) for each title, applies the keyword + heuristic filter; (4) for matches, queries existing issues with title prefix `[competitive-watch]`; if no existing issue, opens a new one tagged `competitive-watch` and cc's the @<maintainer> handle from `docs/MAINTAINERS.md`; (5) writes a heartbeat comment to a sticky tracking issue (search for issue title `Anthropic Watcher: Status Tracker` and comment `Ran $DATE; novel hits: $N`; create the issue if absent). Inline the keyword set: `['5-stage', 'design process', ' IA ', 'wireframe', 'state machine', 'audit']` + heuristic: include title only if `keyword_count >= 2 || (keyword_count >= 1 && /\bdesign(?:ed|er|ing)?\b/i.test(body))`. Create `.github/workflows/anthropic-watcher-heartbeat.yml`: cron `37 8 * * *`; uses `actions/github-script@v7` to query the last `Anthropic-Labs Watcher` workflow run via Actions API; if `run.created_at` > 26 hours ago OR run status is failure, open an issue `Anthropic watcher heartbeat missed (last run: <ts>)` cc'ing the maintainer; otherwise no-op. Create `docs/MAINTAINERS.md`: `# Maintainers\n\n## Anthropic-Labs watcher\n\n- **Primary:** @TBD (project owner to fill in before GA)\n- **Backup:** @TBD\n- **Weekly review cadence:** Friday\n- **Review checklist:** see docs/RAPID-RESPONSE.md\n\n## Schemas + Migrations\n\n- See Pitfall G CI rule (.github/workflows/schema-migration-guard.yml)\n\n## Trust posture\n\n- Copy reviewer: @TBD (project owner)\n- See docs/COPY-REVIEW-CHECKLIST.md`. Create `docs/RAPID-RESPONSE.md` per D-31: `# Rapid-response plan: Anthropic 5-stage equivalent ships during build window\n\n## Trigger\n\n- A `[competitive-watch]` issue lands AND the linked release/post describes a feature overlap of ≥3 of (research → IA → low-fi → interactions → tokens → design system extraction)\n\n## 72-hour response\n\n### Positioning pivot\n\n- Reframe from \"5-stage design process facilitator\" to \"interoperable with Claude Design — bring your own stage outputs, or use design-os end-to-end\"\n- Emphasize the unique value: `audit --reverse-engineer-stages` (Lovable refugee path), gate-runner with not_runnable terminal state, evidence-graded provenance system\n\n### Marketplace copy variants (8 marketplaces)\n\n- skills.sh: emphasize OSS heritage + canon citations\n- claudemarketplaces.com: emphasize the audit verb + slop-tells library\n- mcpmarket.com: emphasize the deterministic CI gates\n- smithery.ai / lobehub / fastmcp.me / playbooks.com / Tessl Registry: standard description\n\n### Outreach list\n\n- Brad Frost (design-systems-as-AI-context heritage) — DM via Twitter/Bluesky + cross-post on Frostapalooza Slack if invited\n- Marty Cagan (build-to-learn intellectual heritage — frame as inspiration, not endorsement)\n- Lenny Rachitsky (PRD pattern — newsletter pitch)\n\n### Decision authority\n\n- The maintainer named in MAINTAINERS.md owns the call to invoke this plan and posts the pivot announcement.`. Tests: `tests/hosts/profiles.test.ts` (3 host workspace dirs exist with required files; each vitest.config sets HOST_PROFILE correctly; package.json workspaces field includes all 3); `tests/watcher/keyword-filter.test.ts` (extract the keyword + heuristic logic into a unit-testable function `evals/watcher/keyword-filter.mjs` — refactor the inline workflow logic into a tiny ESM module so it can be unit-tested; test corpus of ~20 hand-authored title+body pairs; assert known-positive matches + known-negative rejections; assert the heuristic correctly disambiguates `audit` (without design context — reject) from `audit --reverse-engineer-stages` (with design context — match)). Implements DIST-02 (3 v1.5 skills total trigger metadata budget — the 3 SKILL.md skeleton descriptions from Plan 04 are individually ≤200 chars and aggregate well within the 5k cap), DIST-03 (per-skill validation), PREV-01 (final wiring), GTM-06, D-22, D-30, D-31, Pitfall E.</action>
  <verify>
    <automated>npx vitest run tests/hosts/profiles.test.ts tests/watcher/keyword-filter.test.ts --reporter=verbose && test -f .github/workflows/anthropic-watcher.yml && test -f .github/workflows/anthropic-watcher-heartbeat.yml && test -f docs/MAINTAINERS.md && test -f docs/RAPID-RESPONSE.md && grep -q "workspaces" package.json && node -e "const fs=require('fs');const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const expected=['evals/hosts/claude-code','evals/hosts/codex-cli','evals/hosts/cursor'];for(const w of expected){if(!pkg.workspaces.includes(w)){process.exit(1)}}" && DESCRIPTION_TOTAL=$(node -e "const m=require('gray-matter');let sum=0;for(const s of ['design','audit','handoff']){sum+=m.read('skills/'+s+'/SKILL.md').data.description.length}console.log(sum)") && test "$DESCRIPTION_TOTAL" -le 5000</automated>
  </verify>
  <done>3 host-profile workspaces wired into npm workspaces; each has its own vitest config + host-profile test; Anthropic watcher cron + heartbeat workflows exist; MAINTAINERS.md + RAPID-RESPONSE.md exist with the structure per D-30 + D-31; keyword filter has a unit-tested ESM module; aggregate v1.5 SKILL.md description char budget ≤5000 (DIST-02 verified — the 22-skill aggregate is enforced again at Phase 3 when all atoms ship); ≥8 vitest assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawned dev server (vite/next/astro) → environment variables | Secrets must not cross this seam — scrubEnvForPreview enforces |
| spawned dev server → filesystem | The dev server has read access to repoRoot; design-os does not run user-supplied scripts beyond the framework's `dev` command |
| RSS/release feed → workflow → GitHub issue | A malicious upstream could inject crafted titles to spam issues |
| Anthropic watcher cron → maintainer attention | A silently-failed cron erodes the GTM kill-risk monitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | API_KEY env vars leaked to spawned vite/next/astro processes | mitigate | scrubEnvForPreview strips `*_KEY|*_SECRET|*_TOKEN|*_PASSWORD` patterns; tested in security-sandbox.test.ts |
| T-05-02 | Elevation of Privilege | Path-traversal in isPathAllowed (`../`) | mitigate | Resolve + relative check rejects `..` prefix; tested |
| T-05-03 | Denial of Service | get-port allocation could exhaust ephemeral ports under heavy parallel preview load | accept | Phase 1 fixtures are single-spawn; production load is OS-bounded; reserve option releases on process exit |
| T-05-04 | Elevation of Privilege | vm2 alternative considered | mitigate | Phase 1 explicitly does NOT use vm2 or any vm-based sandbox; security sandbox is a permission boundary only (RESEARCH.md "Security Sandbox" pattern + comment in security-sandbox.mjs file header) |
| T-05-05 | Spoofing | Anthropic watcher could be fed fake titles via compromised RSS feed | accept | Watcher creates an issue for human review — not an auto-action; named maintainer triages |
| T-05-06 | Denial of Service | Heartbeat workflow itself could miss runs | accept | Best-effort; if both watcher + heartbeat miss, maintainer notices via weekly Friday review (D-30 hybrid posture) |
| T-05-07 | Repudiation | Watcher silently fails (network error, deleted feed) | mitigate | Heartbeat workflow detects missed daily runs and opens an issue (Pitfall E mitigation) |
| T-05-08 | Tampering | Routing registry hardcodes which routes are not-yet-implemented — a malicious PR could flip them | accept | Code review; the test suite asserts exact set membership so a flip would break tests |
| T-05-09 | Information Disclosure | Reference files could inadvertently contain copyrighted full quotes | mitigate | D-24 mandates citations not full quotes (≤2k tokens per file); corpus-completeness test asserts citation section present |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# 1. tsc + all Plan 05 tests
npx tsc --noEmit
npx vitest run tests/preview/ tests/routing/ tests/references/ tests/hosts/ tests/watcher/

# 2. CLI routing dispatcher works for each route status
tsx bin/design-os.mjs design --route design-bug --design-dir /tmp | grep -q "route_stub_dispatched"
tsx bin/design-os.mjs design --route new-product --design-dir /tmp | grep -q "route_not_yet_implemented"
tsx bin/design-os.mjs design --route invalid --design-dir /tmp | grep -q "unknown_route"

# 3. ROUTE-08 default behavior — no --route given prints prompt
tsx bin/design-os.mjs design --design-dir /tmp | grep -q "Which route"

# 4. References corpus completeness (12 mandatory + 6 gate checklists)
test "$(ls references/*.md | wc -l)" -ge 12   # 12 mandatory; references/prd/lenny-one-pager.md may be in subdir
test "$(ls references/gates/*.md | wc -l)" -eq 6   # stage-1, 2, 3, 4, 5a, 5b

# 5. References each ≤6000 chars (proxy for 2k tokens per D-24)
for f in references/*.md references/prd/*.md; do
  size=$(wc -c < "$f")
  if [ "$size" -gt 6000 ]; then echo "OVERSIZE: $f ($size chars)"; exit 1; fi
done

# 6. Host-profile workspaces wired
node -e "const pkg=require('./package.json');const expected=['evals/hosts/claude-code','evals/hosts/codex-cli','evals/hosts/cursor'];for(const w of expected){if(!pkg.workspaces.includes(w))process.exit(1)}"

# 7. Watcher workflows exist
test -f .github/workflows/anthropic-watcher.yml
test -f .github/workflows/anthropic-watcher-heartbeat.yml

# 8. MAINTAINERS + RAPID-RESPONSE docs exist
test -f docs/MAINTAINERS.md
grep -q "Anthropic-Labs watcher" docs/MAINTAINERS.md
test -f docs/RAPID-RESPONSE.md
grep -q "interoperability with Claude Design" docs/RAPID-RESPONSE.md

# 9. Security sandbox is permission-boundary only (no vm2 anywhere)
! grep -rE "(from 'vm2'|require\\('vm2'\\))" assets/scripts/

# 10. Aggregate SKILL.md description char budget ≤ 5000 (DIST-02 — 3 v1.5 skills)
node -e "const m=require('gray-matter');let sum=0;for(const s of ['design','audit','handoff']){sum+=m.read('skills/'+s+'/SKILL.md').data.description.length}if(sum>5000)process.exit(1);console.log('Total description chars:',sum)"
```
</verification>

<success_criteria>
- Port manager allocates non-colliding ports via get-port reserve + writes port.lock
- Playwright runner spawns + probes with allowed-status check (2xx/3xx/400-403); times out cleanly
- Security sandbox is a permission boundary (path allowlist + env scrub); NO vm2 or vm-based sandbox usage (CVE-2026-22709)
- 3 preview adapter scaffolds (Vite 6 / Next 15 / Astro 5) export prepare() returning { command, args, env, port, readyUrl }
- variant-distance.mjs ships 6-axis Stage 5a metric (preserved per PREV-05); Stage 3 structural metric explicitly deferred + stub throws
- run-subagent.mjs ships Claude Code best-effort + Codex/Cursor sequential fallback per D-23
- Routing registry recognizes all 7 routes; 4 implemented-stub (new-feature, design-bug, brand-refresh, PR-audit) + 3 not-yet-implemented (new-product, mature-app-refactor, DS-extraction); ROUTE-08 default-not-all-5-stages enforced
- 12 mandatory MVPA-06 references exist + 6 stage-gate checklists complete (stage-1, 2, 3, 4, 5a, 5b)
- Each reference ≤6000 chars (proxy for ≤2k tokens per D-24)
- design SKILL.md body updated with route table + default-not-all-5-stages note + references list
- 3 host-profile vitest workspaces wired (claude-code, codex-cli, cursor) per D-22
- Anthropic-Labs watcher daily cron + heartbeat workflow exist; MAINTAINERS.md names owner; RAPID-RESPONSE.md template ships
- Keyword filter unit-tested as a refactored ESM module
- Aggregate v1.5 SKILL.md description char budget ≤5000 (DIST-02 verified for 3 skills; 22-skill aggregate enforced at Phase 3)
- `tsc --noEmit` passes; ≥33 vitest assertions across `tests/preview/`, `tests/routing/`, `tests/references/`, `tests/hosts/`, `tests/watcher/`
</success_criteria>

<output>
After completion, create `.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-05-SUMMARY.md` recording:
- What was built (8 preview scripts + routing registry + dispatcher + 12 refs + 6 gate checklists complete + 3 host profiles + watcher + heartbeat + MAINTAINERS + RAPID-RESPONSE + keyword filter)
- Open Q4 disposition (keyword filter ships with 6 keywords + ≥2 keyword or 1 keyword + design-stem heuristic; tune in week 2 based on first week's hits)
- Pitfall E mitigation (heartbeat workflow shipped)
- Pitfall 9 / GTM kill-risk monitor live from week 1 per STATE.md todo
- Stage 3 structural-diversity metric explicitly deferred to Phase 3 (per project research flag) with a stub that throws
- Files created + tests added (count)
- Phase 1 wrap-up status:
  - 56 (orchestrator-listed: 64 enumerated) phase REQ-IDs distributed across 5 plans
  - All 7 Phase-1 pitfalls (PITFALLS.md mapping) mitigated: Pitfall A (Zod optional/nullable test — Plan 01), Pitfall B (tiktoken safety margin — Plan 02), Pitfall C (Excalidraw pin — Plan 01 package.json), Pitfall D (PII line-start anchor — Plan 04), Pitfall E (watcher heartbeat — Plan 05), Pitfall F (ESLint exhaustiveness — Plan 03), Pitfall G (schema-migration-guard CI — Plan 03), Pitfall 12 (Mermaid renderer — Plan 03)
  - Open questions disposition: Q1 closed (Zod 4 z.toJSONSchema substitution), Q2 closed for Phase 1 (structural-only baseline), Q3 calibration phase (Plan 03 reports number; v2.0 GA enables blocking), Q4 calibration phase (Plan 05 ships 6 keywords + heuristic; tune in week 2), Q5 partial close (Plan 01 pinned 2026.04 + flag scaffolded)
- Ready for Phase 2 (v2.0a Skeleton) — Phase 1 infrastructure complete; downstream blockers cleared
</output>
</content>
</invoke>