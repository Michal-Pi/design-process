---
phase: 03-v2-0b-full-5-stages-lovable-refugee-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/scripts/excalidraw-render.mjs
  - assets/scripts/wireframe-diversity.mjs
  - assets/scripts/gates/stage-3.mjs
  - skills/workflows/sketch.md
  - skills/atoms/lowfi/crazy-eights.md
  - skills/atoms/lowfi/converge.md
  - evals/adversarial/fid-03-styled-wireframe/fixture-builder.mjs
  - evals/adversarial/fid-03-styled-wireframe/run.test.ts
  - evals/golden/excalidraw-render.golden.json
  - tests/gates/stage-3.test.ts
  - references/buxton-sketching.md
  - references/sprint-crazy-eights.md
  - references/shape-up-pitches.md
  - evals/triggers/sketch/triggers.yaml
  - evals/bundles/fixtures/stage-2-to-3/upstream/wireframes.placeholder
  - evals/bundles/fixtures/stage-2-to-3/bundle.md
autonomous: true
requirements:
  - WF-04
  - ATOM-08
  - ATOM-09
  - FID-03
  - MVPB-01
  - MVPB-03
  - MVPB-04
  - REF-03

must_haves:
  truths:
    - "excalidraw-render.mjs accepts a skeleton IR array and produces valid .excalidraw JSON via convertToExcalidrawElements() — the LLM NEVER writes raw element arrays"
    - "wireframe-diversity.mjs correctly rejects any pair of near-identical wireframes with pairwise structural distance < 0.35"
    - "gate-stage-3.mjs returns {kind:'not_runnable', reason:'fidelity-cap-violation-FID-03'} for any .excalidraw file containing non-default strokeColor, backgroundColor, or fontFamily"
    - "gate-stage-3.mjs returns failed_after_repair when fewer than 3 variants exist or structural diversity < 0.35 between any pair"
    - "FID-03 adversarial suite: 100% of styled wireframe fixtures (n≥20) rejected by gate-stage-3.mjs; 100% of clean fixtures pass"
    - "sketch.md SKILL.md exists with ≤200 char description and numbered procedure body citing crazy-eights + converge atoms"
    - "crazy-eights.md and converge.md atoms exist with proper agentskills.io v1 frontmatter"
    - "excalidraw-render.mjs passes lint-determinism.mjs — zero LLM client imports"
    - "wireframe-diversity.mjs golden adversarial fixture distance ≈ 0.0 correctly fails the 0.35 threshold"
    - "evals/golden/excalidraw-render.golden.json exists and verify-golden.mjs passes 5× byte-identical"
  artifacts:
    - path: "assets/scripts/excalidraw-render.mjs"
      provides: "Skeleton IR → Excalidraw JSON via convertToExcalidrawElements(). The sole .excalidraw writer."
      contains: "convertToExcalidrawElements"
    - path: "assets/scripts/wireframe-diversity.mjs"
      provides: "3-factor structural distance: bounding-box grid + element count delta + nesting depth ratio"
      contains: "pairwiseDistance"
    - path: "assets/scripts/gates/stage-3.mjs"
      provides: "Stage 3 gate: count ≥3, FID-03 fidelity check, diversity ≥0.35, CHOICE.md present"
      contains: "fidelity-cap-violation-FID-03"
    - path: "skills/workflows/sketch.md"
      provides: "W3 sketch workflow SKILL.md"
      contains: "crazy-eights"
    - path: "skills/atoms/lowfi/crazy-eights.md"
      provides: "ATOM-08 Crazy 8s procedure"
      contains: "skeleton IR"
    - path: "skills/atoms/lowfi/converge.md"
      provides: "ATOM-09 Decider convergence → CHOICE.md"
      contains: "CHOICE.md"
    - path: "evals/adversarial/fid-03-styled-wireframe/run.test.ts"
      provides: "100% FID-03 rejection adversarial suite (≥20 styled + ≥20 clean fixtures)"
      contains: "fidelity-cap-violation-FID-03"
    - path: "evals/golden/excalidraw-render.golden.json"
      provides: "Golden fixture for verify-golden.mjs determinism CI"
      contains: "excalidrawElements"
  key_links:
    - from: "skills/atoms/lowfi/crazy-eights.md"
      to: "assets/scripts/excalidraw-render.mjs"
      via: "node bin/design-os.mjs excalidraw-render --input skeleton-ir.json --output design/wireframes/<screen>/"
      pattern: "excalidraw-render"
    - from: "assets/scripts/gates/stage-3.mjs"
      to: "assets/scripts/wireframe-diversity.mjs"
      via: "import { computePairwiseDistances } from '../wireframe-diversity.mjs'"
      pattern: "wireframe-diversity"
    - from: "assets/scripts/gates/stage-3.mjs"
      to: ".design-os/preview/<run-id>/wireframes/"
      via: "--staged flag (INVARIANT-01 compliance)"
      pattern: "staged"
---

<objective>
Deliver Stage 3 (Sketch/Low-Fi) by implementing: (1) `excalidraw-render.mjs` — the sole script allowed to emit `.excalidraw` JSON, translating skeleton IR via `convertToExcalidrawElements()`; (2) `wireframe-diversity.mjs` — the novel 3-factor structural diversity metric that prevents near-clone Crazy 8 variants; (3) `gate-stage-3.mjs` — full business logic implementing the count ≥3, FID-03 fidelity cap, structural diversity ≥0.35, and CHOICE.md presence checks; (4) the `sketch` workflow SKILL.md plus `lowfi/crazy-eights` and `lowfi/converge` atoms; and (5) the FID-03 adversarial CI suite.

Purpose: Completing Stage 3 is the first half of closing the Garrett spine. The structural diversity gate is the highest-risk new component in Phase 3 — it must be calibrated against an adversarial near-clone fixture before anything else is considered done.

Output: New scripts `excalidraw-render.mjs` + `wireframe-diversity.mjs`, filled gate `stage-3.mjs`, 3 SKILL.md files, adversarial FID-03 suite, golden fixture, 3 Stage 3 references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-CONTEXT.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@skills/workflows/INVARIANTS.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-01-SUMMARY.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-02-SUMMARY.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-05-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do not explore codebase — use these directly. -->

From assets/scripts/gates/base.mjs:
  runGate(stage, designDir, config) → Promise<GateResultType>
  GateResult discriminated union: { kind: 'pass' | 'pass_with_warnings' | 'failed_after_repair' | 'not_runnable' | 'user_overridden' }

From assets/scripts/gates/stage-3.mjs (Phase 1 skeleton — fill this, do not rewrite):
  export async function runStage3Gate(designDir)
  Phase 1 returns skeleton pass — Phase 3 replaces with real logic.
  CHECKLIST_PATH = join(ROOT, 'references/gates/stage-3.md')

From assets/scripts/schemas/validate.mjs:
  validateArtifact(schemaName, data) → { valid: boolean, errors: ValidationError[] }
  Schemas available: 'persona', 'sitemap', 'interaction-spec', 'audit-report', 'handoff-bundle'

From assets/scripts/verify-golden.mjs:
  verifyGolden(scriptPath, inputArgs, goldenPath) → comparison result
  Golden files live in evals/golden/*.golden.json

From assets/scripts/lint-determinism.mjs:
  Runs at CI; rejects any file under assets/scripts/ with import from LLM client packages.
  excalidraw-render.mjs and wireframe-diversity.mjs MUST have zero LLM imports.

From skills/workflows/audit.md (Phase 2, for format reference):
  SKILL.md frontmatter: name, description (≤200 chars), type: workflow, compatibility: [claude-code, codex-cli, cursor]
  Body: numbered procedure steps, explicit node bin/design-os.mjs invocations (never direct script calls)

Excalidraw IR shape (per D-54 / RESEARCH.md §3.1):
  { type: 'rectangle'|'text'|'frame'|'group', x: number, y: number, w: number, h: number, label: string, children?: IR[] }
  convertToExcalidrawElements() from '@excalidraw/excalidraw' 0.18+

FID-03 default constants (per D-56 / RESEARCH.md §3.2):
  strokeColor: '#1e1e1e'
  backgroundColor: 'transparent'
  fontFamily: 1   (Virgil = index 1 in Excalidraw)

Wireframe diversity metric (per D-55 / RESEARCH.md §3.3):
  Factor 1: bounding-box 3×3 grid histogram cosine distance
  Factor 2: |countA - countB| / max(countA, countB)
  Factor 3: |depthA - depthB| / max(depthA, depthB)
  Composite: (f1 + f2 + f3) / 3  — minimum threshold 0.35 (OQ-4 resolution)

findingId pattern for Stage 3 (per INVARIANT-06):
  3-fidelity-001, 3-diversity-001, 3-count-001, 3-choice-001

Wireframe file naming (OQ-4 resolved): v1.excalidraw … v8.excalidraw (numeric, sortable)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-03-01-A: excalidraw-render.mjs + wireframe-diversity.mjs + golden fixture</name>
  <files>
    assets/scripts/excalidraw-render.mjs,
    assets/scripts/wireframe-diversity.mjs,
    evals/golden/excalidraw-render.golden.json,
    tests/gates/stage-3-emit.test.ts
  </files>
  <behavior>
    - Test 1: excalidraw-render.mjs given a minimal IR array [{type:'rectangle',x:0,y:0,w:100,h:50,label:'Nav'}] produces valid JSON with at least one element entry and no LLM imports
    - Test 2: excalidraw-render.mjs output is byte-identical on two consecutive runs from the same IR (determinism)
    - Test 3: wireframe-diversity.mjs given two IDENTICAL element arrays returns composite distance ≈ 0.0 (< 0.35 → fails threshold check)
    - Test 4: wireframe-diversity.mjs given two structurally distinct arrays (different grid distribution, different count, different depth) returns distance ≥ 0.35
    - Test 5: computePairwiseDistances([a, b, c]) returns array of 3 pairwise distances (nC2 for n=3)
    - Test 6: lint-determinism scan of excalidraw-render.mjs and wireframe-diversity.mjs reports zero violations
  </behavior>
  <action>
    RED: Write tests/gates/stage-3-emit.test.ts with the 6 behaviors above. Run npx vitest run tests/gates/stage-3-emit.test.ts — all 6 must FAIL. Commit: test(03-01): add failing tests for excalidraw-render + wireframe-diversity

    GREEN (implement both scripts):

    assets/scripts/excalidraw-render.mjs — per D-54:
    - Import convertToExcalidrawElements from '@excalidraw/excalidraw'. If the API is unavailable at runtime, fall back to direct element construction per docs.excalidraw.com (Assumption A1 fallback).
    - Export function renderSkeletonIR(irArray): accepts array of IR objects, calls convertToExcalidrawElements, wraps in { type:'excalidraw', version:2, source:'design-os', elements: [...], appState:{}, files:{} }.
    - CLI usage: node bin/design-os.mjs excalidraw-render --input <path-to-ir.json> --output <dir> --screen <name>. Writes <dir>/<name>/v1.excalidraw through v8.excalidraw depending on how many IR objects are in the input array.
    - Add CLI subcommand assets/scripts/cli/excalidraw-render.mjs that wires the Commander --input/--output/--screen flags to renderSkeletonIR.
    - canonicalize() the output elements array (recursive key sort) before writing for determinism.
    - No LLM imports — pure @excalidraw/excalidraw transform. Must pass lint-determinism.mjs.

    assets/scripts/wireframe-diversity.mjs — per D-55:
    - Export function computeStructuralFingerprint(elements): returns { gridHistogram: number[9], elementCount: number, maxDepth: number }.
    - Grid histogram: for each element, compute which of 9 cells (3×3) its center falls in given the bounding box of all elements combined. Normalize to sum=1.
    - Max depth: recursively count container nesting (elements with children arrays or grouped by containerId) — use recursion depth, not element count.
    - Export function gridCosineSimilarity(h1, h2): standard cosine similarity on two 9-element arrays.
    - Export function pairwiseDistance(fingerprintA, fingerprintB): returns composite (f1 + f2 + f3) / 3 as described in RESEARCH.md §3.3.
    - Export function computePairwiseDistances(elementsArrays): returns array of { i, j, distance } for all pairs.
    - Export function checkDiversityThreshold(elementsArrays, threshold=0.35): returns { passes: boolean, violations: [{i,j,distance}] }.
    - Must pass lint-determinism.mjs (no LLM imports).

    evals/golden/excalidraw-render.golden.json — golden fixture for verify-golden.mjs:
    - Run renderSkeletonIR on a 3-element IR array: [{type:'rectangle',x:0,y:0,w:200,h:60,label:'Header'}, {type:'rectangle',x:0,y:80,w:200,h:300,label:'Content'}, {type:'text',x:10,y:10,w:180,h:30,label:'Title'}].
    - Capture output and write to evals/golden/excalidraw-render.golden.json.
    - Add the golden fixture to verify-golden.mjs CI (extend the golden registry to include 'excalidraw-render' → 'evals/golden/excalidraw-render.golden.json').

    Commit: feat(03-01): implement excalidraw-render.mjs + wireframe-diversity.mjs + golden fixture
  </action>
  <verify>
    <automated>npx vitest run tests/gates/stage-3-emit.test.ts && node assets/scripts/verify-golden.mjs 2>&1 | grep -E "(PASS|excalidraw)" && node assets/scripts/lint-determinism.mjs assets/scripts/excalidraw-render.mjs assets/scripts/wireframe-diversity.mjs 2>&1 | grep -E "(CLEAN|0 violation)"</automated>
  </verify>
  <done>6/6 emit tests pass. verify-golden.mjs PASS on excalidraw-render fixture. lint-determinism reports 0 violations for both new scripts. Identical IR input produces byte-identical output on two consecutive runs.</done>
</task>

<task type="auto" tdd="true">
  <name>T-03-01-B: gate-stage-3.mjs full business logic + FID-03 adversarial suite</name>
  <files>
    assets/scripts/gates/stage-3.mjs,
    tests/gates/stage-3.test.ts,
    evals/adversarial/fid-03-styled-wireframe/fixture-builder.mjs,
    evals/adversarial/fid-03-styled-wireframe/run.test.ts,
    references/gates/stage-3.md
  </files>
  <behavior>
    - Test 1: gate returns {kind:'not_runnable', reason:'fidelity-cap-violation-FID-03', evidence:[...]} when any .excalidraw file in staged path has strokeColor !== '#1e1e1e'
    - Test 2: gate returns {kind:'not_runnable', reason:'fidelity-cap-violation-FID-03'} when backgroundColor !== 'transparent'
    - Test 3: gate returns {kind:'not_runnable', reason:'fidelity-cap-violation-FID-03'} when fontFamily !== 1
    - Test 4: gate returns {kind:'failed_after_repair', findings:[{findingId:'3-count-001'}]} when staged path has fewer than 3 .excalidraw files
    - Test 5: gate returns {kind:'failed_after_repair', findings:[{findingId:'3-diversity-001'}]} when 3+ variants exist but any pairwise distance < 0.35 (near-clone golden fixture)
    - Test 6: gate returns {kind:'failed_after_repair', findings:[{findingId:'3-choice-001'}]} when ≥3 diverse clean variants exist but CHOICE.md is absent
    - Test 7: gate returns {kind:'pass'} when ≥3 diverse clean variants exist AND CHOICE.md present AND no FID-03 violations
    - Test 8: gate runs against --staged .design-os/preview/<run-id>/ path, NOT against design/ (INVARIANT-01)
    - FID-03 adversarial: fixture-builder.mjs generates 20 styled + 20 clean .excalidraw files; run.test.ts asserts 20/20 rejection of styled, 20/20 pass of clean
  </behavior>
  <action>
    RED: Write tests/gates/stage-3.test.ts with tests 1-8 above. Run npx vitest run tests/gates/stage-3.test.ts — all must FAIL (gate still returns skeleton pass). Commit: test(03-01): add failing tests for gate-stage-3 full business logic

    GREEN (implement gate-stage-3.mjs):

    Replace the Phase 1 skeleton body of assets/scripts/gates/stage-3.mjs with:

    1. Accept designDir (the staged path — callers pass .design-os/preview/<run-id>/ per INVARIANT-01).
    2. Glob `designDir/wireframes/**/*.excalidraw` for all excalidraw files (use globby 14.x).
    3. Count check: if count < 3, return { kind:'failed_after_repair', reason:'insufficient-variants', findings:[{findingId:'3-count-001', stage:3, severity:'BLOCKER', fixRecipe:'Need ≥3 wireframe variants. Re-run crazy-eights atom.'}] }.
    4. FID-03 check: for each .excalidraw file, parse JSON, iterate elements[]. For each element check strokeColor, backgroundColor, fontFamily against FID03_DEFAULTS = { strokeColor:'#1e1e1e', backgroundColor:'transparent', fontFamily:1 }. Collect violations. If any violation, return { kind:'not_runnable', reason:'fidelity-cap-violation-FID-03', evidence: violations }.
       Note: not_runnable (not failed_after_repair) because the fix is re-emit via excalidraw-render.mjs, not repair.
    5. Structural diversity check: load elements[] from each .excalidraw. Call checkDiversityThreshold(elementsArrays, 0.35) from wireframe-diversity.mjs. If passes===false, return { kind:'failed_after_repair', reason:'insufficient-diversity', findings:[{findingId:'3-diversity-001', stage:3, severity:'BLOCKER', evidence:violations, fixRecipe:'Variants too similar (pairwise distance < 0.35). Re-run crazy-eights with more layout diversity.'}] }.
    6. CHOICE.md check: verify `designDir/wireframes/*/CHOICE.md` exists (globby). If absent, return { kind:'failed_after_repair', findings:[{findingId:'3-choice-001', stage:3, severity:'BLOCKER', fixRecipe:'Run converge atom to produce CHOICE.md selecting one variant.'}] }.
    7. All checks pass: return { kind:'pass', evidence:'proto' }.

    Create references/gates/stage-3.md checklist with the 4-column GFM table format matching stage-1.md and stage-2.md (Check | Required for PASS | Required for VALIDATED grade | Citation). Include: variant-count, fidelity-cap, structural-diversity, choice-present checks.

    Create evals/adversarial/fid-03-styled-wireframe/ suite:
    - fixture-builder.mjs: generates 20 styled .excalidraw files (each with deliberate color injection: strokeColor:'#FF0000' or backgroundColor:'#0000FF' or fontFamily:2) and 20 clean files (default values only). Writes to a temp dir during test run.
    - run.test.ts: calls fixture-builder, then calls gate on each fixture. Asserts styled files 20/20 return { kind:'not_runnable', reason:'fidelity-cap-violation-FID-03' }. Asserts clean files 20/20 return kind that is NOT not_runnable for FID-03 (pass, pass_with_warnings, or failed_after_repair for other reasons).
    Pattern mirrors Phase 2 evals/adversarial/red-05-synthetic-block/run.test.ts.

    Commit: feat(03-01): gate-stage-3.mjs full business logic + FID-03 adversarial suite
  </action>
  <verify>
    <automated>npx vitest run tests/gates/stage-3.test.ts evals/adversarial/fid-03-styled-wireframe/run.test.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>8/8 gate unit tests pass. FID-03 adversarial: 20/20 styled rejected, 20/20 clean not-FID-03-rejected. Full vitest suite ≥ 815 tests still passing (no regressions). tsc --noEmit clean. lint-determinism clean on stage-3.mjs.</done>
</task>

<task type="auto">
  <name>T-03-01-C: sketch SKILL.md + crazy-eights + converge atoms + triggers + reference files + bundle fixture update</name>
  <files>
    skills/workflows/sketch.md,
    skills/atoms/lowfi/crazy-eights.md,
    skills/atoms/lowfi/converge.md,
    evals/triggers/sketch/triggers.yaml,
    references/buxton-sketching.md,
    references/sprint-crazy-eights.md,
    references/shape-up-pitches.md,
    evals/bundles/fixtures/stage-2-to-3/upstream/wireframes.placeholder,
    evals/bundles/fixtures/stage-2-to-3/bundle.md
  </files>
  <action>
    Create skills/workflows/sketch.md — W3 workflow SKILL.md (per D-54, WF-04):
    - Frontmatter: name: design-os/sketch, description ≤200 chars (e.g., "Stage 3: Generate ≥8 divergent wireframe concepts via Crazy 8s, converge on 1 with CHOICE.md. Excalidraw JSON emitted by script — never hand-built."), type: workflow, stage: 3, compatibility: [claude-code, codex-cli, cursor].
    - Procedure (numbered steps per Phase 2 SKILL.md format):
      1. Read Stage 2 handoff bundle from .design-os/preview/<run-id>/stage-2-bundle.md.
      2. Run crazy-eights atom (see skills/atoms/lowfi/crazy-eights.md) — produces skeleton-ir.json with 8 wireframe IR objects in .design-os/preview/<run-id>/wireframes/<screen>/.
      3. Emit Excalidraw files: `node bin/design-os.mjs excalidraw-render --input .design-os/preview/<run-id>/wireframes/<screen>/skeleton-ir.json --output .design-os/preview/<run-id>/wireframes/<screen>/` — produces v1.excalidraw through v8.excalidraw.
      4. Run gate: `node bin/design-os.mjs gate --stage 3 --staged .design-os/preview/<run-id>/` — must pass FID-03 + diversity checks.
      5. Run converge atom (see skills/atoms/lowfi/converge.md) — produces .design-os/preview/<run-id>/wireframes/<screen>/CHOICE.md.
      6. Re-run gate to confirm CHOICE.md present.
      7. Build handoff bundle: `node bin/design-os.mjs handoff-bundle --from 3 --to 4 --staged .design-os/preview/<run-id>/`.
      8. Surface diff; await user `--apply` before continuing to Stage 4.
    - References section: @references/buxton-sketching.md, @references/sprint-crazy-eights.md, @references/shape-up-pitches.md.
    - INVARIANTS section: cite INVARIANT-01 (gate-against-staged), INVARIANT-02 (--apply required), INVARIANT-04 (≤200 char description), INVARIANT-05 (no LLM in scripts).
    - Token budget: ≤25k p50 (COST-03, D-66 sketch ceiling).

    Create skills/atoms/lowfi/crazy-eights.md — ATOM-08:
    - Frontmatter: name: design-os/lowfi/crazy-eights, type: atom, stage: 3, compatibility: [claude-code, codex-cli, cursor], description ≤200 chars.
    - Two modes: standalone-bootstrap (user invokes directly) and workflow-procedure (called from sketch.md step 2).
    - Procedure: given a screen name from the Stage 2 sitemap, produce 8 wireframe skeleton IR objects — each representing a structurally DIFFERENT layout approach for that screen (e.g., card-list, hero+CTA, table, sidebar+content, bottom-nav, wizard, split-pane, dashboard). Each IR object: { type, x, y, w, h, label, children? }. Emit as skeleton-ir.json array.
    - Strict instruction: "Emit IR objects only. DO NOT produce raw Excalidraw element JSON with literal type/version/elements fields. The skeleton IR will be converted to Excalidraw format by excalidraw-render.mjs."
    - Quality guide: each of the 8 slots must differ in layout axis (list vs grid vs spatial), information hierarchy, and primary navigation pattern. Near-identical layouts will be rejected by wireframe-diversity.mjs with pairwise distance < 0.35.
    - Reference: @references/sprint-crazy-eights.md, @references/buxton-sketching.md, @references/shape-up-pitches.md.
    - Wire naming: variants emitted as v1.excalidraw … v8.excalidraw (OQ-4 resolution).

    Create skills/atoms/lowfi/converge.md — ATOM-09:
    - Frontmatter: name: design-os/lowfi/converge, type: atom, stage: 3, description ≤200 chars.
    - Procedure: given the ≥3 selected wireframe variants for a screen (from .design-os/preview/<run-id>/wireframes/<screen>/), produce CHOICE.md with: (a) selected variant filename (e.g., v3.excalidraw), (b) rationale citing ≥2 specific layout reasons, (c) list of rejected variants and why each was eliminated.
    - Format for CHOICE.md: YAML frontmatter (artifact: wireframe-choice, stage: 3, schemaVersion: 1) + body sections: ## Selected Variant, ## Rationale, ## Rejected Variants.
    - Instruction: the choice is the executor's judgment — do not default to 'v1'. Review all variants against the user task before choosing.

    Create evals/triggers/sketch/triggers.yaml:
    - Skill: design-os/sketch
    - shouldFire (≥12 prompts): "create wireframes for the dashboard screen", "generate low-fi sketches for onboarding", "make crazy eights for the checkout flow", "produce wireframe variants for search results", "sketch layout alternatives for the profile page", "run stage 3 on the admin screen", "low fidelity wireframes for notifications", "excalidraw wireframes for the navigation", "wireframe concepts for the landing page", "sketch divergent layouts for cart screen", "create sketch variants for home screen", "run crazy eights on signup flow"
    - shouldNotFire (≥12 prompts): "create hi-fi mockups", "generate token colors", "audit the design system", "build the react component", "run stage 4 interaction design", "deploy the app", "create a persona", "map the user journey", "build a sitemap", "generate DESIGN.md", "run style workflow", "systematize tokens"

    Create 3 reference files (Markdown summaries with citations — concise but substantive):
    - references/buxton-sketching.md: Bill Buxton's "Sketching User Experiences" — key principles: sketching as ideation not documentation, low cost = high exploration rate, physical attributes of sketches (quick, timely, inexpensive, disposable, abundant). Cite: Buxton (2007) ISBN 978-0123740373. Stage: 3.
    - references/sprint-crazy-eights.md: Google Ventures Sprint "Crazy 8s" exercise — 8 sketches in 8 minutes, forces divergent thinking, quantity over quality at ideation phase, time constraint prevents attachment. Cite: Knapp, Zeratsky, Kowitz (2016) "Sprint". Stage: 3.
    - references/shape-up-pitches.md: Basecamp Shape Up "Fat Marker Sketches" and pitch format — breadboard + fat-marker sketch as appetite-bounded scope. Cite: Ryan Singer (2019) "Shape Up" (basecamp.com/shapeup). Stage: 3.

    Update evals/bundles/fixtures/stage-2-to-3/upstream/wireframes.placeholder:
    - Replace Phase 1 placeholder content with a realistic Stage 2→3 handoff bundle sample: include a sitemap.json reference, 2 screen names, note that wireframe skeleton IR will be placed in .design-os/preview/<run-id>/wireframes/<screen>/ per sketch.md workflow.

    Update evals/bundles/fixtures/stage-2-to-3/bundle.md:
    - Update artifactsInventory to reference the new wireframes.placeholder format. Recompute sourceHash (SHA-256 of upstream/ directory content). Keep bundle.md structurally valid per handoff-bundle.v1.json schema.

    Verify SKILL.md description lengths (INVARIANT-04):
    - sketch.md description: count chars, must be ≤200.
    - crazy-eights.md description: count chars, must be ≤200.
    - converge.md description: count chars, must be ≤200.
  </action>
  <verify>
    <automated>node -e "const {readFileSync}=require('fs');const m=require('gray-matter');['skills/workflows/sketch.md','skills/atoms/lowfi/crazy-eights.md','skills/atoms/lowfi/converge.md'].forEach(p=>{const d=m(readFileSync(p,'utf8')).data;if(d.description.length>200)throw new Error(p+' desc too long: '+d.description.length)}); console.log('PASS all descriptions ≤200 chars')" && npx vitest run tests/eval/phase2-skillgrade.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>sketch.md, crazy-eights.md, converge.md exist with valid agentskills.io v1 frontmatter and ≤200 char descriptions. triggers.yaml has ≥12 shouldFire + ≥12 shouldNotFire prompts. 3 reference files exist with substantive content and citations. Bundle fixture updated. Phase 2 skillgrade test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skeleton IR → Excalidraw JSON | LLM-produced IR data fed to excalidraw-render.mjs. Malformed IR could cause parse errors. |
| .excalidraw files → gate | Gate reads arbitrary JSON from staged path — path traversal risk. |
| --staged flag input | CLI accepts user-provided path. Must validate path is inside project. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | excalidraw-render.mjs --input path | mitigate | Validate --input path exists and is a .json file. Reject paths containing `..` (path traversal guard). |
| T-03-01-02 | Tampering | gate-stage-3.mjs --staged path | mitigate | Validate staged dir exists. Reject paths outside project root (same guard as INVARIANT-01 staged-path enforcement). |
| T-03-01-03 | Information Disclosure | FID-03 evidence array | accept | Evidence array contains only element property values, no PII. Low risk. |
| T-03-01-04 | Denial of Service | Large .excalidraw with 10k+ elements | accept | Design-time tool, not a server. Memory pressure on local machine is acceptable. No timeout needed. |
</threat_model>

<verification>
After all 3 tasks complete:

1. npx vitest run — must show ≥ 815 + new Stage 3 tests, all passing
2. npx tsc --noEmit — zero errors
3. node assets/scripts/lint-determinism.mjs assets/scripts/excalidraw-render.mjs assets/scripts/wireframe-diversity.mjs assets/scripts/gates/stage-3.mjs — CLEAN
4. node assets/scripts/verify-golden.mjs — PASS (excalidraw-render golden fixture byte-identical)
5. FID-03 adversarial: npx vitest run evals/adversarial/fid-03-styled-wireframe/run.test.ts — 40/40 pass
6. node -e with gray-matter check — all 3 SKILL.md descriptions ≤200 chars
</verification>

<success_criteria>
- gate-stage-3.mjs: count ≥3, FID-03 fidelity, diversity ≥0.35, CHOICE.md checks all implemented and unit tested
- excalidraw-render.mjs is the sole script that emits .excalidraw files (no raw JSON hand-building in any SKILL.md)
- FID-03 adversarial suite: 20/20 styled wireframes rejected, 20/20 clean wireframes pass non-FID-03 checks
- wireframe-diversity.mjs near-clone golden fixture returns distance ≈ 0.0 (correctly fails 0.35 threshold)
- verify-golden.mjs PASS on excalidraw-render.golden.json
- sketch.md + crazy-eights.md + converge.md exist with proper frontmatter and ≤200 char descriptions
- All prior 815 tests still pass (zero regressions)
- 03-02 can start immediately in parallel (zero shared files)
</success_criteria>

<output>
After completion, create .planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-01-SUMMARY.md following the template at @$HOME/.claude/get-shit-done/templates/summary.md with:
- phase: 03-v2-0b-full-5-stages-lovable-refugee-path / plan: 01 / subsystem: stage-3-sketch
- dependency_graph.provides: excalidraw-render.mjs, wireframe-diversity.mjs, gate-stage-3.mjs (full), sketch.md, crazy-eights.md, converge.md, FID-03 adversarial suite
- Record the calibrated threshold value used (0.35 or adjusted) and the golden fixture near-clone distance score
- Record any A1 fallback (if convertToExcalidrawElements() API was unavailable)
</output>
