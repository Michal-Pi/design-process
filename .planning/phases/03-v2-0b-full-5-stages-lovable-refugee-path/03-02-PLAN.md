---
phase: 03-v2-0b-full-5-stages-lovable-refugee-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/scripts/state-machine-emit.mjs
  - assets/scripts/mermaid-render.mjs
  - assets/scripts/gates/stage-4.mjs
  - assets/scripts/audit/stage-3-pr.mjs
  - assets/scripts/audit/stage-4-pr.mjs
  - skills/workflows/interact.md
  - skills/atoms/ixd/state-catalog.md
  - skills/atoms/ixd/pattern-variants.md
  - skills/atoms/ixd/state-machine.md
  - evals/golden/state-machine-emit.golden.json
  - tests/gates/stage-4.test.ts
  - tests/audit/stage-3-pr.test.ts
  - tests/audit/stage-4-pr.test.ts
  - references/saffer-microinteractions.md
  - references/tidwell-patterns.md
  - references/head-motion.md
  - references/hax-18.md
  - references/xstate-v5.md
  - references/apg.md
  - references/material-3.md
  - evals/triggers/interact/triggers.yaml
  - evals/bundles/fixtures/stage-3-to-4/upstream/interactions.placeholder
  - evals/bundles/fixtures/stage-3-to-4/bundle.md
autonomous: true
requirements:
  - WF-05
  - ATOM-10
  - ATOM-11
  - ATOM-12
  - FID-04
  - MVPB-01
  - MVPB-03
  - MVPB-04
  - MVPB-07
  - MVPB-08
  - REF-03
  - AUDIT-01

must_haves:
  truths:
    - "state-machine-emit.mjs takes a single interaction-spec.v1.json IR and produces BOTH Mermaid stateDiagram-v2 AND (conditionally per D-57) XState v5 machine — never two separate LLM passes"
    - "Mermaid output is validated by mermaid-render.mjs extended for stateDiagram-v2, with max-2-retry repair loop on validation failure"
    - "XState v5 machine is emitted ONLY when asyncOperations:true AND stateCount≥3 AND hasConditionalTransitions:true (D-57 heuristic)"
    - "gate-stage-4.mjs checks (a) sitemap route coverage, (b) loading/empty/error/success states present, (c) no undefined state names in Mermaid diagram"
    - "stage-3-pr.mjs detects new screens without CHOICE.md; stage-4-pr.mjs detects async ops without loading/error states"
    - "state-machine-emit.mjs passes lint-determinism.mjs — zero LLM client imports"
    - "evals/golden/state-machine-emit.golden.json exists and verify-golden.mjs passes 5× byte-identical"
    - "interact.md SKILL.md, state-catalog.md, pattern-variants.md, state-machine.md atoms all have ≤200 char descriptions"
  artifacts:
    - path: "assets/scripts/state-machine-emit.mjs"
      provides: "Single IR → Mermaid stateDiagram-v2 + conditional XState v5 machine emit"
      contains: "stateDiagram-v2"
    - path: "assets/scripts/mermaid-render.mjs"
      provides: "Extended to dispatch on stateDiagram-v2 diagram type with 2-retry repair"
      contains: "stateDiagram-v2"
    - path: "assets/scripts/gates/stage-4.mjs"
      provides: "Stage 4 gate: sitemap coverage, state completeness, no open transitions (D-59)"
      contains: "4-coverage-001"
    - path: "assets/scripts/audit/stage-3-pr.mjs"
      provides: "PR detector for Stage 3: new screens without CHOICE.md, significant layout drift"
      contains: "3-pr-choice-001"
    - path: "assets/scripts/audit/stage-4-pr.mjs"
      provides: "PR detector for Stage 4: missing state catalogs, async without loading/error, HAX-18"
      contains: "4-pr-states-001"
    - path: "evals/golden/state-machine-emit.golden.json"
      provides: "Golden fixture for verify-golden.mjs determinism CI"
      contains: "stateDiagram-v2"
    - path: "skills/workflows/interact.md"
      provides: "W4 interact workflow SKILL.md"
      contains: "state-catalog"
  key_links:
    - from: "skills/atoms/ixd/state-machine.md"
      to: "assets/scripts/state-machine-emit.mjs"
      via: "node bin/design-os.mjs state-machine-emit --spec design/interactions/<screen>.spec.md --output .design-os/preview/<run-id>/interactions/"
      pattern: "state-machine-emit"
    - from: "assets/scripts/gates/stage-4.mjs"
      to: "design/ia/sitemap.json"
      via: "sitemap route coverage check using globby"
      pattern: "sitemap.*route"
    - from: "assets/scripts/state-machine-emit.mjs"
      to: "assets/scripts/mermaid-render.mjs"
      via: "validateMermaidSource(mmdString) for stateDiagram-v2 validation"
      pattern: "mermaid-render"
---

<objective>
Deliver Stage 4 (Interact/IxD) by implementing: (1) `state-machine-emit.mjs` — the single IR→dual-output script that produces both Mermaid stateDiagram-v2 (always) and XState v5 machine (conditionally per D-57) from `interaction-spec.v1.json`; (2) `mermaid-render.mjs` extended for stateDiagram-v2 diagram type with the 2-retry repair loop; (3) `gate-stage-4.mjs` — full business logic for the three D-59 completeness conditions; (4) Stage 3 and Stage 4 PR detectors; (5) the `interact` workflow SKILL.md and three IxD atoms; and (6) 7 new Stage 4 reference files.

Purpose: Stage 4 is the second half of closing the Garrett spine, and the XState conditional emit is the most architecturally novel component. The golden test must exist before implementation, as the Phase 1 skeleton `state-machine-emit.mjs` has no fixture and `verify-golden.mjs` CI requires it (Pitfall G).

Output: Filled `state-machine-emit.mjs`, extended `mermaid-render.mjs`, full `gate-stage-4.mjs`, two PR detectors, 4 SKILL.md files, 7 references, golden fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-CONTEXT.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-RESEARCH.md
@.planning/PROJECT.md
@skills/workflows/INVARIANTS.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-01-SUMMARY.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-05-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do not explore codebase — use these directly. -->

From assets/scripts/gates/stage-4.mjs (Phase 1 skeleton — fill this):
  export async function runStage4Gate(designDir)
  Phase 1 returns skeleton pass — Phase 3 replaces with D-59 logic.

From schemas/dist/interaction-spec.v1.json (Phase 1, already shipped):
  Required fields: asyncOperations (boolean), stateCount (number), hasConditionalTransitions (boolean)
  states[] array: [{ name: string, type: 'loading'|'empty'|'error'|'success'|'custom' }]
  transitions[] array: [{ from: string, to: string, event: string }]

D-57 XState trigger heuristic:
  const needsXState = spec.asyncOperations === true && spec.stateCount >= 3 && spec.hasConditionalTransitions === true;

XState v5 setup() pattern (Context7 /statelyai/xstate verified):
  import { setup, assign } from 'xstate';
  export const machine = setup({ types: { context: {} as {...}, events: {} as {...} } }).createMachine({ ... });

Mermaid stateDiagram-v2 emit pattern (per D-58 / RESEARCH.md §4.2):
  lines = ['stateDiagram-v2']
  lines.push('  [*] --> idle')
  for (t of transitions) lines.push(`  ${t.from} --> ${t.to} : ${t.event}`)

mermaid-render.mjs extension point (per RESEARCH.md §4.3, already at line 57+):
  const diagramType = source.trimStart().startsWith('stateDiagram-v2') ? 'stateDiagram-v2' : detectDiagramType(source);
  Extend renderMermaidFile() dispatcher to handle 'stateDiagram-v2' alongside existing flowchart.

State-name extractor for D-59(c) (regex, no full AST needed):
  const stateNames = new Set([...source.matchAll(/^\s*(\w[\w-]*)\s*$/gm), ...source.matchAll(/(\w[\w-]*)\s*-->/gm), ...source.matchAll(/-->\s*(\w[\w-]*)/gm)].flat(1).map(m => m[1]).filter(Boolean));

findingId pattern for Stage 4 (INVARIANT-06):
  4-coverage-001, 4-states-001, 4-open-transition-001

PR detector findingId pattern for Stage 3 PR: 3-pr-choice-001, 3-pr-layout-001
PR detector findingId pattern for Stage 4 PR: 4-pr-states-001, 4-pr-hax18-001

Stage 4 artifact paths (per D-58):
  design/interactions/<screen>.spec.md — spec frontmatter + states
  design/interactions/<screen>.diagram.mmd — Mermaid stateDiagram-v2 canonical
  design/interactions/<screen>.machine.ts — XState v5 (conditional)
  Kebab-case for screen names with spaces (e.g., checkout-flow.diagram.mmd)

Token budget: interact ≤30k p50 (COST-04, D-66)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-03-02-A: state-machine-emit.mjs + golden fixture + mermaid-render.mjs stateDiagram-v2 extension</name>
  <files>
    assets/scripts/state-machine-emit.mjs,
    assets/scripts/mermaid-render.mjs,
    evals/golden/state-machine-emit.golden.json,
    tests/state-machine/emit.test.ts
  </files>
  <behavior>
    - Test 1: emitMermaid(spec) with 3-state spec produces valid stateDiagram-v2 string starting with 'stateDiagram-v2' and containing all transition arrows
    - Test 2: emitXState(spec) with asyncOperations:true + stateCount:3 + hasConditionalTransitions:true produces TypeScript with 'setup(' and 'createMachine(' substrings
    - Test 3: emitFromSpec(spec) with D-57 trigger conditions TRUE returns { mermaidSource, xstateSource } (both present)
    - Test 4: emitFromSpec(spec) with asyncOperations:false returns { mermaidSource, xstateSource: null } (XState absent)
    - Test 5: emitFromSpec(spec) with stateCount:2 returns { mermaidSource, xstateSource: null } (stateCount < 3)
    - Test 6: output is byte-identical on two consecutive runs from the same spec (determinism)
    - Test 7: lint-determinism scan of state-machine-emit.mjs reports zero violations
    - Test 8: mermaid-render.mjs validateMermaidSource() with stateDiagram-v2 input does not throw (extended dispatch handles it)
    - Test 9: mermaid-render.mjs validateMermaidSource() with composite state syntax (state name { ... }) does not throw
    - Test 10: verify-golden.mjs PASS on state-machine-emit.golden.json (5× byte-identical)
  </behavior>
  <action>
    RED: Create tests/state-machine/emit.test.ts with tests 1-10. Run npx vitest run tests/state-machine/emit.test.ts — all must FAIL. Commit: test(03-02): add failing tests for state-machine-emit + mermaid-render stateDiagram-v2

    GREEN (state-machine-emit.mjs):

    Fill the Phase 1 skeleton assets/scripts/state-machine-emit.mjs with:
    - Import interaction-spec.v1.json schema from schemas/dist/ and use ajv validateArtifact() to validate input spec at entry.
    - Export function emitMermaid(spec): produces stateDiagram-v2 string from spec.states[] and spec.transitions[]. Always emits [*] --> <first-state> entry transition. Maps state type 'loading' → annotate with <<loading>>, etc. (simple comment annotation, not full note blocks).
    - Export function emitXState(spec): produces XState v5 TypeScript using the setup() pattern from Context7 /statelyai/xstate. Emits from spec.states[] and spec.transitions[]; uses assign() for DONE/ERROR transitions.
    - Export function emitFromSpec(specPath): reads .spec.md frontmatter via gray-matter, validates against interaction-spec.v1.json, applies D-57 trigger heuristic, calls emitMermaid always, calls emitXState conditionally, returns { mermaidSource, xstateSource: string|null, diagramPath, machinePath }.
    - Export CLI-facing function: emitToFiles(specPath, outputDir) writes .diagram.mmd and (if xstateSource) .machine.ts to outputDir. Validates Mermaid output via mermaid-render.mjs validateMermaidSource() — on validation failure triggers max-2-retry repair: logs the error and returns a repair prompt (the caller SKILL.md handles re-generation; the script does not call LLM).
    - canonicalize() the Mermaid output (sort transitions alphabetically) for determinism.
    - No LLM imports. Must pass lint-determinism.mjs.
    - Add CLI subcommand assets/scripts/cli/state-machine-emit.mjs wiring --spec/--output/--screen flags to emitToFiles.

    EXTEND assets/scripts/mermaid-render.mjs for stateDiagram-v2:
    - At the diagram-type dispatch (line 57+), add a case for source lines starting with 'stateDiagram-v2'.
    - The stateDiagram-v2 renderer should run the same validate→render pipeline as flowchart. Use @mermaid-js/mermaid-cli headless for rendering.
    - Add a composite state syntax test case (state name { ... }) to ensure the Pitfall B complex syntax is handled.
    - Max-2-retry repair loop: same pattern as Phase 2 flowchart repair. On Mermaid validation failure, return { valid: false, error: <validation error string> }. The caller (SKILL.md atom or state-machine-emit.mjs) handles re-generation.

    CREATE evals/golden/state-machine-emit.golden.json:
    - Input fixture: a 4-state async spec { asyncOperations:true, stateCount:4, hasConditionalTransitions:true, states:[{name:'idle',type:'custom'},{name:'loading',type:'loading'},{name:'success',type:'success'},{name:'error',type:'error'}], transitions:[{from:'idle',to:'loading',event:'SUBMIT'},{from:'loading',to:'success',event:'DONE'},{from:'loading',to:'error',event:'ERROR'},{from:'error',to:'idle',event:'RETRY'}] }.
    - Capture emitMermaid output + emitXState output, write to golden JSON: { input: specData, mermaidOutput: "...", xstateOutput: "..." }.
    - Register in verify-golden.mjs registry.

    Commit: feat(03-02): state-machine-emit.mjs + golden fixture + mermaid-render stateDiagram-v2
  </action>
  <verify>
    <automated>npx vitest run tests/state-machine/emit.test.ts && node assets/scripts/verify-golden.mjs 2>&1 | grep -E "(PASS|state-machine)" && node assets/scripts/lint-determinism.mjs assets/scripts/state-machine-emit.mjs 2>&1 | grep -E "(CLEAN|0 violation)"</automated>
  </verify>
  <done>10/10 emit tests pass. verify-golden.mjs PASS on state-machine-emit fixture. lint-determinism clean. XState emitted iff all three D-57 conditions true. Mermaid stateDiagram-v2 renders without error in mermaid-render.mjs.</done>
</task>

<task type="auto" tdd="true">
  <name>T-03-02-B: gate-stage-4.mjs full business logic + stage-3-pr.mjs + stage-4-pr.mjs</name>
  <files>
    assets/scripts/gates/stage-4.mjs,
    assets/scripts/audit/stage-3-pr.mjs,
    assets/scripts/audit/stage-4-pr.mjs,
    tests/gates/stage-4.test.ts,
    tests/audit/stage-3-pr.test.ts,
    tests/audit/stage-4-pr.test.ts,
    references/gates/stage-4.md
  </files>
  <behavior>
    Gate tests:
    - Test 1: gate returns {kind:'failed_after_repair', findings:[{findingId:'4-coverage-001'}]} when a sitemap route has no corresponding .spec.md in staged interactions/
    - Test 2: gate returns {kind:'failed_after_repair', findings:[{findingId:'4-states-001'}]} when a .spec.md exists but is missing any of loading/empty/error/success state types
    - Test 3: gate returns {kind:'failed_after_repair', findings:[{findingId:'4-open-transition-001'}]} when .diagram.mmd contains a transition to a state name not declared in the diagram
    - Test 4: gate returns {kind:'pass'} when all routes have spec files, all specs have 4 canonical state types, and no open transitions
    - Test 5: gate runs against --staged path, not design/ (INVARIANT-01)

    PR detector tests:
    - Test 6: stage-3-pr.mjs detects a new screen in sitemap.json that has no corresponding CHOICE.md in design/wireframes/ — returns finding 3-pr-choice-001
    - Test 7: stage-4-pr.mjs detects a .spec.md with asyncOperations:true but missing loading state — returns finding 4-pr-states-001
    - Test 8: stage-4-pr.mjs detects a .spec.md with transitions containing hax-18 interaction without corresponding hax-18.md reference citation — returns finding 4-pr-hax18-001
    - Test 9: both PR detectors return empty findings arrays for clean fixtures
  </behavior>
  <action>
    RED: Write tests/gates/stage-4.test.ts (tests 1-5) and tests/audit/stage-3-pr.test.ts + tests/audit/stage-4-pr.test.ts (tests 6-9). Run vitest on all three — all must FAIL. Commit: test(03-02): add failing tests for gate-stage-4 + stage-3/4-pr detectors

    GREEN (gate-stage-4.mjs):

    Replace Phase 1 skeleton with D-59 three-condition checklist:

    Condition (a) — Sitemap route coverage:
    - Read design/ia/sitemap.json from designDir. Extract all route paths.
    - Glob `stagedDir/interactions/*.spec.md` (or designDir if no --staged). Build set of screen names (stem of filename, kebab-case).
    - For each sitemap route path, derive expected screen name (last path segment, kebab-case). If no corresponding .spec.md exists, emit finding { findingId:'4-coverage-001', stage:4, severity:'BLOCKER', evidence:{ route: path }, fixRecipe:'Run ixd/state-catalog atom for screen '+screenName }.
    - Use globby 14.x for filesystem walks (RESEARCH.md §12 "Don't Hand-Roll" rule).

    Condition (b) — State completeness:
    - For each .spec.md, parse YAML frontmatter via gray-matter. Validate against interaction-spec.v1.json via ajv validateArtifact().
    - Extract states[].type array. Check that the set includes at least 'loading', 'empty', 'error', 'success'. Missing states → finding { findingId:'4-states-001', stage:4, severity:'BLOCKER', evidence:{ screen, missing:['loading','error'] }, fixRecipe:'Add missing state types to '+screen+'.spec.md' }.

    Condition (c) — No open transitions:
    - For each .diagram.mmd, extract all state names using the regex-based extractor from RESEARCH.md §4.3.
    - Extract all transition targets from --> patterns. Check each target exists in the state name set.
    - Open targets → finding { findingId:'4-open-transition-001', stage:4, severity:'BLOCKER', evidence:{ screen, openTargets:['unknownState'] }, fixRecipe:'State "unknownState" in diagram is not defined.' }.

    If no findings across all conditions: return { kind:'pass', evidence:'proto' }.
    If any finding: return { kind:'failed_after_repair', reason:'gate-conditions-unmet', findings:[...] }.

    Create references/gates/stage-4.md: 4-column GFM checklist table (Check | Required for PASS | Required for VALIDATED | Citation). Entries: sitemap-coverage, state-completeness, no-open-transitions, mermaid-present, xstate-conditional.

    stage-3-pr.mjs:
    - Export detectStage3PrIssues({ sitemapPath, wireframesDir }): reads sitemap.json, for each route checks if design/wireframes/<screen>/CHOICE.md exists. Missing → finding 3-pr-choice-001 (WARN severity).
    - Also detects significant layout drift: if a .excalidraw file in wireframesDir was modified in the PR diff (git diff --name-only), note that re-running gate-stage-3 is recommended (INFO finding 3-pr-layout-001).
    - No LLM imports. Must pass lint-determinism.mjs.

    stage-4-pr.mjs:
    - Export detectStage4PrIssues({ designDir }): globs design/interactions/*.spec.md. For each spec: if asyncOperations:true AND (states missing 'loading' or 'error') → finding 4-pr-states-001 (ERROR).
    - HAX-18 check: if spec.asyncOperations:true (AI product signals), verify that .spec.md body references 'hax-18' or 'HAX-18'. Missing citation → INFO finding 4-pr-hax18-001 with fixRecipe: "Review references/hax-18.md for AI-specific interaction guidelines (Amershi et al. CHI 2019)."
    - No LLM imports.

    Commit: feat(03-02): gate-stage-4.mjs full logic + stage-3-pr.mjs + stage-4-pr.mjs
  </action>
  <verify>
    <automated>npx vitest run tests/gates/stage-4.test.ts tests/audit/stage-3-pr.test.ts tests/audit/stage-4-pr.test.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>9/9 gate + PR detector tests pass. Full vitest suite ≥ 815 tests still passing (no regressions). tsc --noEmit clean. lint-determinism clean on stage-4.mjs, stage-3-pr.mjs, stage-4-pr.mjs.</done>
</task>

<task type="auto">
  <name>T-03-02-C: interact SKILL.md + 3 IxD atoms + 7 reference files + triggers + bundle fixture update</name>
  <files>
    skills/workflows/interact.md,
    skills/atoms/ixd/state-catalog.md,
    skills/atoms/ixd/pattern-variants.md,
    skills/atoms/ixd/state-machine.md,
    evals/triggers/interact/triggers.yaml,
    references/saffer-microinteractions.md,
    references/tidwell-patterns.md,
    references/head-motion.md,
    references/hax-18.md,
    references/xstate-v5.md,
    references/apg.md,
    references/material-3.md,
    evals/bundles/fixtures/stage-3-to-4/upstream/interactions.placeholder,
    evals/bundles/fixtures/stage-3-to-4/bundle.md
  </files>
  <action>
    Create skills/workflows/interact.md — W4 workflow SKILL.md (per WF-05, D-57, D-58, D-59):
    - Frontmatter: name: design-os/interact, description ≤200 chars (e.g., "Stage 4: Enumerate loading/empty/error/success states per screen, map interaction patterns (Tidwell/APG), emit Mermaid stateDiagram-v2 + conditional XState v5."), type: workflow, stage: 4, compatibility: [claude-code, codex-cli, cursor].
    - Procedure (numbered steps):
      1. Read Stage 3 handoff bundle from .design-os/preview/<run-id>/stage-3-bundle.md.
      2. For each screen in sitemap.json, run ixd/state-catalog atom.
      3. Run ixd/pattern-variants atom to map Tidwell/APG patterns.
      4. For each screen, run: `node bin/design-os.mjs state-machine-emit --spec .design-os/preview/<run-id>/interactions/<screen>.spec.md --output .design-os/preview/<run-id>/interactions/ --screen <screen>` — emits .diagram.mmd always, .machine.ts conditionally.
      5. Validate Mermaid output; on failure run max-2-retry repair loop (request corrected diagram from LLM with validation error as context). Re-run state-machine-emit.
      6. Run gate: `node bin/design-os.mjs gate --stage 4 --staged .design-os/preview/<run-id>/`.
      7. Build Stage 4→5a handoff bundle: `node bin/design-os.mjs handoff-bundle --from 4 --to 5a --staged .design-os/preview/<run-id>/`.
      8. Surface diff; await --apply.
    - FID-04 section: "Do NOT produce hi-fi interaction specs. No color, no typography, no shadow. Mermaid stateDiagram-v2 is the designer-readable canonical. XState is the dev artifact — emitted only for async + ≥3 states + conditional transitions."
    - Budget: ≤30k tokens p50 (COST-04, D-66).
    - References: @references/saffer-microinteractions.md, @references/tidwell-patterns.md, @references/hax-18.md, @references/xstate-v5.md, @references/apg.md.
    - INVARIANTS: INVARIANT-01 (staged path), INVARIANT-02 (--apply required), INVARIANT-05 (no LLM in scripts).

    Create skills/atoms/ixd/state-catalog.md — ATOM-12:
    - Frontmatter: name: design-os/ixd/state-catalog, description ≤200 chars, type: atom, stage: 4.
    - Procedure: for a given screen, enumerate all possible UI states. Required: loading, empty, error, success. Optional: add screen-specific states (e.g., 'offline', 'permission-denied', 'rate-limited'). Populate interaction-spec.v1.json frontmatter: asyncOperations, stateCount, hasConditionalTransitions. Emit as <screen>.spec.md.
    - Guidance on asyncOperations: "Only set asyncOperations:true if the screen fetches data, submits a form, or performs an async network operation. Static screens should be asyncOperations:false."
    - HAX-18 note: if asyncOperations:true, check references/hax-18.md for AI-product interaction guidelines (Amershi et al. 2019 CHI). Cite relevant guidelines in spec body.

    Create skills/atoms/ixd/pattern-variants.md — ATOM-11:
    - Frontmatter: name: design-os/ixd/pattern-variants, description ≤200 chars, type: atom, stage: 4.
    - Procedure: for each screen, identify 3 candidate interaction patterns from: Tidwell (Designing Interfaces 3e), W3C APG (Aria Patterns Guide), Material Design 3. For each pattern: name, pattern source, tradeoffs table (pros/cons), accessibility note from APG. Output: <screen>-patterns.md.

    Create skills/atoms/ixd/state-machine.md — ATOM-10:
    - Frontmatter: name: design-os/ixd/state-machine, description ≤200 chars, type: atom, stage: 4.
    - Procedure: given <screen>.spec.md, invoke state-machine-emit: `node bin/design-os.mjs state-machine-emit --spec .design-os/preview/<run-id>/interactions/<screen>.spec.md --output .design-os/preview/<run-id>/interactions/`. Check for repair-needed signal in output; if validation failed, correct the state transitions and re-run (max 2 retries). Confirm .diagram.mmd and (if triggered) .machine.ts in staged output.

    Create evals/triggers/interact/triggers.yaml:
    - Skill: design-os/interact
    - shouldFire (≥12): "map interaction states for the dashboard", "enumerate loading and error states for checkout", "create state machine for the auth flow", "run stage 4 IxD on search results screen", "build state diagram for notification system", "add interaction design to profile screen", "create state catalog for onboarding wizard", "map loading/empty/error/success states", "generate XState machine for the data table", "run interact workflow on admin panel", "enumerate UI states for the upload feature", "IxD state catalog for settings page"
    - shouldNotFire (≥12): "create wireframe sketches", "generate token colors", "audit slop tells", "build react component", "run stage 3 on admin screen", "deploy the app", "create a persona", "map the sitemap", "generate DESIGN.md", "systematize design system", "run style workflow", "reverse engineer the prototype"

    Create 7 reference files (Markdown, stage: 4, substantive summaries with citations):
    - references/saffer-microinteractions.md: Dan Saffer "Microinteractions" — 4-part anatomy: trigger, rules, feedback, loops+modes. Key principle: details matter more than features. Cite: Saffer (2013) O'Reilly.
    - references/tidwell-patterns.md: Jenifer Tidwell "Designing Interfaces 3e" — key patterns: list builder, card stack, fill-in-the-blanks, responsive disclosure, progress indicators, inline validation. Cite: Tidwell, Brewer, Valencia (2020) O'Reilly.
    - references/head-motion.md: Head et al. "Motion Design Guidelines for Apps" — purpose-driven animation, timing functions, reduced-motion respect. Cite: Material Motion spec + prefers-reduced-motion media query (WCAG 2.3 SC 2.3.3).
    - references/hax-18.md: Amershi et al. "Software Engineering for Machine Learning: A Case Study" + HAX Workbook — 18 guidelines for human-AI interaction. Key: G1 (make clear it's AI), G7 (support efficient invocation), G16 (convey AI limitations). Cite: Amershi et al., CHI 2019. Stage: 4.
    - references/xstate-v5.md: XState v5 setup() pattern, actor model, assign actions. Key change from v4: setup() replaces createMachine() directly. Conditional transitions via guards. Cite: XState docs v5 (statelyai.github.io), Context7 /statelyai/xstate. Stage: 4.
    - references/apg.md: W3C Aria Patterns Guide (APG) — design patterns for accessible widgets (combobox, listbox, menu, dialog, tabs). Each pattern has ARIA roles, keyboard interactions, focus management rules. Cite: w3.org/WAI/ARIA/apg/. Stage: 4.
    - references/material-3.md: Material Design 3 — interaction states model (default, hovered, focused, pressed, dragged, disabled), dynamic color, motion tokens. Cite: m3.material.io. Stage: 4.

    Update evals/bundles/fixtures/stage-3-to-4/:
    - Replace Phase 1 placeholder interactions.placeholder with a realistic Stage 3→4 handoff bundle content referencing a screen name, CHOICE.md selection, and skeleton-ir output.
    - Recompute sourceHash in bundle.md. Validate bundle.md against handoff-bundle.v1.json schema.

    Verify all SKILL.md descriptions ≤200 chars (INVARIANT-04):
    interact.md, state-catalog.md, pattern-variants.md, state-machine.md
  </action>
  <verify>
    <automated>node -e "const {readFileSync}=require('fs');const m=require('gray-matter');['skills/workflows/interact.md','skills/atoms/ixd/state-catalog.md','skills/atoms/ixd/pattern-variants.md','skills/atoms/ixd/state-machine.md'].forEach(p=>{const d=m(readFileSync(p,'utf8')).data;if(d.description.length>200)throw new Error(p+' desc too long: '+d.description.length)}); console.log('PASS')" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>interact.md + 3 IxD atoms exist with valid frontmatter and ≤200 char descriptions. 7 reference files exist with substantive content and citations. triggers.yaml has ≥12 shouldFire + ≥12 shouldNotFire prompts. Bundle fixture updated. Full vitest suite ≥ 815 tests passing (no regressions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interaction-spec.v1.json → state-machine-emit | LLM-produced spec JSON fed to deterministic emitter. Malformed spec could cause emit errors. |
| .diagram.mmd → gate D-59(c) | Gate parses Mermaid text using regex — overly broad regex could false-positive on comment text. |
| --spec path flag | CLI accepts user-provided path. Must validate file exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | state-machine-emit.mjs --spec path | mitigate | Validate --spec path exists and ends in .spec.md. Reject paths with `..`. |
| T-03-02-02 | Tampering | gate-stage-4.mjs regex state-name extractor | mitigate | Regex matches `\w[\w-]*` identifiers only. Test against Mermaid comment syntax to prevent false positives. |
| T-03-02-03 | Repudiation | XState .machine.ts emitted without D-57 trigger | accept | D-57 heuristic is deterministic from spec frontmatter. If frontmatter is wrong, re-run state-catalog atom. |
| T-03-02-04 | Denial of Service | Mermaid validation with cyclic state diagram | accept | Design-time tool; no server. Mermaid CLI headless handles gracefully with error output. |
</threat_model>

<verification>
After all 3 tasks complete:

1. npx vitest run — must show ≥ 815 + new Stage 4 tests, all passing
2. npx tsc --noEmit — zero errors
3. node assets/scripts/lint-determinism.mjs assets/scripts/state-machine-emit.mjs assets/scripts/gates/stage-4.mjs assets/scripts/audit/stage-3-pr.mjs assets/scripts/audit/stage-4-pr.mjs — CLEAN
4. node assets/scripts/verify-golden.mjs — PASS (state-machine-emit golden fixture byte-identical)
5. XState emit conditional test: spec with asyncOperations:false → xstateSource is null
6. All 4 SKILL.md descriptions ≤200 chars
7. Zero file overlap with 03-01 (can be confirmed by diffing files_modified arrays — no shared files)
</verification>

<success_criteria>
- state-machine-emit.mjs emits Mermaid always, XState only when all 3 D-57 conditions true — golden test verifies 5×
- mermaid-render.mjs handles stateDiagram-v2 without error; repair loop returns structured error for caller to handle
- gate-stage-4.mjs: all three D-59 conditions covered and unit tested
- stage-3-pr.mjs and stage-4-pr.mjs return structured findingIds compliant with INVARIANT-06
- interact.md + 3 IxD atom SKILL.md files exist with proper frontmatter and ≤200 char descriptions
- 7 reference files with substantive content and proper citations
- All prior 815 tests still pass (zero regressions)
- 03-01 can run concurrently (confirmed zero file overlap)
</success_criteria>

<output>
After completion, create .planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-02-SUMMARY.md with:
- phase: 03-v2-0b-full-5-stages-lovable-refugee-path / plan: 02 / subsystem: stage-4-interact
- dependency_graph.provides: state-machine-emit.mjs, extended mermaid-render.mjs, gate-stage-4.mjs (full), stage-3-pr.mjs, stage-4-pr.mjs, interact.md, 3 IxD atoms, 7 references
- Record the Pitfall B (stateDiagram-v2 composite state) test result
- Record if Mermaid validation extension required any adjustments from RESEARCH.md §4.3 plan
</output>
