---
phase: 03-v2-0b-full-5-stages-lovable-refugee-path
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - assets/scripts/routing/dispatch.mjs
  - assets/scripts/cli/audit.mjs
  - skills/design/SKILL.md
  - evals/fixtures/budget/mature-app-refactor.fixture.json
  - evals/fixtures/budget/ds-extraction.fixture.json
  - evals/fixtures/budget/audit-all-stages.fixture.json
  - tests/routing/dispatch-real-stages.test.ts
  - tests/audit/all-stages.test.ts
  - evals/triggers/sketch/triggers.yaml
  - evals/triggers/interact/triggers.yaml
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-03
  - MVPB-07
  - ROUTE-06
  - AUDIT-02
  - AUDIT-04
  - MVPB-09
  - COST-03
  - COST-04

must_haves:
  truths:
    - "dispatch.mjs routes new-product, mature-app-refactor, DS-extraction promoted from route_not_yet_implemented to real dispatch"
    - "new-product route dispatches all 7 stages in sequence with independent per-stage token budgets per D-66"
    - "mature-app-refactor route dispatches exactly: audit --stage 2 --pr, audit --stage 4 --pr, systematize — skips stages 1, 3, 5a (D-67)"
    - "DS-extraction route invokes audit --reverse-engineer-stages then backfills stages 1, 2, 4, 5b — budget ≤120k (OQ-3)"
    - "audit --all-stages produces unified AUDIT-REPORT.md sorted by (severity DESC, stage ASC) per D-68"
    - "audit --new-feature --feature <name> is a scoped post-hoc validator (D-69), does not generate new artifacts"
    - "Phase 2 routes new-feature, design-bug, brand-refresh, PR-audit still pass dispatch regression test unchanged"
    - "skills/design/SKILL.md updated with new route branches and token budget hints per D-66"
    - "evals/triggers/sketch/triggers.yaml and evals/triggers/interact/triggers.yaml tuned to recall ≥0.85"
    - "All 5 ROADMAP Phase 3 success criteria verifiable against this plan's deliverables"
  artifacts:
    - path: "assets/scripts/routing/dispatch.mjs"
      provides: "3 promoted routes: new-product, mature-app-refactor, DS-extraction"
      contains: "new-product"
    - path: "assets/scripts/cli/audit.mjs"
      provides: "Extended with --all-stages (sorted findings) and --new-feature --feature <name>"
      contains: "all-stages"
    - path: "skills/design/SKILL.md"
      provides: "Updated route branches + token budget hints per D-66"
      contains: "new-product"
    - path: "tests/routing/dispatch-real-stages.test.ts"
      provides: "Extended to cover 3 new routes without breaking 4 Phase 2 routes"
      contains: "new-product"
    - path: "tests/audit/all-stages.test.ts"
      provides: "Unit tests for --all-stages sort algorithm (D-68)"
      contains: "severity DESC"
  key_links:
    - from: "assets/scripts/routing/dispatch.mjs"
      to: "assets/scripts/run-subagent.mjs"
      via: "dispatchSubagent([{ workflow, args, budget }]) — same pattern as Phase 2 routes"
      pattern: "dispatchSubagent"
    - from: "assets/scripts/cli/audit.mjs"
      to: "per-stage detectors (stage-1..stage-5b-pr.mjs)"
      via: "runAudit({ allStages: true }) calls each detector in sequence"
      pattern: "allStages"
    - from: "assets/scripts/cli/audit.mjs"
      to: "design/ia/sitemap.json"
      via: "findSitemapNode(sitemapJson, featureName) for --new-feature scoping"
      pattern: "findSitemapNode"
---

<objective>
Complete Phase 3 by wiring all three remaining routes in `dispatch.mjs`, adding `audit --all-stages` unified reporting and `audit --new-feature` post-hoc validation to `audit.mjs`, updating `skills/design/SKILL.md` with the Phase 3 routing matrix, and tuning triggers for the new Stage 3+4 workflows. This plan must leave all 5 ROADMAP Phase 3 success criteria verifiable.

Purpose: Route completion is the final gate for Phase 3 end-to-end functionality. The Phase 2 regression suite must pass unchanged — `dispatch.mjs` extension must not alter the behavior of `new-feature`, `design-bug`, `brand-refresh`, `PR-audit` routes. `audit --all-stages` (D-68) is the ROADMAP SC-5 deliverable.

Output: Extended dispatch.mjs (3 new routes), extended audit.mjs (--all-stages + --new-feature), updated SKILL.md, 3 budget fixtures, updated trigger files, extended dispatch test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-CONTEXT.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-RESEARCH.md
@.planning/PROJECT.md
@skills/workflows/INVARIANTS.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-05-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-01-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-02-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-03-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do not explore codebase — use these directly. -->

From assets/scripts/routing/dispatch.mjs (Phase 2, extend — do not rewrite):
  Phase 2 wires: new-feature, design-bug, brand-refresh, PR-audit (4 routes dispatched via dispatchSubagent).
  Phase 2 stubs: new-product, mature-app-refactor, DS-extraction return { kind:'route_not_yet_implemented' }.
  Phase 3: replace the 3 stubs with real dispatch. Do NOT modify the 4 existing real routes.

dispatch.mjs new-product handler (D-66):
  case 'new-product':
    return dispatchSubagent([
      { workflow:'ingest',      args:'',        budget: 5_000  },
      { workflow:'discover',    args:'',        budget: 30_000 },
      { workflow:'structure',   args:'',        budget: 25_000 },
      { workflow:'sketch',      args:'',        budget: 25_000 },
      { workflow:'interact',    args:'',        budget: 30_000 },
      { workflow:'style',       args:'',        budget: 25_000 },
      { workflow:'systematize', args:'',        budget: 10_000 },
    ]);

dispatch.mjs mature-app-refactor handler (D-67):
  case 'mature-app-refactor':
    return dispatchSubagent([
      { workflow:'audit', args:'--stage 2 --pr', budget: 15_000 },
      { workflow:'audit', args:'--stage 4 --pr', budget: 15_000 },
      { workflow:'systematize', args:'',          budget: 15_000 },
    ]);

dispatch.mjs DS-extraction handler (ROUTE-06, OQ-3 resolved at 120k):
  case 'DS-extraction':
    return dispatchSubagent([
      { workflow:'audit', args:'--reverse-engineer-stages', budget: 60_000 },
      { workflow:'discover',    args:'--from-inferred',     budget: 15_000 },
      { workflow:'structure',   args:'--from-inferred',     budget: 15_000 },
      { workflow:'interact',    args:'--from-inferred',     budget: 15_000 },
      { workflow:'systematize', args:'--from-inferred',     budget: 15_000 },
    ]);  // Total: 120k (OQ-3 resolution)

From assets/scripts/cli/audit.mjs (Phase 2, extend — do not rewrite):
  Phase 2: runAudit({ slopTells, pr, scanDir, designDir, ... })
  Phase 3 adds: allStages flag + newFeature + featureName options.

audit.mjs --all-stages sort key (D-68):
  const SEVERITY_RANK = { BLOCKER: 4, ERROR: 3, WARN: 2, INFO: 1 };
  findings.sort((a, b) => {
    const sRank = SEVERITY_RANK[b.severity] - SEVERITY_RANK[a.severity];
    if (sRank !== 0) return sRank;
    return a.stage - b.stage;  // earlier stage first within same severity tier
  });

audit.mjs --new-feature (D-69):
  Read design/ia/sitemap.json, find the route node matching featureName (by path or label).
  Scope designDir to that node's subtree.
  Call runAuditAllStages({ designDir: scopedDir }) — same as --all-stages but scoped.
  Output: AUDIT-REPORT.md with title "Feature audit: <featureName>".

SKILL.md design description update (D-66 route table):
  Existing: skills/design/SKILL.md (Phase 2 v2.0a routes table)
  Phase 3: add routes new-product (all 5 stages, ≤150k), mature-app-refactor (stages 2+4+5b, ≤45k), DS-extraction (reverse-engineer + backfill, ≤120k)

Phase 2 route regression test (existing tests/routing/dispatch-real-stages.test.ts):
  Must still pass for: new-feature, design-bug, brand-refresh, PR-audit.
  Add 3 new test cases for: new-product, mature-app-refactor, DS-extraction.

D-68 sort algorithm constants (RESEARCH.md §8.4):
  BLOCKER=4, ERROR=3, WARNING=2, INFO=1 (note: severity enum in schema uses BLOCKER/WARN/INFO; map WARNING→WARN for schema compliance)
  Stage numbers: 1, 2, 3, 4, '5a', '5b' — numeric comparison with '5a'→5.1, '5b'→5.2 for ordering

trigger recall target: ≥0.85 recall, ≤0.15 false-trigger (D-32 calibration pattern from Phase 2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-03-05-A: dispatch.mjs route promotion (3 routes) + dispatch regression test extension</name>
  <files>
    assets/scripts/routing/dispatch.mjs,
    tests/routing/dispatch-real-stages.test.ts,
    evals/fixtures/budget/mature-app-refactor.fixture.json,
    evals/fixtures/budget/ds-extraction.fixture.json
  </files>
  <behavior>
    - Test 1: dispatch('new-product', opts) returns a result with kind !== 'route_not_yet_implemented'
    - Test 2: dispatch('new-product', opts) calls dispatchSubagent with exactly 7 stages in order: ingest, discover, structure, sketch, interact, style, systematize
    - Test 3: dispatch('new-product', opts) passes correct budget per stage (ingest:5000, discover:30000, structure:25000, sketch:25000, interact:30000, style:25000, systematize:10000)
    - Test 4: dispatch('mature-app-refactor', opts) calls dispatchSubagent with 3 stages: audit--stage-2-pr, audit--stage-4-pr, systematize
    - Test 5: dispatch('DS-extraction', opts) calls dispatchSubagent with 5 stages, first is 'audit --reverse-engineer-stages', total budget = 120000
    - Test 6: dispatch('new-feature', opts) still passes unchanged (Phase 2 regression)
    - Test 7: dispatch('design-bug', opts) still passes unchanged (Phase 2 regression)
    - Test 8: dispatch('brand-refresh', opts) still passes unchanged (Phase 2 regression)
    - Test 9: dispatch('PR-audit', opts) still passes unchanged (Phase 2 regression)
    - Test 10: budget fixtures sum correctly: mature-app-refactor sums to 45000, ds-extraction sums to 120000
  </behavior>
  <action>
    RED: Update tests/routing/dispatch-real-stages.test.ts with tests 1-10 (tests 6-9 already exist — keep them; add tests 1-5 and 10). Run npx vitest run tests/routing/dispatch-real-stages.test.ts — tests 1-5 must FAIL (routes still return route_not_yet_implemented). Tests 6-9 must PASS. Commit: test(03-05): add failing dispatch tests for 3 new Phase 3 routes

    GREEN (dispatch.mjs extension):

    In assets/scripts/routing/dispatch.mjs, find the three 'route_not_yet_implemented' stubs and replace with:

    1. 'new-product' handler: per D-66 interface above (7-stage sequence with independent per-stage budgets).
    2. 'mature-app-refactor' handler: per D-67 (3-stage: audit stage-2 pr, audit stage-4 pr, systematize).
    3. 'DS-extraction' handler: per ROUTE-06 + OQ-3 (5-stage: reverse-engineer + 4 backfill stages, total 120k).

    CRITICAL: Do NOT modify the switch/case handlers for new-feature, design-bug, brand-refresh, PR-audit. Do NOT modify detectStack() or any shared utility code. Only replace the three stub return values.

    Note (MVPB-07): the dispatch routing for Stage 3 (sketch) and Stage 4 (interact) — specifically that routes correctly sequence these stages without falling back to Phase 2 behavior — is satisfied here by the new-product and mature-app-refactor route handlers above. The mature-app-refactor handler explicitly skips Stage 3 per D-67 (dispatches audit--stage-4-pr, not sketch/interact workflows directly). This satisfies MVPB-07's requirement that routing-level dispatch correctly selects which stages run per route.

    Also update dispatch.mjs to pass tokenBudget in handoff bundle preamble:
    - The dispatchSubagent call receives budget per stage. Each sub-agent receives a `tokenBudget: N` hint in the handoff bundle preamble per D-66: "Stages that complete under budget do NOT donate headroom to later stages — each ceiling is independent."
    - Pass this hint through the existing dispatchSubagent interface if it supports a budget field; otherwise add a new field. Check existing dispatchSubagent signature before modifying.

    Create evals/fixtures/budget/mature-app-refactor.fixture.json:
    {
      "route": "mature-app-refactor",
      "description": "Stage 2 audit + Stage 4 audit + Stage 5b systematize. Skips stages 1, 3, 5a.",
      "budgetTokens": 45000,
      "stageAllocations": { "audit-stage-2-pr": 15000, "audit-stage-4-pr": 15000, "systematize": 15000 },
      "notes": "D-67: mature-app-refactor route = Stage 2 audit + Stage 4 audit + Stage 5b systematize. ≤45k total. Source: CONTEXT.md D-67, ROADMAP SC-5."
    }

    Create evals/fixtures/budget/ds-extraction.fixture.json:
    {
      "route": "DS-extraction",
      "description": "Lovable refugee: reverse-engineer-stages + backfill stages 2, 4, 5b from inferred artifacts.",
      "budgetTokens": 120000,
      "stageAllocations": { "reverse-engineer-stages": 60000, "discover-from-inferred": 15000, "structure-from-inferred": 15000, "interact-from-inferred": 15000, "systematize-from-inferred": 15000 },
      "notes": "OQ-3 resolved: 120k (not 60k). The 60k figure in CONTEXT.md domain boundary referred only to Stage 5b sub-step. ROUTE-06 full route = 120k. Source: CONTEXT.md D-62, RESEARCH.md OQ-3."
    }

    Commit: feat(03-05): promote 3 dispatch routes (new-product, mature-app-refactor, DS-extraction)
  </action>
  <verify>
    <automated>npx vitest run tests/routing/dispatch-real-stages.test.ts && node -e "['evals/fixtures/budget/mature-app-refactor.fixture.json','evals/fixtures/budget/ds-extraction.fixture.json'].forEach(p=>{const d=JSON.parse(require('fs').readFileSync(p,'utf8'));const sum=Object.values(d.stageAllocations).reduce((a,b)=>a+b,0);if(sum!==d.budgetTokens)throw new Error(p+' allocations sum '+sum+' != budget '+d.budgetTokens);console.log('PASS',p,sum)})" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>10/10 dispatch tests pass (3 new routes + 4 Phase 2 regression routes). Budget fixtures: mature-app-refactor sums to 45000, ds-extraction sums to 120000. Full vitest suite green. tsc --noEmit clean.</done>
</task>

<task type="auto" tdd="true">
  <name>T-03-05-B: audit.mjs --all-stages + --new-feature + audit-all-stages budget fixture</name>
  <files>
    assets/scripts/cli/audit.mjs,
    tests/audit/all-stages.test.ts,
    evals/fixtures/budget/audit-all-stages.fixture.json
  </files>
  <behavior>
    - Test 1: runAudit({ allStages: true, designDir }) calls each per-stage detector (stage-1-pr, stage-2-pr, stage-3-pr, stage-4-pr, stage-5a-pr, stage-5b-pr) and collects all findings
    - Test 2: findings are sorted by SEVERITY_RANK DESC then stage ASC — BLOCKER stage-2 ranks before BLOCKER stage-4; BLOCKER stage-2 ranks before ERROR stage-1
    - Test 3: stage numeric ordering: stage 1 < 2 < 3 < 4 < 5a < 5b (5a→5.1, 5b→5.2 for comparison)
    - Test 4: output AUDIT-REPORT.md validates against audit-report.v1.json schema
    - Test 5: runAudit({ newFeature: 'checkout', featureName: 'checkout', designDir }) scopes to the 'checkout' sitemap subtree and does NOT generate new artifacts
    - Test 6: audit --new-feature on a feature not in sitemap → error, not empty report
    - Test 7: audit --all-stages on a fixture with no findings produces findings:[] in AUDIT-REPORT.md (not findings: null — Phase 2 bug regression guard)
  </behavior>
  <action>
    RED: Write tests/audit/all-stages.test.ts with tests 1-7. Run vitest — all must FAIL (--allStages and --newFeature options not yet added). Commit: test(03-05): add failing all-stages and new-feature audit tests

    GREEN (extend assets/scripts/cli/audit.mjs):

    Add to Commander audit command options:
    - --all-stages: runs all per-stage detectors sequentially.
    - --new-feature and --feature <name>: scoped post-hoc validator.

    Implement runAuditAllStages({ designDir, featureName? }):
    - Run each per-stage detector in order: stage-1-pr.mjs, stage-2-pr.mjs (from Phase 2), stage-3-pr.mjs (from 03-02), stage-4-pr.mjs (from 03-02), stage-5a-pr.mjs (from Phase 2), stage-5b-pr.mjs (from Phase 2).
    - If featureName is provided, scope designDir to the sitemap subtree: read design/ia/sitemap.json, find route node by path or label matching featureName. Create a scoped temp dir with only the relevant artifacts. If no node found → throw Error('Feature "'+featureName+'" not found in sitemap').
    - Collect all findings into a single array.
    - Sort by D-68 algorithm: severity BLOCKER=4, ERROR=3, WARN=2, INFO=1 DESC; then stage numeric ASC. For stage string '5a' use 5.1, '5b' use 5.2 for comparison.
    - Emit unified AUDIT-REPORT.md with auditType: 'all-stages' (or 'feature-audit' for --new-feature). Use existing buildAuditReport() from Phase 2.
    - findings:[] (not null) for clean runs — use the existing normalizeSeverity() and findings:[] guard from Phase 2 codex-review fix pass.

    Wire into the existing runAudit() function: when opts.allStages is truthy, call runAuditAllStages. When opts.newFeature is truthy, call runAuditAllStages({ designDir, featureName: opts.feature }).

    Create evals/fixtures/budget/audit-all-stages.fixture.json:
    {
      "route": "audit-all-stages",
      "description": "Unified per-stage audit across all 6 stage detectors",
      "budgetTokens": 30000,
      "stageAllocations": { "stage-1-pr": 5000, "stage-2-pr": 5000, "stage-3-pr": 5000, "stage-4-pr": 5000, "stage-5a-pr": 5000, "stage-5b-pr": 5000 },
      "notes": "audit --all-stages runs 6 detectors sequentially. Budget is an estimate — this is a local filesystem scan with no LLM calls; actual cost is near-zero. Included for completeness. AUDIT-02."
    }

    Commit: feat(03-05): audit --all-stages + --new-feature + audit-all-stages budget fixture
  </action>
  <verify>
    <automated>npx vitest run tests/audit/all-stages.test.ts && node assets/scripts/cli/audit.mjs --help 2>&1 | grep -E "(all-stages|new-feature)" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>7/7 all-stages + new-feature tests pass. CLI --help shows --all-stages and --new-feature options. AUDIT-REPORT.md validates against schema with findings:[] for clean fixture. Sort: BLOCKER stage-2 before BLOCKER stage-4 before ERROR stage-1. Full vitest suite green.</done>
</task>

<task type="auto">
  <name>T-03-05-C: skills/design/SKILL.md update + trigger tuning + phase 3 end-to-end verification</name>
  <files>
    skills/design/SKILL.md,
    evals/triggers/sketch/triggers.yaml,
    evals/triggers/interact/triggers.yaml
  </files>
  <action>
    UPDATE skills/design/SKILL.md (Phase 2 v2.0a file — extend, do not replace existing routes):
    - Keep all existing Phase 2 route documentation (new-feature, design-bug, brand-refresh, PR-audit).
    - Add Phase 3 routes section:
      ### Phase 3 routes (v2.0b)
      | Route | Stages run | Budget p50 | Use case |
      |-------|-----------|-----------|---------|
      | new-product | ingest→discover→structure→sketch→interact→style→systematize | ≤150k | Fresh PRD, full 5-stage design |
      | mature-app-refactor | audit stage-2 + audit stage-4 + systematize | ≤45k | Existing product, fill IA + IxD gaps + extract DS |
      | DS-extraction | audit --reverse-engineer-stages + backfill | ≤120k | Lovable/v0/Bolt refugee with prototype |
    - Add per-stage budget table (D-66): ingest ≤5k, discover ≤30k, structure ≤25k, sketch ≤25k, interact ≤30k, style ≤25k, systematize ≤10k.
    - Add note: "Per-stage ceilings are independent — stages do NOT donate unused budget to later stages. The 2× soft-stop from Phase 2 (run-subagent.mjs) is preserved."
    - Verify SKILL.md description ≤200 chars (INVARIANT-04).

    TUNE evals/triggers/sketch/triggers.yaml (created by 03-01, now tune for ≥0.85 recall):
    - Review existing shouldFire/shouldNotFire prompts from 03-01.
    - Apply D-32 calibration: front-load trigger phrases in shouldFire descriptions.
    - Add any prompts that would increase recall if recall < 0.85 based on static-analysis fallback.
    - Ensure shouldFire prompts clearly mention: 'wireframe', 'sketch', 'low-fi', 'crazy eights', 'excalidraw', 'stage 3'.
    - Ensure shouldNotFire prompts do NOT mention any of the above.

    TUNE evals/triggers/interact/triggers.yaml (created by 03-02, now tune for ≥0.85 recall):
    - Same calibration approach.
    - Ensure shouldFire prompts mention: 'state machine', 'interaction design', 'state catalog', 'IxD', 'stage 4', 'loading state', 'XState'.
    - Ensure shouldNotFire prompts do NOT trigger on: 'wireframe', 'tokens', 'systematize', 'audit'.

    PHASE 3 END-TO-END VERIFICATION (final check before SUMMARY):
    Run the following verification battery to confirm ROADMAP Phase 3 success criteria are met:

    SC-1 (Stage 5a PASS): node bin/complete-design.mjs gate --stage 5a --design-dir <fixture-with-interactions> — must return kind:'pass' or kind:'pass_with_warnings', NOT not_runnable.

    SC-2 (INFERRED artifacts): node bin/complete-design.mjs audit --reverse-engineer-stages --source evals/fixtures/e2e/next15-tailwind4-shadcn/ — must create design/inferred/ artifacts with INFERRED banner in each.

    SC-3 (Gate rejections): npx vitest run evals/adversarial/fid-03-styled-wireframe/ evals/adversarial/fid-06-frost-recurrence/ — both suites pass.

    SC-4 (Migration): node bin/complete-design.mjs migrate --from 2.0a --to 2.0b --design-dir evals/fixtures/migration/v2.0a-to-v2.0b/ — dry-run shows diff with wireframeRefs and interactionNeeds additions.

    SC-5 (audit --all-stages): node bin/complete-design.mjs audit --all-stages --design-dir <fixture-with-stage-2-and-4-gaps> — outputs ranked AUDIT-REPORT.md with stage-2 and stage-4 findings.

    Record all 5 SC results in the 03-05-SUMMARY.md.
  </action>
  <verify>
    <automated>node -e "const {readFileSync}=require('fs');const m=require('gray-matter');const p='skills/design/SKILL.md';const d=m(readFileSync(p,'utf8')).data;if(d.description&&d.description.length>200)throw new Error('desc too long: '+d.description.length);console.log('PASS design SKILL.md')" && npx vitest run evals/runners/skillgrade.mjs 2>&1 | grep -E "(PASS|recall)" || npx vitest run tests/eval/phase2-skillgrade.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>skills/design/SKILL.md updated with Phase 3 routes and budget table. trigger files tuned (static-analysis recall check passes). Phase 3 SC-1..SC-5 all verified as described. Full vitest suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch.mjs route handler | User-controlled --route flag selects handler. Invalid route already handled by Phase 2 fallback. |
| audit --new-feature --feature | User-controlled feature name used to index sitemap. Injection via sitemap.json not possible (JSON parse, not eval). |
| AUDIT-REPORT.md emit | Findings from multiple detectors merged into one report. Cross-contamination of findings between stages must not occur. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | dispatch.mjs new route handler | accept | Routes are hardcoded switch cases; no dynamic route loading. User cannot inject new routes via CLI. |
| T-03-05-02 | Repudiation | audit --all-stages sorting | accept | Sort is deterministic (SEVERITY_RANK map + stage numeric). Golden test covers this. |
| T-03-05-03 | Tampering | --new-feature feature name used as file path | mitigate | findSitemapNode() matches by route label/path string; does NOT use feature name as a filesystem path component. |
| T-03-05-04 | Information Disclosure | audit --all-stages reveals all design artifacts | accept | complete-design is a local tool; user owns all design/ artifacts. No network exposure. |
</threat_model>

<verification>
After all 3 tasks complete:

1. npx vitest run — all tests pass (815 baseline + all Wave 1, 2, 3 additions)
2. npx tsc --noEmit — zero errors
3. node assets/scripts/lint-determinism.mjs assets/scripts/routing/dispatch.mjs assets/scripts/cli/audit.mjs — CLEAN
4. Phase 2 route regression: npx vitest run tests/routing/dispatch-real-stages.test.ts — tests 6-9 (Phase 2 routes) still pass
5. D-68 sort: BLOCKER stage-2 appears before BLOCKER stage-4 before ERROR stage-1 in --all-stages output
6. skills/design/SKILL.md description ≤200 chars
7. All 5 ROADMAP Phase 3 success criteria verified as documented in T-03-05-C
</verification>

<success_criteria>
- dispatch.mjs: 3 new routes promoted; 4 Phase 2 routes unmodified; dispatch regression test 10/10
- audit.mjs: --all-stages sorts by (severity DESC, stage ASC); --new-feature scopes to feature subtree; no new-artifacts generated
- skills/design/SKILL.md updated with Phase 3 route table + per-stage budget hints per D-66
- Budget fixtures: mature-app-refactor=45k, ds-extraction=120k, audit-all-stages budget fixture present
- trigger files tuned: static-analysis recall check passes for sketch + interact
- ROADMAP Phase 3 SC-1..SC-5 all verified (documented in SUMMARY.md)
- All prior tests passing (zero regressions from 815 baseline + all prior plans in this phase)
</success_criteria>

<output>
After completion, create .planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-05-SUMMARY.md with:
- phase: 03-v2-0b-full-5-stages-lovable-refugee-path / plan: 05 / subsystem: route-completion + audit
- dependency_graph.provides: 3 promoted dispatch routes, audit --all-stages, audit --new-feature, updated design SKILL.md, 3 budget fixtures, tuned triggers
- Record OQ-3 (DS-extraction budget = 120k) confirmed in fixture
- Record ROADMAP SC-1..SC-5 verification results (pass/fail with evidence)
- Record final Phase 3 test count (total tests after this plan)
- Record any Phase 2 regression issues found and fixed during T-03-05-A
</output>
