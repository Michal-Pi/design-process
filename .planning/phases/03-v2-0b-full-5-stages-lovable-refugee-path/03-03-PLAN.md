---
phase: 03-v2-0b-full-5-stages-lovable-refugee-path
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - assets/scripts/gates/stage-5a.mjs
  - assets/scripts/gates/stage-5b.mjs
  - tests/gates/stage-5a-not-runnable-regression.test.ts
  - tests/gates/stage-5a-full-gate.test.ts
  - evals/adversarial/fid-06-frost-recurrence/fixture-builder.mjs
  - evals/adversarial/fid-06-frost-recurrence/run.test.ts
  - skills/atoms/system/scaffold-component.md
  - references/wodtke-ia.md
  - references/spencer-card-sort.md
  - evals/fixtures/budget/new-product-full.fixture.json
autonomous: true
requirements:
  - FID-06
  - ATOM-15
  - MVPB-02
  - MVPB-05
  - COST-03
  - COST-04

must_haves:
  truths:
    - "gate-stage-5a.mjs returns {kind:'pass'} (not not_runnable) when design/interactions/ has ≥1 .spec.md file and full checklist passes"
    - "gate-stage-5a.mjs still returns {kind:'not_runnable', reason:'stage-4-artifacts-absent'} when interactions/ is empty or missing (Phase 2 backward compat)"
    - "stage-5a-not-runnable-regression.test.ts tests 1+2+4+5 still pass (empty/no interactions/ cases); test 3 (v2.0a hard-code) replaced with a passing Phase 3 full-gate test in the SAME commit as stage-5a.mjs modification (OQ-1 resolution)"
    - "gate-stage-5b.mjs returns {kind:'not_runnable', reason:'frost-recurrence-not-met'} with BLOCKER findings when any promoted component appears < 3× across wireframes/ + interactions/"
    - "FID-06 adversarial suite: component appearing exactly 2× is blocked by gate-stage-5b.mjs with finding 5b-frost-002"
    - "scaffold-component.md ATOM-15 exists with ≤200 char description"
    - "new-product-full.fixture.json exists at 150k token ceiling"
    - "All prior 815 tests + Wave 1 (03-01, 03-02) tests still pass after this plan"
  artifacts:
    - path: "assets/scripts/gates/stage-5a.mjs"
      provides: "Conditional gate: not_runnable when empty; full 4-condition checklist when non-empty"
      contains: "runFullStage5aChecklist"
    - path: "assets/scripts/gates/stage-5b.mjs"
      provides: "Hard BLOCKER when Frost recurrence < 3×"
      contains: "frost-recurrence-not-met"
    - path: "tests/gates/stage-5a-not-runnable-regression.test.ts"
      provides: "Updated: test 3 narrows to allow full-gate behavior when interactions/ non-empty"
      contains: "D-60"
    - path: "tests/gates/stage-5a-full-gate.test.ts"
      provides: "New: covers full Stage 5a checklist path (D-60)"
      contains: "runFullStage5aChecklist"
    - path: "evals/adversarial/fid-06-frost-recurrence/run.test.ts"
      provides: "FID-06 adversarial: component at 2× blocked with BLOCKER finding 5b-frost-002"
      contains: "frost-recurrence-not-met"
  key_links:
    - from: "assets/scripts/gates/stage-5a.mjs"
      to: "design/interactions/*.spec.md"
      via: "readdir(interactionsDir) count check — conditional branch"
      pattern: "specFiles.length"
    - from: "assets/scripts/gates/stage-5b.mjs"
      to: "design/wireframes/**/*.excalidraw + design/interactions/*.spec.md"
      via: "Frost recurrence counter scanning both artifact types"
      pattern: "frost-recurrence"
---

<objective>
Promote Stage 5a and Stage 5b gates from their Phase 2 stub/lite state to full enforcement. This is the highest regression-risk plan in Phase 3: the existing `stage-5a-not-runnable-regression.test.ts` test 3 explicitly asserts the v2.0a hard-code behavior that Phase 3 removes — modifying `stage-5a.mjs` without updating this test in the SAME commit will break the CI green baseline. The OQ-1 resolution mandates atomic commit. Stage 5b promotion adds the hard BLOCKER for Frost ≥3× recurrence (D-70). Also delivers ATOM-15 scaffold-component and the final 2 reference files completing REF-03.

Purpose: Gate promotions are prerequisites for 03-03 executors to run wave-2 work. Stage 5a returning PASS (not not_runnable) is ROADMAP SC-1. Stage 5b Frost enforcement is ROADMAP SC-3.

Output: Modified gate files, updated + new gate tests, FID-06 adversarial suite, ATOM-15, 2 reference files, budget fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-CONTEXT.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-RESEARCH.md
@.planning/PROJECT.md
@skills/workflows/INVARIANTS.md
@.planning/phases/01-v1-5-infrastructure-determinism-foundation/01-02-SUMMARY.md
@.planning/phases/02-v2-0a-skeleton-4-stages-end-to-end-lite-mode-stage-5a-5b/02-04-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-01-SUMMARY.md
@.planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-02-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Do not explore codebase — use these directly. -->

From assets/scripts/gates/stage-5a.mjs (current — MODIFY, do not rewrite):
  export async function runStage5aGate(designDir)
  Currently: always returns { kind:'not_runnable', reason:'stage-4-artifacts-absent' }
  D-43 comment on line with "v2.0a hard-coded terminal state" is the line to replace.
  GATE-07 and GATE-08 comments MUST remain in the file after modification (OQ-1: these string checks in test 4 must still pass).

From assets/scripts/gates/stage-5b.mjs (current — MODIFY):
  Currently returns { kind:'pass_with_warnings' } with informational Frost note (finding 5b-frost-001, INFO)
  Phase 3: upgrade to { kind:'not_runnable', reason:'frost-recurrence-not-met' } when any component < 3×

From tests/gates/stage-5a-not-runnable-regression.test.ts (current — MODIFY atomically with stage-5a.mjs):
  Test 1: no interactions/ → not_runnable (KEEP — still valid)
  Test 2: empty interactions/ → not_runnable (KEEP — still valid)
  Test 3: "returns not_runnable even when interactions/ HAS real files (v2.0a hard-code)" → REPLACE
  Test 4: GATE-07/GATE-08 string check → KEEP (strings still in file per OQ-1)
  Test 5: GateResult shape check → KEEP (still valid)
  The describe block title can remain "D-43 regression guard" — just rename test 3.

Stage 5a full checklist conditions (D-60):
  1. Every sitemap route has a state spec (filesystem check via globby)
  2. design/wireframes/*/CHOICE.md exists for ≥1 screen
  3. design/tokens.json is DTCG-valid (call existing dtcg-lint.mjs or tokens-project.mjs validation)
  4. tokens.json frontmatter: stage:5a (not 5a-lite) AND evidence: proto|validated (not INFERRED)

Stage 5b Frost counter (D-61):
  Scan .excalidraw files for component label occurrences (case-insensitive label match)
  Scan .spec.md files for component name references in body text (case-insensitive)
  Total per-component count from both sources
  Component with count < 3 → BLOCKER finding 5b-frost-002

FID-06 adversarial fixture structure (RESEARCH.md §9.2):
  Component "Button" appearing exactly 2×: 1 wireframe mention + 1 spec mention
  Assert: gate returns { kind:'not_runnable', reason:'frost-recurrence-not-met' } with finding 5b-frost-002
  Mirrors Phase 2 evals/adversarial/red-05-synthetic-block/ structure

Budget fixture format (matches existing evals/fixtures/budget/ files):
  { route: 'new-product', budget: 150000, stages: { ingest: 5000, discover: 30000, structure: 25000, sketch: 25000, interact: 30000, style: 25000, systematize: 10000 } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>T-03-03-A: gate-stage-5a.mjs promotion — ATOMIC commit with regression test update (OQ-1 CRITICAL)</name>
  <files>
    assets/scripts/gates/stage-5a.mjs,
    tests/gates/stage-5a-not-runnable-regression.test.ts,
    tests/gates/stage-5a-full-gate.test.ts
  </files>
  <behavior>
    stage-5a-not-runnable-regression.test.ts (keep passing, update test 3):
    - Test 1 (keep): no interactions/ → not_runnable, reason:'stage-4-artifacts-absent'
    - Test 2 (keep): empty interactions/ (hidden files only) → not_runnable, reason:'stage-4-artifacts-absent'
    - Test 3 (REPLACE): "Phase 3: returns full-gate result when interactions/ has real spec files" → MUST PASS after gate modification
    - Test 4 (keep): GATE-07, GATE-08, 'stage-4-artifacts-absent' strings present in stage-5a.mjs
    - Test 5 (keep): GateResult shape valid

    stage-5a-full-gate.test.ts (new):
    - Test A: full-gate fixture with valid .spec.md files + sitemap.json + tokens.json + CHOICE.md → {kind:'pass'} or {kind:'pass_with_warnings'}
    - Test B: full-gate fixture missing tokens.json → finding with fixRecipe referencing tokens.json
    - Test C: full-gate fixture with tokens.json frontmatter stage:'5a-lite' → finding about outdated stage marker
    - Test D: full-gate fixture with zero CHOICE.md files → finding about missing CHOICE.md
    - Test E: full-gate fixture where not all sitemap routes have spec files → finding about coverage gap
  </behavior>
  <action>
    CRITICAL: The stage-5a.mjs modification and the test 3 replacement MUST be in a SINGLE COMMIT. Never separate them. This prevents CI from running in a state where the gate is modified but the old "always not_runnable" test assertion still asserts the removed behavior.

    Step 1 — Write new tests FIRST (before modifying the gate):
    - Update tests/gates/stage-5a-not-runnable-regression.test.ts: replace test 3 with "Phase 3: returns full-gate result when interactions/ has real spec files" — this test should FAIL currently (the gate still hard-codes not_runnable).
    - Write tests/gates/stage-5a-full-gate.test.ts with tests A-E above — all should FAIL currently.
    - Run npx vitest run tests/gates/stage-5a*.test.ts — confirm test 3 FAILS and tests A-E FAIL (gate not yet modified). Commit: test(03-03): add failing D-60 gate-stage-5a full-gate tests

    Step 2 — Modify stage-5a.mjs (fills the conditional logic per D-60):
    Extend the existing gate file (do NOT rewrite from scratch):
    - Keep the GATE-07 + GATE-08 comment header intact.
    - Keep the existsSync(interactionsDir) → not_runnable branch.
    - Keep the realFiles filter for hidden files.
    - REPLACE the "D-43 v2.0a hard-coded terminal state" block with the conditional:
      if (specFiles.length === 0) { return { kind:'not_runnable', reason:'stage-4-artifacts-absent' }; }
      // D-60: run full checklist when interactions/ is non-empty (Phase 3)
      return runFullStage5aChecklist(designDir, specFiles);
    - Implement runFullStage5aChecklist(designDir, specFiles) as an internal function:
      Condition 1: for each sitemap route (from design/ia/sitemap.json), verify corresponding .spec.md exists.
      Condition 2: glob design/wireframes/*/CHOICE.md — must find ≥1.
      Condition 3: validate design/tokens.json against DTCG schema (use existing validate.mjs).
      Condition 4: parse tokens.json frontmatter — stage must be '5a' (not '5a-lite'); evidence must be 'proto' or 'validated'.
      On any failure: return { kind:'pass_with_warnings', evidence:'proto', findings:[...] } (warnings, not hard block — full block is for hard-gate failures in later Phase 4).
      On all pass: return { kind:'pass', evidence:'proto' }.
    - Filter spec files (entries ending in .spec.md only) before passing to the checklist.

    IMPORTANT: This modification is a single commit that includes BOTH the modified stage-5a.mjs AND the updated stage-5a-not-runnable-regression.test.ts (test 3 replacement) AND the new stage-5a-full-gate.test.ts.

    Run npx vitest run tests/gates/stage-5a*.test.ts — ALL tests must pass.
    Run npx vitest run — full suite must pass (no regressions).

    Commit: feat(03-03): gate-stage-5a D-60 full-gate promotion — atomic with regression test update (OQ-1)
  </action>
  <verify>
    <automated>npx vitest run tests/gates/stage-5a-not-runnable-regression.test.ts tests/gates/stage-5a-full-gate.test.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 existing regression tests pass (tests 1, 2, 4, 5 unmodified; test 3 updated). All 5 new full-gate tests (A-E) pass. Full vitest suite shows 0 regressions from pre-plan baseline. The single commit contains both stage-5a.mjs modification and test update (git log confirms). GATE-07, GATE-08, 'stage-4-artifacts-absent' strings still present in stage-5a.mjs.</done>
</task>

<task type="auto" tdd="true">
  <name>T-03-03-B: gate-stage-5b.mjs Frost hard BLOCKER + FID-06 adversarial suite (D-70)</name>
  <files>
    assets/scripts/gates/stage-5b.mjs,
    evals/adversarial/fid-06-frost-recurrence/fixture-builder.mjs,
    evals/adversarial/fid-06-frost-recurrence/run.test.ts,
    tests/gates/stage-5b-frost.test.ts
  </files>
  <behavior>
    - Test 1: gate-stage-5b.mjs with component "Button" appearing 2× (1 wireframe + 1 spec) returns {kind:'not_runnable', reason:'frost-recurrence-not-met'} with finding {findingId:'5b-frost-002', severity:'BLOCKER'}
    - Test 2: gate with component appearing exactly 3× returns no frost-recurrence-not-met finding
    - Test 3: gate with component appearing 0× in wireframes but 3× in specs returns no frost-recurrence-not-met (count from both sources combined)
    - Test 4: gate with component appearing 1× wireframe + 1× spec = 2× total → BLOCKER
    - Test 5: FID-06 adversarial run.test.ts: fixture-builder.mjs produces component at exactly 2× total recurrence; gate-stage-5b.mjs returns BLOCKER 5b-frost-002
    - Test 6: the old test asserting 'status: na' informational Frost note (finding 5b-frost-001, INFO) — this test NO LONGER exists (replaced by BLOCKER behavior)
  </behavior>
  <action>
    RED: Write tests/gates/stage-5b-frost.test.ts with tests 1-4 above. Note: test 6 is a reminder to DELETE the old INFO-level Frost test from existing stage-5b test files (if present). Run vitest — tests 1-4 must FAIL (gate currently returns INFO not BLOCKER). Commit: test(03-03): add failing FID-06 BLOCKER tests for gate-stage-5b

    GREEN (upgrade stage-5b.mjs):

    Modify assets/scripts/gates/stage-5b.mjs — per D-61 / D-70:
    - Keep all existing gate logic (tokens.json check, DESIGN.md check, schema validation).
    - REPLACE the informational Frost note (finding 5b-frost-001, INFO) with the hard BLOCKER scan:

    Add function countComponentRecurrences(designDir):
    - Glob `designDir/wireframes/**/*.excalidraw` — for each file, parse JSON, collect element labels (element.label, case-insensitive). Build per-component occurrence map.
    - Glob `designDir/interactions/*.spec.md` — for each file, read body text, find component name references (case-insensitive string match). Add to the same occurrence map.
    - Return map: { [componentName: string]: number }.

    In the gate main flow (after tokens.json and DESIGN.md checks):
    - If no tokens.json (early not_runnable), skip Frost check.
    - Parse promoted components from tokens.json (the component-tier keys extracted from DTCG token structure).
    - For each promoted component, check countComponentRecurrences(designDir)[componentName] >= 3.
    - Components with count < 3 → BLOCKER finding: { findingId:'5b-frost-002', stage:'5b', severity:'BLOCKER', evidence:{ component:name, count:n, threshold:3 }, fixRecipe:`Component '${name}' appears ${n}× — requires ≥3 per Frost atomic design discipline (FID-06). Ensure it appears in at least 3 wireframes or interaction specs before promotion.` }.
    - If any frost BLOCKER: return { kind:'not_runnable', reason:'frost-recurrence-not-met', findings:[...blockerFindings] }.

    Create evals/adversarial/fid-06-frost-recurrence/ suite:
    - fixture-builder.mjs: creates a temp design dir with:
      - tokens.json containing one promoted component "Button" in component-tier.
      - wireframes/login/v3.excalidraw with element label "Button" (1× wireframe).
      - interactions/login.spec.md with body text referencing "Button" once (1× spec).
      Total recurrence: 2× (below threshold of 3).
    - run.test.ts: builds fixture, runs gate-stage-5b.mjs, asserts result.kind === 'not_runnable', result.reason === 'frost-recurrence-not-met', result.findings[0].findingId === '5b-frost-002', result.findings[0].severity === 'BLOCKER'.

    Commit: feat(03-03): gate-stage-5b FID-06 hard BLOCKER + adversarial suite (D-70)
  </action>
  <verify>
    <automated>npx vitest run tests/gates/stage-5b-frost.test.ts evals/adversarial/fid-06-frost-recurrence/run.test.ts && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>4/4 stage-5b frost tests pass. FID-06 adversarial: component at 2× returns BLOCKER 5b-frost-002. Full vitest suite ≥ all prior tests, zero regressions. tsc --noEmit clean. lint-determinism clean on stage-5b.mjs.</done>
</task>

<task type="auto">
  <name>T-03-03-C: ATOM-15 scaffold-component + 2 reference files + budget fixture</name>
  <files>
    skills/atoms/system/scaffold-component.md,
    references/wodtke-ia.md,
    references/spencer-card-sort.md,
    evals/fixtures/budget/new-product-full.fixture.json
  </files>
  <action>
    Create skills/atoms/system/scaffold-component.md — ATOM-15:
    - Frontmatter: name: complete-design/system/scaffold-component, description ≤200 chars (e.g., "Stage 5b: Scaffold a component that recurs ≥3× — creates DTCG token entry, Mermaid diagram stub, and wiring notes."), type: atom, stage: 5b, compatibility: [claude-code, codex-cli, cursor].
    - Procedure:
      1. Verify the target component passes gate-stage-5b Frost recurrence check (≥3× in wireframes + interactions combined). Run: `node bin/complete-design.mjs gate --stage 5b --staged .complete-design/preview/<run-id>/`.
      2. Create DTCG token entry in design/tokens.json: component-tier key for the component, referencing primitive and semantic tokens established in Stage 5a.
      3. Create component scaffold: design/components/<component-name>.md with wiring notes (props table, state enum, accessibility requirements from APG pattern).
      4. Emit Mermaid component state stub: a minimal stateDiagram-v2 for any stateful component (e.g., Button has loading/disabled/default states).
    - FID-06 guidance: "Only scaffold components that appear ≥3× in upstream wireframes and interaction specs. Gate will block if count < 3."
    - References: @references/apg.md, @references/material-3.md.

    Create references/wodtke-ia.md:
    - Christina Wodtke "Information Architecture: Blueprints for the Web" (3e) — card sorting methodology, affinity diagramming, content inventory, taxonomy design. Key concepts: top-down vs bottom-up IA, faceted navigation, scent of information. Cite: Wodtke & Govella (2009) O'Reilly; Wodtke (2023) 3rd ed. Stage: 2 (IA cross-reference for reverse-engineer).

    Create references/spencer-card-sort.md:
    - Donna Spencer "Card Sorting: Designing Usable Categories" — how to run card sorting studies, open vs closed sorts, analysis of sort results, tree-testing follow-up. Cite: Spencer (2009) Rosenfeld Media. Reference to Optimal Workshop for digital card sorting (v2.1+ ingestion deferred per DEFERRED section). Stage: 2 (IA planning).

    Create evals/fixtures/budget/new-product-full.fixture.json:
    {
      "route": "new-product",
      "description": "Full 5-stage new-product route (v2.0b)",
      "budgetTokens": 150000,
      "stageAllocations": {
        "ingest": 5000,
        "discover": 30000,
        "structure": 25000,
        "sketch": 25000,
        "interact": 30000,
        "style": 25000,
        "systematize": 10000
      },
      "notes": "D-66: per-stage ceilings are independent — no headroom donation between stages. Source: CONTEXT.md D-66, ROADMAP SC-5, COST-03 (sketch p50 ≤25k), COST-04 (interact p50 ≤30k), COST-07 (full ≤150k)."
    }

    Verify description length: scaffold-component.md description ≤200 chars.
  </action>
  <verify>
    <automated>node -e "const {readFileSync}=require('fs');const m=require('gray-matter');const p='skills/atoms/system/scaffold-component.md';const d=m(readFileSync(p,'utf8')).data;if(d.description.length>200)throw new Error('desc too long: '+d.description.length);console.log('PASS',d.description.length,'chars')" && node -e "const d=JSON.parse(require('fs').readFileSync('evals/fixtures/budget/new-product-full.fixture.json','utf8'));if(d.budgetTokens!==150000)throw new Error('wrong budget');const sum=Object.values(d.stageAllocations).reduce((a,b)=>a+b,0);if(sum!==150000)throw new Error('allocations sum to '+sum+' not 150000');console.log('PASS budget sums to 150000')" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>scaffold-component.md exists with ≤200 char description. Budget fixture sums to 150000 across 7 stages. 2 reference files exist. Full vitest suite passes (all prior tests green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .excalidraw element labels → Frost counter | Labels are strings from LLM-generated wireframes. Case-insensitive matching prevents trivial bypass. |
| gate-stage-5a full checklist | Reads design/tokens.json and sitemap.json from designDir. Path validated by caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | stage-5a full checklist designDir path | mitigate | Validate designDir exists before readdir. Use join() not string concatenation for sub-paths. |
| T-03-03-02 | Repudiation | Frost counter finds 0 components in tokens.json | accept | If no component-tier tokens exist, Frost check is vacuously satisfied (nothing to promote). Gate continues to other checks. |
| T-03-03-03 | Tampering | stage-5b Frost count — component names with regex special chars | mitigate | Use literal string matching (case-insensitive includes), not regex, for component name search in spec.md bodies. |
| T-03-03-04 | Information Disclosure | FID-06 adversarial fixture in temp dir | accept | Fixture is synthetic test data, no PII. Temp dir is cleaned up after test run. |
</threat_model>

<verification>
After all 3 tasks complete:

1. npx vitest run — ≥ all prior tests (815 + Wave 1 new tests), all passing
2. npx tsc --noEmit — zero errors
3. Stage 5a tests: npx vitest run tests/gates/stage-5a-not-runnable-regression.test.ts tests/gates/stage-5a-full-gate.test.ts — all pass
4. FID-06 adversarial: npx vitest run evals/adversarial/fid-06-frost-recurrence/run.test.ts — BLOCKER asserted
5. git log --oneline -5 — confirm the atomic stage-5a commit (gate modification + test update in single commit)
6. node -e check — scaffold-component.md description ≤200 chars
7. Budget fixture sums to 150000 across 7 stage allocations
</verification>

<success_criteria>
- gate-stage-5a.mjs: full checklist path implemented; not_runnable preserved for empty/missing interactions/; GATE-07/GATE-08/stage-4-artifacts-absent strings still present
- stage-5a-not-runnable-regression.test.ts test 3 replaced AND stage-5a.mjs modified in ONE commit (OQ-1)
- gate-stage-5b.mjs: Frost recurrence BLOCKER (not_runnable with 5b-frost-002) when count < 3
- FID-06 adversarial: component at 2× total → BLOCKER confirmed in CI
- ATOM-15 scaffold-component.md with ≤200 char description and proper frontmatter
- Budget fixture new-product-full.fixture.json sums to 150000 exactly
- All prior tests still passing (zero regressions from 815 baseline + Wave 1 additions)
- ROADMAP SC-1 (Stage 5a PASS path) and ROADMAP SC-3 (Stage 5b Frost count-enforced) now testable
</success_criteria>

<output>
After completion, create .planning/phases/03-v2-0b-full-5-stages-lovable-refugee-path/03-03-SUMMARY.md with:
- phase: 03-v2-0b-full-5-stages-lovable-refugee-path / plan: 03 / subsystem: gate-promotions
- dependency_graph.provides: stage-5a.mjs (full gate), stage-5b.mjs (hard BLOCKER), updated regression tests, FID-06 adversarial suite, ATOM-15, 2 reference files, budget fixture
- Record the git commit SHA of the atomic stage-5a modification commit (OQ-1 evidence)
- Record which of the 4 Stage 5a full checklist conditions triggered warnings on the test fixture
</output>
