// next.config.mjs
// Minimal Next.js 15 config for design-os e2e fixture.
// Used by registry.mjs detectStack() to identify this as a Next.js project.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
