# evals/triggers/interact/triggers.yaml
# Trigger evaluation prompts for complete-design/interact skill.
#
# shouldFire: prompts that should activate the interact workflow
# shouldNotFire: prompts that should NOT activate the interact workflow
#
# Minimum: 12 prompts per category (recall ≥0.85, false-trigger ≤0.15 per R15)
# Phase 3 tuning: added prompts for XState, IxD, state-catalog terminology (D-32)
#
# Source: PLAN.md 03-02 Task C, 03-05 Task C; CONTEXT.md D-32
# Implements: WF-05 trigger corpus

skill: complete-design/interact

shouldFire:
  - "map interaction states for the dashboard"
  - "enumerate loading and error states for checkout"
  - "create state machine for the auth flow"
  - "run stage 4 IxD on search results screen"
  - "build state diagram for notification system"
  - "add interaction design to profile screen"
  - "create state catalog for onboarding wizard"
  - "map loading/empty/error/success states"
  - "generate XState machine for the data table"
  - "run interact workflow on admin panel"
  - "enumerate UI states for the upload feature"
  - "IxD state catalog for settings page"
  - "create interaction spec for the login screen"
  - "map all states for the shopping cart"
  - "build XState v5 machine for the multi-step form"
  - "create a Mermaid stateDiagram for the feed screen"
  - "enumerate all loading states and transition flows for the search feature"

shouldNotFire:
  - "create wireframe sketches"
  - "generate token colors"
  - "audit slop tells"
  - "build react component"
  - "run stage 3 on admin screen"
  - "deploy the app"
  - "create a persona"
  - "map the sitemap"
  - "generate DESIGN.md"
  - "systematize design system"
  - "run style workflow"
  - "reverse engineer the prototype"
  - "run stage 2 structure workflow"
  - "create low-fidelity wireframe"
  - "run audit --all-stages"
  - "generate token palette"
  - "create crazy eights variants"
